# Verification

[ARCHITECTURE.md](ARCHITECTURE.md) defines behavior; this document defines evidence required to claim it. Exercise production interfaces, real SQLite transactions and independent expected results. Crash tests terminate a fixture process and reopen in a fresh process; injected returned errors are fault tests, not crash evidence. Record tested revision, machine, dependency versions, inputs and measurements. Prototypes, source inspection, compilation and accepted decisions are not production qualification; [research evidence](research/README.md) retains their scope and limitations.

For clean-start implementation, verify that the production build graph contains only the redesigned path and deliberately retained dependencies. Classify extracted tests against the accepted contract; obsolete behavior is not a compatibility requirement. The first complete slice must prove original request-answer recovery, no dispatch after rollback, indeterminate tool outcomes without replay after lost custody, safe delayed cleanup and isolation of established processing-result bindings from later Turns, with queued admissions recovered before selection.

## Canonical gates

Confirm commands in [build.zig](build.zig). `zig build check` covers source validation, native ReleaseSafe tests and ReleaseSmall deliverables. Evaluator/protocol/QuickJS/build-graph changes also run `zig build workflow-check`, including applicable sanitizer, mutation/property, fuzz and leak checks. Dependency, build, persisted-format and CI-bootstrap changes additionally run from an empty cache. Documentation-only changes check references, consistency and `git diff --check`. Measurement provenance reports a Git revision and dirty state only when `git -C` resolves the exact measurement root as its top level; extracted packages outside Git or nested below an unrelated repository report both fields as unavailable.

Production measurement runners use the Go 1.27.1 toolchain pinned by `research/go.mod` and only the modules pinned by `research/go.sum`. Run `GOTOOLCHAIN=local go -C research mod download`, then `zig build measurement-check` before an applicable `zig build measure-*` step. Every result records the exact Go and gopsutil versions, `go.sum` hash, binary hash, platform, Git revision and dirty state. The Go measurement dependency enters neither the Zig Host nor ordinary production clients; Python remains only where an unrelated integration test or research probe owns it.

Release evidence includes deterministic Session, model, tool, permission, context, compaction, workflow and server fixtures; hard-termination recovery at each distinct acknowledgement/effect boundary; compiler-backed discovery of both production graphs; and the opt-in live Codex repair through those same owners. Classify intentionally unused/conditional/test declarations and remove obsolete interfaces rather than hiding them. Never label interrupted/unrun checks passed. Live provider calls remain opt-in.

### Product tenets at stage completion

Each implementation stage must demonstrate the following through its completed caller flow, alongside its correctness and recovery checks:

- **Simplicity:** explain the required behavior or demonstrated cost that justifies each added module, durable fact, cache or queue. Check for duplicate authority and caller-owned coordination that belongs inside an existing owner; compare a simpler approach where one could preserve the required behavior. Identify dependencies introduced or removed on another owner's representation, call order or lifetime; justify each retained dependency by required behavior. Passing tests establishes tested behavior, not design simplicity.
- **Explainability:** walk ordinary work, failure and recovery in plain language using the stage's normal results and inspection. A reader should be able to tell what happened, what remains uncertain and what can happen next. Explanations must follow owning facts without treating optional diagnostics as recovery authority. Check this as each caller path appears, before final presentation work.
- **Memory footprint:** rerun comparable fixtures as resource owners are added, recording aggregate peak and retained-idle footprint, including native libraries and Latifa helpers. At fixed capacity, vary payload and durable-history populations independently and exercise fill/drain with delayed cleanup or failure. Explain changes from the preceding measurement and resolve unexplained growth before dependent work builds on it. Use the existing [resource measurements and targets](#resources-and-accepted-host-qualification-targets); stage checks do not replace qualification of the complete simultaneous workload or introduce per-module quotas.

Record these checks with the stage's existing evidence; no separate scorecard or reporting system is required.

## Platform qualification

Run applicable behavior, recovery and resource gates on the available Mac. For Linux, record portable-API/dependency evidence, cross-compilation and unexecuted assumptions separately; no distribution/CPU runtime fleet is required. Known incompatibility needs an explicit response, not hidden fallback. Identify local Store support, accessible scratch and known memory-backed or unknown backing. Verify reproducible pinned dependencies, selected credential backend, locked/unavailable stores, access checks, account-preserving refresh and failed atomic save without plaintext fallback or credential inheritance. Linux plaintext storage does not imply protection from same-user Bash. Unavailable platform counters are not zero.

## Authority, Session identity and admission

- Reconstruct observations from owning canonical rows without resident Session state, historical Attempt/Completion entities or cross-owner table access. Operation owns current execution/retry facts and immutable Resolution/content; its ID identifies the result. Reject overwritten/cleared resolutions, foreign references, wrong digests, overflow and stale execution identities.
- First configuration without an explicit model or Workspace rejects without a partial Session. Verify initial defaults: empty instructions, Bash/Edit tools, `ask` permissions and text output; an explicit empty tools list disables tools. Later omissions preserve existing settings rather than reapplying defaults. Verify explicit empty instructions, empty tools and null outputSchema clear their respective settings, while omission leaves each unchanged and prior admitted model requests retain their frozen settings. Configure two Sessions from one JavaScript settings object, mutate it after invocation, and verify captured inputs remain unchanged and later updates to one Session do not affect the other.
- First complete configuration atomically creates Session, baseline, Workspace/access scope and original request answer; incomplete configuration and messages to unknown Session references reject without partial Sessions. Configuration with no queued input starts no work; an update that makes existing queued input resumable does not require another message to advance it. Later mutable updates apply linearly without comparing the original baseline; immutable-field changes reject. Omitted settings retain earlier references.
- Direct and workflow configuration/messages share core idempotency key semantics. Matching retries recover the original acceptance or definite rejection before reconsidering current conditions; changed inputs conflict. Recover the same accepted queue admission before selection, after processing binding and after final result or exclusion. Lost replies, later initialization/configuration, terminality and restart cannot turn rejection into acceptance, reapply old settings, enqueue duplicates or change an established processing-result binding. Multiple message keys selected by one Turn return its same original schema-bound outcome. Distinct keys preserve intentional identical requests; malformed envelopes without usable identity and uncommitted failures promise no saved answer.
- Pure `session(name)` creates no durable state or core request. Full Session references remain exact, opaque and unchanged across Workflows; distinguish Session reference, Workflow creation idempotency key, Workflow-and-Session-scoped call identity and opaque core idempotency key and internal Turn ID. Test namespace ambiguity, long/prefix/non-ASCII references, case and differently encoded Unicode without normalization. Verify different client namespaces separate equal local names, equal full references select the same Session across clients, core requires no Workflow lookup or namespace interpretation, and reuse does not reapply the receiving client's namespace. Verify coordinator Session references use `workflow/<canonical decimal id>/<exact name>`, including names with slashes and non-ASCII text, different Workflow IDs, replay and unchanged cross-client reuse. At the maximum positive signed-64 ID, 99 name bytes fit and 100 exceed the complete-reference bound; shorter IDs permit the corresponding remaining bytes. Core does not parse or reserve the coordinator prefix. Complete Session references accept exactly 128 UTF-8 bytes and reject 129 before Session mutation; cover multibyte values, namespace/component-encoding overhead and inputs whose local name fits but complete reference does not. Count the key value separately from transport escaping and qualify worst-case wire expansion. Verify core enforces only the complete-reference bound, without interpreting client components, truncating or normalizing; distinguish bytes from character count and Session population.
- Complete successive Turns in one linear Session; reject concurrent second Turns and branching. Queue admissions have no processing-Turn binding until selected. Successful selection binds and projects the eligible admission-order prefix atomically with Turn initiation or the next assistant-response Operation; compaction leaves it unselected. Race new message admission against selection and assistant-only settlement in both commit orders. Establish each selection/projection at most once; preserve input beyond the cutoff for the next boundary. Stopped/cancelled messages remain inapplicable and readable with reasons, without deletion or later revival.
- Run A, admit B, then fail A before selecting B: with no C, core automatically starts the next resumable Turn from B. B's caller remains pending through A's failure and receives the processing Turn's result. Compare B admitted before and after failure: both advance without another caller wake. Race C against successor admission: preserve queue order and single-Turn occupancy whether C joins the selected prefix or waits for the successor's next boundary. Retrying B's identity recovers that same admission/result, never another B or A's unrelated failure.
- Distinguish failure before selection from failure while processing selected input. If B was already selected, its processing Turn's failure is final for B and does not automatically queue it again. Reject a selected prefix during local preparation before projection: atomically bind that prefix to the typed failed outcome without a fake projection, Operation, manifest or Attempt; leave later input unbound. Rollback preserves the prior queue and bindings. Repeat deterministic preparation failure and verify it cannot repeatedly restart the same selected input. A failure with no eligible queued input creates no successor.
- Drop every queue notification after committed admission, terminal settlement, a blocking prerequisite clearing and restart; storage-backed discovery still advances eligible input under fair service. Saturate capacity, then release it: queued work keeps its identity/order and advances without a new submission, per-Session worker, wait flag, duplicate selection or busy loop. Crash before/after queue admission, selection, failure settlement, occupancy release and successor admission; reopen committed facts and continue once. Preserve established failed results and resolved Operations. Existing applied messages are not duplicated; uncertain Bash/Edit is not automatically replayed. Missing/corrupt/incompatible continuation still prevents unsafe progress; no visible-history fallback or queue-driven retry bypass. Distinguish explicit stop/cancellation from failure.
- Turn outcomes release Session occupancy atomically after all effect, permission and ordered-result obligations settle. Pre-request failure records selected-input provenance/bindings without a fake Operation/manifest/Attempt/projection; Operation-derived failure references the causing Operation's Resolution within that Turn. Preserve explicit causal identities and derived runnable/permission/in-flight/terminal conditions; retry waiting remains in-flight without live custody.

- Core keys share one Store-wide state-changing-command domain: race equal and conflicting inputs, including changed Session and kind; only one committed binding/mutation wins. Equal text in the separate Workflow creation domain remains independent. Exercise authorized cross-client recovery and denied/revoked observation without altering the saved answer. Through actual caller interfaces, distinguish saved acceptance/rejection, proven binding conflict and invocation failure without a trustworthy answer; storage failure cannot fabricate a saved rejection or replace an existing binding.
- Accept arbitrary exact UTF-8 core keys through 128 bytes, including empty strings, non-UUID strings, NUL and distinct Unicode encodings; reject 129 bytes without mutation. Core performs no UUID parsing or normalization. Qualify full public key bounds and worst-case transport escaping in recovery requests, acknowledgments and errors.
- Retain terminal and pending recovery bindings, intents and required content through cleanup and restart. Storage exhaustion or detectable lost/corrupt history cannot turn old identity into fresh work; no independent expiration is supported.

## Context, provider output and compaction

While a model request is active, queue message A, change model context, then queue message B. When both messages are selected at the next assistant-response input boundary, verify both use that request's selected context rather than settings copied at their message admissions. Race configuration against request selection in both commit orders: only configuration within the selected view affects the new request. Crash and reopen before selection and after Operation admission; pending input uses the subsequently selected view, while replacement Attempts retain the admitted view. Preserve ordered explicit instruction inclusion and separate action-authorization semantics.

Freeze the historical input/settings view, schema, tool catalog and provenance at model Operation admission. Append messages, change Session settings and accept previously admitted model/tool results afterward; restart and prove that the older view remains unchanged. Use result acceptance position, not producing Operation admission, for visibility. With unchanged adapter rules, reconstruction retains equivalent semantic input. Deliberately changed adapter rules may change selection/rendering from that same view without changing binding, output contract, authorization or allowance; no frozen adapter version is required. Fresh Operations use their own historical boundaries. No Turn-wide model-setting snapshot, model-request counter or absolute deadline substitutes for the owning scopes. Resume preserves the saved provider binding without fixture substitution.

Exercise the core-owned view through adapter consumers without database access: settings, applicable history, complete ordered provider output including private-only results, compaction item-to-end slices and tool results in originating call order despite reverse completion. Reject private reads through generic content access and foreign/expired handles; release all readers on preparation failure. Check shared-boundary reads across concurrent admissions without holding a transaction through adapter work. Admit compaction, append a message, then accept the compaction result. Recover the covered frontier from the producing Operation's admission position and keep the intervening message in the later suffix. Result acceptance controls base availability, not source coverage; no separate input list or coverage record is required.

Build complete provider requests into charged scratch before transport handoff. Vary object/string bytes and item count independently; preserve opaque strings, escaped keys, nested/unknown fields and complete compaction suffixes without whole-payload decoding or resident item collections. Compare against independent expected inputs and byte digests; inject mid-read, final-integrity, short-write and scratch-exhaustion failures and verify no incomplete request becomes dispatchable. Measure language allocations separately from SQLite/native/whole-Host memory. The historical-view and streaming prototypes in [research](research/README.md#provider-wire-contract) use synthetic output and do not qualify real encryption, full applicability/lineage, dispatch/cancellation integration or power-loss recovery.

Exercise instructions A→B→C, A→B→A and fresh A→A before another request: retain every explicit update in order, including values never used alone. Same-request retry and patches omitting instructions add nothing. Content deduplication cannot coalesce updates. Test changes during model/tool waits and compaction, entries already covered by compaction, and atomic entries/projections/Operation/manifest rollback. Request-admission rollback leaves updates pending; post-commit preparation failure does not undo their inclusion, and retries include them once. Instruction placement preserves tool pairs and operator authority; provider-produced lookalikes gain none; inclusion is not provider consumption.

Exercise supported Session settings through independent sparse updates, including reasoning effort, the offered Bash/Edit subset, context policy and default model output limits, during active work and across restart. Verify persistence and the ordinary request/permission invariants. Reject changes to Session identity or Workspace/access scope, unsupported values, and known continuation incompatibility atomically, including mixed updates with otherwise valid fields; preserve the prior configuration. Session configuration cannot change Host startup policies. Model/provider changes require adapter compatibility evidence rather than silently dropping saved continuation. This covers supported settings, not hypothetical fields or unproved provider capabilities.

Through real provider interpretation and settlement, cover:

- authentication refresh/account continuity, DNS/TLS, fragmentation at every SSE/JSON boundary, escaping, terminal records, partial/disconnected/late streams, cancellation, post-commit materialization failure and deterministic incompatibility;
- optional schema absence producing text, supported structured output, schema changes/clearing after request admission, unsupported schemas, invalid output, refusal and incomplete generation; no prose conversion, fabricated success, schema revalidation against later settings or automatic model repair;
- assistant text plus three ordered calls; malformed final call, duplicate identity, unknown tool or invalid arguments rejects the complete candidate. Child Operations/permissions and output publish atomically; independent Bash/Edit execution and arbitrary completion order preserve call-ordered results without Workspace isolation or sibling head-of-line blocking;
- a new message before assistant-only settlement preventing premature Final Answer; permission mode changes affecting Action admission rather than fixed decoding; stop before acceptance rejecting late consequences;
- immutable Operation-owned output with exact opaque/encrypted reasoning, signatures, continuation and unknown fields inside supported open records, stored once. Derived replay drops only response-only fields. Unknown consequential unions retain bounded rejection/provenance without Conversation/continuation/effects or mandatory permanent raw payload;
- loss of provider caches causing equivalent local reconstruction; corrupt/missing/incompatible continuation failing explicitly without dropping private meaning or rebuilding from visible text alone.

Use two fixture consumers of the private interfaces: deterministic effect evidence for core ordering/recovery, and fake credentials/transport feeding raw provider frames through the real lowering, capture and interpreter. The first cannot qualify provider grammar; the second cannot bypass validation by supplying already-approved Actions. Compare requests/results with independent expectations. Existing `model_operation.Provider`, `deterministic_provider.zig` and `codex_harness_test.zig` demonstrate historical consumers, not the redesigned interfaces: the current adapter discards reasoning and the current tool catalog still uses Git-backed Patch. Do not preserve those formats or caps as compatibility obligations.

Inject missing credentials, rejected refresh, account mismatch, failed atomic credential save and concurrent refresh consumers. No failed credential preparation dispatches a model request or falls back to another backend; readers cannot observe partially replaced credentials. HTTP authentication rejection must not cause a hidden resend. Distinguish DNS/TLS, HTTP status, body/capture failure and protocol rejection, preserving available Retry-After/correlation and absent observations accurately. Hold header reads and teardown callbacks across cancellation; credentials/request buffers survive their consumers and release afterward without logging or child inheritance. These fixture cases do not qualify live authentication or platform credential backends.

Parse sealed provider output once using the serial workspace and one sequential metadata file. Vary bytes, item count and nesting independently; verify bounded state, immutable source/range lifetime, scratch charging and no per-item file/resident list or queue of prepared responses. Late validation, source-read, allocation, scratch or import failure publishes no partial content/reference or semantic consequence. Fixed-content checks cannot decide current permissions, retry eligibility or Turn completion. Ordinary sequential parsing and atomic SQLite publication may block; measure both rather than requiring a worker or manually resumable parser.

At capture→validation→import boundaries, hold source reads open while interruption, a late callback and cleanup compete. Assert a single writer before seal, immutable source through range reads/commit/rollback, no writes after seal and no scratch-range escape as a canonical reference. Fill execution capacity with completed captures while delaying the serial workspace: captures retain custody and bounded resources, with no extra prepared-payload population. While validation is delayed, release finished transport/request resources with no remaining consumer, then reread the unchanged sealed source under its original Operation/Attempt provenance while custody stays occupied. Release metadata/source at their last consumer and callbacks only at safe cleanup. Fail each construction/open/seal/read/import boundary and account for every acquired resource.

Compaction fixtures cover approximate trigger/output reservation, provider usage plus newly appended estimates, repeated compaction, intact call/result pairs, source changes during summarization, acknowledgement loss, malformed/empty/oversized/stale/incomplete results and complete lineage/frontier coverage. Never reject/truncate messages solely at the soft trigger. Select the newest accepted base, then validate structural/provider compatibility; unresolved compaction cannot replace it and an invalid selected base cannot trigger an older-base search. Preserve canonical Conversation and exact ordered suffix/continuation. Confirmed overflow resolves the old Operation, then compacts and creates a new manifest; exhaustion fails explicitly without silently dropping history.

The [provider wire research fixture](research/README.md#provider-wire-contract) is a synthetic serialization/storage model, not the adapter. Its independent golden inputs cover ordered reasoning/text/calls, open-field retention and response-only `created_by` removal. Production qualification must run the same cases through sealed SSE output, the real interpreter, atomic Operation-owned import and fresh-process request materialization. Include complete-versus-added encrypted content, duplicate/missing indices or identities, terminal disagreement, unknown phase/content/annotation/control variants, late malformed calls and rollback. Preserve IDs, status, phase, raw argument strings and private metadata; do not mistake SDK convenience fields for raw-wire response-only fields. Test all generic content and report/export paths, not just Conversation projection, against private reasoning/compaction access.

For the selected subscription compaction route, verify a final request-only `compaction_trigger`, `store:false`, complete frozen source input and successful terminal output with exactly one supported nonempty compaction item. Reopen using the same producing Operation/ordinal; reconstruct retained original host inputs plus the output slice and complete later suffix without duplicates, truncation or a second durable replay object. Cover multiple successive bases, messages/instructions arriving during compaction, no compaction Conversation projection, unexpected Action rejection and a retained-input window that still cannot fit. Generic API standalone/automatic-compaction examples do not qualify this subscription route; automatic response compaction stays disabled until separately evidenced.

Keep requested model, each available served-model observation, `x-request-id` and response ID through actual production restart; absent evidence must remain absent. Source-level same-binding replay and synthetic strings do not prove backend acceptance of encrypted state. Opt-in live qualification must exercise same-model reasoning → tool results → continuation, cache-independent full local reconstruction, compaction → continuation and unavailable/incompatible saved state. Record the requested model and served observations for each call without credentials or private payload publication. No cross-model continuation, subscription standalone endpoint availability, provider retention guarantee, production privacy boundary or power-loss durability is established by this research.

## Execution, retries and permission

Terminate before/after reservation, Attempt admission, dispatch, scratch seal, retry settlement, final settlement and observation. Before admission rollback, no new identity, consumed allowance or permit survives. After commit, recover exact current facts and conservative uncertainty without recreating the one-shot Dispatch Permit. Lost scratch/preparation is not durable evidence. Final resolution, required content and consequences commit together. Retry settlement updates current failure/eligibility/accounting while leaving Resolution absent; fresh admission revalidates everything and cannot duplicate a stale due selection.

Exercise the [cross-owner boundaries](ARCHITECTURE.md#commit-boundaries-and-crash-recovery) through direct core calls and Runtime's ordinary core client. At each cut assert the committed answer/Operation facts, who retains permit/custody/source handles, whether that owner remains usable, and which owner makes the next recovery call. Observe launch with an independent fixture counter: rollback launches zero; one permit launches at most once; crash after admission but before launch still makes tools indeterminate. Fail the first post-commit preparation, authentication or spawn step and verify the reserved owner still retains its Attempt binding and cleanup responsibility. Contrast live known preparation failure with death before that failure is saved; neither refunds consumed allowance. Apply existing retry policy rather than retrying every preparation error.

Seal valid output, then fail validation/import or terminate before settlement commit. Assert no partial Resolution, continuation, Tool Results or released Turn occupancy; fresh recovery must not promote surviving named files or manufacture evidence from absent scratch. After successful settlement, delay physical cleanup and inject stale callbacks: saved observations remain usable, custody stays occupied, and no later delivery changes meaning. Inject canonical read/save failure separately in core and Runtime: fence Host dispatch, retain safe local cleanup, and recover committed prefixes only after explicit repaired restart. Independent transactions must not allow the healthy owner to continue dispatching through a canonical fault. Keep returned-fault tests, fresh-process crash tests and unexecuted platform assumptions distinct.

Preserve allowance and frozen inputs across failed A, admitted B and restart, including diagnostic deletion and arithmetic exhaustion. At most one terminal delivery escapes the effect owner, none after detachment; stale/duplicate callbacks cannot touch reused custody or replace meaning. Logical completion does not release physical resources still draining. No transaction spans external execution/delivery; denial, validation failure and interruption resolve without fabricated Attempts/evidence. Distinguish the scopes explicitly: denied Actions have a Resolution without an Attempt; a retryable model failure leaves the same Operation without a Resolution until final settlement. Saved authorization without an Attempt remains eligible; admitted uncertain Bash/Edit becomes indeterminate with no replay or mandatory inspection, even for unchanged, expected-result, divergent, missing or unreadable targets. Subsequent investigation is model-selected ordinary work.

Trace one accepted model response proposing Bash and Edit through exact authorization, independent completion, ordered Tool Results and the next provider request. Then crash after Edit copyback but before its result commit: an independent target observation confirms the change, while recovery records indeterminate without launching Edit again. Contrast complete model output whose settlement loses to stop: no output, continuation or child Action is accepted. Try wrong Operation/Attempt evidence and delayed old callbacks; local ownership rejects them without interpreting provider IDs as authority. Fail Bash capture after successful process exit and Edit flush after writes; neither exit status nor completed scratch can fabricate success. Verify each effect's typed evidence retains its actual cause and mutation uncertainty through core publication.

Verify one initial model Attempt plus at most **three retries**, default **2/4/8-second** waits, longer valid Retry-After taking precedence and shorter values not reducing waits. Temporary connection/rate-limit/server/inactivity failures share this per-Operation allowance; permanent authentication/invalid requests, storage and deterministic continuation failures do not blindly retry. New Operations have independent allowance. Uncertain transport retains possible duplicate billing.

Provider inactivity defaults to **five minutes** before first body data and between chunks; body/heartbeat data resets it, Host preparation does not consume it, and a progressing stream may run longer. Bash defaults to **five minutes** elapsed execution: permission/capacity waits do not consume it and output does not reset it. Explicit positive finite representable overrides, including longer durations, replace the default without arbitrary ceilings; overflow rejects rather than clamps. Timeout preserves evidence/custody and does not prove remote cancellation. Test configured retry/wait/inactivity/Bash/excerpt defaults and restart without changing admitted facts.

Admit a model Operation offering Bash, then remove Bash from Session configuration before accepting its response. Validate its Bash proposal against the original catalog while selecting permission independently at child Action admission; require authorization before execution. A later model Operation offers no Bash and cannot admit a Bash proposal. Exercise both catalog-change/admission orders and restart with the frozen catalog retained; no latest-settings recheck may replace the proposing Operation's contract.

Permission fixtures cover default ask, explicit bypass, exact Local Owner/request/Operation/descriptor binding, repeated equal decisions and conflicting/stale/inapplicable/unauthorized decisions. Ask→bypass leaves existing requests pending; bypass→ask preserves prior authorization. Both configuration/Action admission orders preserve selected mode/provenance; running Actions are unaffected by configuration. No generic response batch or conversational input request is accepted.

Configure a Session through Workflow Runtime with explicit bypass, disconnect the configuring client and admit an Action under bypass. Restart the Host, reuse the Session and admit another new Action; verify that it receives Authorization without a Permission Request from the retained bypass mode, independently of any pre-restart Authorization. Change the mode to ask and admit another Action; verify that it creates a Permission Request. With ask, disconnect with a pending exact request, restart and reconnect through another authorized client; verify the same request remains answerable without a resident waiter or payload copy. Stop or prior settlement still makes a stale request inapplicable. Configure the same Session from two Workflows in both commit orders and verify one shared policy at Action admission, with no Workflow-local grant or automatic reset on Workflow completion/disconnect. These traces do not imply replay of an already-attempted uncertain effect.

## Exact Edit

Test proposal-only preview/admission without target reads or execution custody. After authorization and Attempt admission, require an existing eligible file; creation remains Bash. Cover relative/absolute paths, symlinks, hard links, files outside Workspace/Git and ordinary access failures. Approval never relocates or searches for another match.

Use independent exact-byte expectations for nonempty lists of 1-based `[start,end)` ranges: insertion, deletion, adjacent ranges, rejected overlap, insertion inside/at a replacement boundary and repeated insertion positions. Cover empty files `[1,1)`, EOF `N+1`, LF/CRLF, UTF-8/BOM, unterminated final lines, no phantom trailing line and no implicit newline. Mixed growth/shrinkage retains original coordinates; late mismatch rejects every edit before target mutation. Extra matches elsewhere are irrelevant; unrelated execution-state bytes remain unchanged without a concurrent-writer guarantee.

Build complete output in charged unlinked scratch, then copy through the same opened target handle, set final length afterward and flush; hard-link/open-handle observers retain the file object. Reject pathname reopening, rename publication or success from incomplete scratch. Vary file/line/range/expected/replacement sizes across the reusable **16 KiB** copy window without whole-value residency or an inherited search quota/three-file layout.

Inject cancellation, read/mismatch/scratch failures before mutation, partial/short writes, length/flush failures and crashes before/after result commit. Empty-output truncation is a mutation. Not-applied requires established absence of mutation; write failure/cancellation is not rollback. Retain custody through safe cleanup; lost outcome is indeterminate without replay, target inspection, restoration or scratch-based recovery. Edit inherits neither Bash nor provider timeout and adds no timer. Measure large/slow/erroring filesystem work and bounded resources; windows do not prove latency or native-crash isolation.

Delay Edit filesystem I/O across cancellation and result handling; buffers and the same target/output handles remain valid until I/O ends. Inject descriptor-reader failure both before and after target mutation and distinguish not-applied from changed/uncertain output. Scratch completion never authorizes recovery replay. Verify no resource remains after safe cleanup and no credit is returned early.

## Workflow replay and cancellation

Reject invalid JavaScript and provably invalid entry-point/call forms before new Workflow acceptance, with precise diagnostics and no author-code execution, Session calls or partial Workflow. Valid dynamic keys and names without verbs remain admissible; runtime checks still reject actual invalid arguments, oversize generated keys and changed repeated bindings. Test aliases/shadowing and uncertain control flow without speculative rejection. Compile the same captured source that is committed, recover an existing matching Workflow without recompilation, and race equal/conflicting creation during validation. Exercise validation memory/time/scratch failure, disconnect and child cleanup under the shared evaluator lifecycle; no transaction spans compilation, no retained VM/bytecode or extra unbounded validation workers, and protected controls remain serviceable.

Verify one Store-wide Workflow ID is preserved across creation recovery, reevaluation, operation retries and restart, and never reassigned to another committed Workflow. Source and initial inputs remain immutable; changing source under the same caller Workflow creation idempotency key conflicts, while a fresh key creates an independent Workflow even with identical source. No separate program registration/version entity is required; deliberate Session reuse remains possible.

Accept a Workflow creation idempotency key of exactly 128 UTF-8 bytes and reject 129 before Workflow acceptance; cover multibyte values, exact preservation and transport escaping separately from the value bound.

Lose Workflow-creation replies before/after commit and restart the Host: equal Workflow creation idempotency key/inputs create or attach exactly one Workflow, changed inputs conflict, and attachment after terminal completion does not evaluate again. Detach every client between configuration acknowledgments, message admission and result recording; saved work continues and reconnecting inspection recovers its facts.

Check public declaration types against runtime settings validation, including closed tool/permission values, string model identifiers and structured schemas. Plain JavaScript callers receive the same validation without constructors, runtime enums or TypeScript transpilation.

Pin the JavaScript signatures `session(name)`, `configure(sessionReference, submissionName, settings)` and `sendMessage(sessionReference, submissionName, text)`. Verify synchronous string construction without durable state, configuration fulfillment with `undefined` only after commit, message waiting while queued, fulfillment with its processing Turn's original text/structured value and rejection on its processing failure or stop/cancellation exclusion. Pass an inspected key directly into a different Workflow without lookup, creation or renamespacing. Preserve sparse configuration, no per-message setting overrides and no duplicate effects on replay.

Verify submission names accept exactly 128 UTF-8 bytes and reject 129 without truncation/normalization, including multibyte text. A full 128-byte Session reference and a full 128-byte submission name must coexist in one valid coordinator submission; the saved 36-byte UUIDv4 is independent of both values and contains neither component.

Verify equal Submission names on different Sessions within one Workflow create distinct submissions; the same Workflow/full Session reference/Submission name with matching inputs recovers its original answer, and changed kind or inputs conflicts. Two aliases of the same full Session reference share the scope. A new Workflow reusing that Session and Submission name creates new work, while replay/restart preserves the original binding. Configuration and message calls share the scope; statically repeated names on distinct Sessions are not errors.

Exercise public Workflow Runtime with private fixed-input evaluation: no live core connection, caller-managed evaluator or native Promise/dependency graph. Test no/partial/all results, caught failures, empty fan-out and nested staged joins. `Promise.all`/`allSettled` work; `race`/`any` are absent. Stable scanner/finding keys preserve identity when B-only then A+B results change branch order; completion-sensitive reuse with changed inputs fails complete prevalidation before new intents. Capture call inputs before later JavaScript mutation. This does not detect accidental new keys or prove arbitrary mutable JavaScript deterministic.

Queue B behind A in a shared Session through public Workflow Runtime. Let A fail before B is selected: do not save A's failure as B's result or reevaluate B's caller as a failed dependency. Complete B's successor and record that outcome; later evaluations and lost-reply recovery preserve it even after newer Session work. Repeat with B selected before failure, selected preparation rejection and cancellation before selection. The appropriate recorded failure/cancellation is then delivered once. A caught or uncaught failure of another call does not discard, rebind or implicitly cancel B.

Compose two configured review Sessions and a summary Session through the public Runtime. Make the second review finish first, advance either review Session with another caller, and crash between summary intent/core admission/answer recording. The summary submission binds the original answers in author-selected order; replay recovers that submission without admitting another message. This is not an exactly-once model/effect guarantee. Repeat with one rejected review, caught and uncaught: no summary on an uncaught join rejection, no implicit stop of the other admitted review, and explicit Workflow cancellation retains its separate propagation rules.

Crash before/after saved submission intent, core commit and workflow answer recording. Recover unanswered calls through ordinary core requests; no integrated transaction, privileged table reads or core Workflow fence. A failed Runtime answer-recording transaction cannot roll back committed core admission; after repair/restart, recover its original answer without a second mutation. Snapshot only workflow-saved original results and retain captured content through materialization; arrivals after the snapshot read view is fixed stay absent until later evaluation and do not invalidate current publication; cancellation/stale-generation checks still apply. Test missing/null/failure distinctions, invalid ranges/short reads/lost content, on-demand selective/sequential/parallel lookup, independent object values across equal calls and identity on repeated awaits of one Promise. User-held values and native indexes remain charged.

Vary visible result/call count independently of bytes and verify prepared-input descriptor/file counts remain bounded rather than one per result. Fail preparation after each acquired file/descriptor and during growth; no child starts from incomplete input. During evaluation/cancellation, delay input reads and output writes, and test output-close before exit, stale generations and failure after saved call prefixes. All parent/child/metadata resources retain owners until final consumers and reaping finish. Crash with abandoned artifacts: fresh evaluation uses current saved facts, never leftover output, and cannot recover unsaved calls from scratch.

Retain committed call prefixes after late failure/crash; atomically publish complete pending dependencies or terminal outcome. A fulfilled root still submits encountered unawaited calls before success, but does not wait for them; later completion cannot reopen the Workflow and unawaited continuations do not survive exit. Root rejection, prevalidation failure, cancellation and stale generations cannot publish false success.

The initial polling loop selects oldest eligible Workflows with stable ties; partial dependency progress may reevaluate a join. Recheck immediately after complete lifecycle cleanup and ordinary service; arm one **one-second** asynchronous timer only after an empty query. Test new, newly eligible and interrupted Workflows at startup; interrupted evaluation uses fresh current saved results, preserves calls and fences old publication. Results arriving before dependency publication remain discoverable; considered results alone do not loop. No waiting heap/callback/per-Workflow timer, missed-tick backlog, resident ready set or completion-hook dependency is required. Measure actual SQLite selection work across empty, direct-only, waiting, shared-result, historical-growth and sustained-pipeline populations; overload promises no starvation bound.

Cancellation fences new calls, recovers every unanswered saved submission, then stops distinct Sessions from accepted messages. Previously undelivered calls may now configure/start work before stop; configuration is not rolled back and configuration/declaration/read-only use adds no stop target. Test rejection, unavailable core/storage and crashes before/between/after stops. Before each stop, commit one cancellation-owned intent/key per distinct Session. Crash before/after intent commit, core stop commit and Runtime answer recording; repeated traversal converges on that same key, including successful/idle stops, without affecting work newer than the original stop cutoff. Completed cancellation never propagates again. No unanswered call or required stop can be forgotten. Explicitly cover failed A with unselected B: if cancellation's stop commits before successor selection, the saved stop cutoff excludes B and its call receives cancellation without a synthetic Turn. If successor selection commits first, the ordinary stop settles that active work. Completed cancellation never revives excluded B or propagates to later C; an unfinished pass after a crash recovers committed stop selections. A stop that never committed may select later work on its first admission; retain the independent-transaction effect/cost window. Inject intent allocation/commit failure: cancellation remains unfinished and no unsaved stop is sent. Scale distinct targets beyond resident batch capacity and verify storage-backed bounded traversal and retained recovery history.

Verify saved-token delivery through ordinary core APIs and separate Runtime/core transactions. Crash before/after intent commit, core commit and Runtime answer recording, and lose replies without crashes. Only committed intents dispatch; every recovery uses their immutable token. Race equal author identities with equal/conflicting inputs; losers never send discarded candidates. Inject token collisions separately, bounded regeneration exhaustion and entropy failure; existing intents still recover without randomness, and core conflicts never regenerate committed tokens.

Restart direct clients and Workflow launchers after lost replies using durably captured destination, key and inputs. Change/delete original input files and verify recovery uses retained immutable content. An absent observation permits resubmitting the same key, not allocating another. Concurrent creation of a caller request record converges or fails explicitly without overwriting it. Include Session stops, Permission Decisions and Model Interruptions in same-key recovery after caller restart. Pin canonical comparison, including omission versus clearing, across supported restarts; incidental transport identifiers do not affect identity.

## Controls, inspection and local server

For every state-changing core command (configuration, message, Session stop, Permission Decision and Model Interruption), lose replies before/after commit and retry after intervening work and restart. Verify immutable inputs, admission selection and original acceptance/rejection, no second mutation, authorization on recovery, and conflicts for changed kind/target/payload under the same Store-wide key. Race equal and conflicting commands, including across command kinds. Retry accepted permission/interruption after target settlement and recover the original answer before applicability checks; a fresh command still obeys settled-target rules. An idle stop followed by new input must remain idle on retry. Reads need no saved command record; unavailable capacity and failed commits promise no saved answer. External uncertain effects remain non-replayable. Deliberately reuse an old stop key with identical inputs after newer work starts: core returns the original stop, demonstrating that new-intent uniqueness is a caller obligation, not detectable from equal inputs. Inject a mismatched core-key binding for a saved cancellation stop: expose the conflict without repeated dispatch, key replacement or successful cancellation; retain the intent and fence across restart.

Prepare and inspect the production SQLite schema from an empty Store. Accepted stops retain only command key, Session, optional selected Turn and admission cutoff. Exact-interruption rows retain the supplied Session/Turn/Operation even when rejected. An interrupted model Operation has exactly one causal command-key field; absence and non-interrupted Resolutions have none. Assert that no stop Operation, stop ID, completion bit, duplicate Turn-outcome key or per-message exclusion flag was introduced. Derive observation and queue eligibility from the canonical relations after reopen.

Race exact Model Interruption/Session stop against admission, pre/post-launch, retry waiting, live streams and sealed output. Use observable owner-boundary milestones for prepared-before-handoff, sealed-before-settlement and physical cleanup; do not infer those phases from elapsed sleeps. Suppress the first accepted control's disposable local hint in one pre-handoff stop and one sealed exact interruption, then replay while cleanup remains occupied so the canonical handoff/settlement recheck decides the loser and a duplicate stale hint causes no second cancellation, cleanup or fence. First committed authority wins; exact-target controls preserve their supplied target and obey their repeat/conflict rules, while matching Session-stop retries recover the original selection and only a fresh key selects current work again. Invalid Action/resolved/inaccessible targets reject where required by the control, without a whole-Workflow revision precondition. Stop acknowledgement differs from completion; model custody may drain afterward, tools retain ordered results/cleanup, stops without an active Turn complete after their queued-prefix exclusion commits and a slow Session cannot block later stops. Shared Workflows/Turns do not gain exclusive ownership or automatic sibling cancellation.

Race a Session stop against unselected admission and successor selection, including the idle gap after failure and a blocked preparation. The committed stop excludes only its selected admission prefix; later admission is not retroactively cancelled. Recheck selection eligibility at commit, reject late processing publication over an earlier stop, and derive cancellation from the stop fact without per-message lifecycle flags or a fabricated processing Turn. No queued work selected by that stop restarts the Session after stop completion.

Inspect all associated durable Sessions from accepted configuration/messages, including configured-only Sessions; declarations/rejections alone add none. Show exact full references and existing identifying context. Inspect W1 then type its chosen key directly into W2: share current state/history using W2 request identities, without output metadata, previous-Workflow plumbing or snapshot cloning; W1's bound results remain unchanged. Recover lost acceptance before relying on association.

Ordinary Session inspection reports unselected pending-message count during work and after failure, alongside work state/latest outcome. Automatic selection removes input from that count without a new caller message. Stop/cancellation-excluded input and selected preparation failures are readable as not applied, with full content and cause. A previous Turn's failed outcome cannot mask pending work or settle its calls. Read message identity through admission, selection and final outcome; established results never change when the Session advances.

Default Session waits return on selected-work completion or actionable permission blocking progress, with the actionable requests; explicit terminal-only waits remain pending through permission waits. Exercise already-satisfied conditions, empty idle Sessions, the gap before queued input starts its successor, permission alongside progressing sibling work, another caller answering permission during delivery, later Session reuse and missed/spurious notifications. With no active Turn and existing queued input, wait selects the oldest current admission and follows its processing result/exclusion; it cannot mistake the queue for idle or attach to later input. A wait already selecting A still returns A's outcome even if B starts a successor. Neither wait starts/resumes work nor follows future work; returned permission observations do not replace exact-target validation.

Observe completed, failed and cancelled work through inspection and terminal-state waits: each returns the recorded outcome as data. The corresponding workflow message Promise fulfils with its answer or rejects with its recorded failure/cancellation. Observation access/storage/transport failures remain separate errors and cannot fabricate a work outcome. Lose a submission reply after commit and recover its acceptance/rejection by original core idempotency key; a timeout alone proves neither rejection nor work failure.

Composed inspection may span committed moments without global revision/cross-owner reads. Test stale permission indicators with current exact-target controls, complete collections without logical caps or public paging, bounded windows/escaping, larger-than-window strings, interrupted/truncated capture/delivery and scratch cleanup. Release database resources before slow delivery. Measure workflow/core query, encoding, scratch, queue and total latency under history growth, repeated polls and concurrent controls/settlements; each class must progress despite non-preemptible calls.

For configuration/message submissions, disconnect during partial ingress, after sealing but during admission, and after commit before reply. Assert respectively no mutation, a source retained until commit/rollback, and recoverable original acknowledgment. Hold slow report/export readers while controls and admission run: no DB handle/transaction or execution credit follows delivery, and capture/delivery populations stay within ordinary exchanges. Inject mid-capture and partial-delivery failure; consumers must reject incomplete framing rather than accept a prefix. Close only after pending reads/writes end, and verify restart takes a fresh observation.

Fresh server/client processes cover competing starts, equivalent/unsupported Store aliases, distinct Stores, stale/unexpected/long sockets, wrong version, inaccessible peers and restart races without auto-start, client SQLite or blind unlink. All clients detaching leaves work advancing; waits select work once and history supports recent/kind/after reads plus unapplied inputs. SIGINT/timeout/output failure detaches without cancellation. Verify stdout framing, stderr diagnostics and workflow-failure versus invocation-failure exit semantics.

Qualify **128 connections: 120 ordinary + 8 classification/short-control places**, one request/response each, no idle keepalive/pipelining. The saturated control fixture holds 120 fully received inspection exchanges after complete report capture, streams one genuinely large valid output with bounded fixture memory into the real Store import, and submits eight controls concurrently through the protected places only after observing actual settlement-mutex acquisition. Without delaying inside the Store lock, require monotonic timestamps to prove at least one control entered before settlement completed and acquired the Store mutex afterward; a run without observed overlap fails qualification. Require that output to commit, verify its answer bytes or independent digest remain unchanged and readable after the later controls, and verify those controls observe resolved/current-work semantics. Retain a separate control-first case proving sealed settlement loses the canonical race. Record total durable-acknowledgement p95/maximum, control queue wait, Store-lock wait, Store service, post-commit reply, semantic completion, and physical custody/resource release as separately defined intervals; retain stalled incomplete-upload evidence only as an ingress diagnostic. Exercise **16 KiB** combined request-line/headers, separately derived short-control request/acknowledgment/error bounds, **10-second** header deadline and **60-second** transfer inactivity. Healthy slow transfers/Host backpressure must not look inactive. Test trickled headers, slow acknowledgements, saturation/churn, oversized rejection before mutation and lost configuration/message replies recovered by same-key admission retry. Waits/reports cannot consume protected control places. For Session stop, Workflow cancellation, Model Interruption and Permission Decision, derive each complete request and response bound from its fields, framing and worst-case encoding. Verify maximum valid values fit without truncation or content scratch, oversized requests reject before mutation, and complete semantic errors fit their own bound. Do not inherit a fixed 8 KiB allowance from historical prototypes or policy.

Fill ordinary capacity with waits and slow complete-report delivery, then acknowledge stops/cancellation through control headroom without awaiting their completion there. Also exhaust control places: unavailable admission is not a saved control result. Lose a committed direct stop reply and reuse the Session: retrying the original key recovers the original active/idle selection and cutoff; only a fresh key can select newer work. Verify original acknowledgment recovery separately from pending/completed stop observation across restart. Include keys with the full 128-byte allowance and worst-case escaping in protected request/response bounds, with no content scratch. Completion observation waits for ordinary capacity without undoing the control. Reconnect after partial report delivery and verify a fresh complete report, with no retained cursor or accepted prefix.

Hold live model transports while accepted controls race preparation, handoff and terminal settlement. Drop or duplicate the process-local control hint and verify that canonical rechecks still decide cancellation and late settlement without fencing the Host. The execution owner alone cancels transport and retains custody through cleanup. Repeat connection fill/drain and control churn while recording peak and retained resources; explain any retained growth against the whole-Host target.

Compiled types/golden fixtures pin fields, tags, omissions and `u64`-class string encodings; CLI Markdown adds no facts. Implement and test selected history defaults and content-range edges (empty content, EOF, reversed/out-of-range bounds, multibyte boundaries and overflow) without silent loss or private-content exposure. Exact presentation/default content is not a separate design-readiness gate. Test integer/SQL/wire overflow, exact SHA-256, OS path/socket expansion, NUL/header safety, recursive values as immutable content and untrusted text separated from controls. OAuth tests cover JSON control-byte escaping, malformed compact tokens, long legal account/token values and account continuity without normalization or leakage.

## Execution tracking and waiting

Allocate exactly `active_capacity` neutral records at startup or fail startup. Full-array traversal, reservation rollback, completion and delayed cleanup preserve neutral+occupied=capacity; neutral work creates no durable fact/resource. Measure record size/alignment/allocation independently from libraries and effects. The initial implementation uses the full scan without a free list; custody is never a durable slot registry.

At capacity, eligible work waits in SQLite without Attempt, permit, consumed allowance, scratch or resident waiter. After release, select oldest eligible Operation with stable ties; permission/retry/restart do not reset age. Verify indexed selection under growing history, atomic revalidation and no uncertain-tool redispatch. Controls proceed without execution credit.

Test OS waits with client/provider/subprocess/cleanup events and timers, including due retry without external activity and approval while full starting after release. Startup scans before waiting; pending progress cannot sleep; arrivals across the wait transition cannot disappear. Measure idle/wakeup and completion bursts, service between individual settlements/socket events, stale readiness after removal/reuse and non-preemptible import/callback delay.

If evidence justifies replacing custody scans or Workflow polling, test the replacement against owning facts: discard discovery hints, inject stale/duplicate entries, saturate its bounded storage, race completion/cancellation and delay cleanup through slot reuse. Eligible work must still be rediscovered in the required order; no hint grants a permit, revives terminal work, overwrites a result or frees live custody. Restart must progress from committed facts without reconstructing launch authority. Verify allocation failure/release and fixed memory population as durable waiters grow, and compare whole-Host memory, idle CPU, fairness and control/discovery latency against the initial algorithm. These cases apply when a replacement is introduced; they do not require adding one.

## Resources and accepted Host qualification targets

Record allocation owner, multiplier, maximum, ordinary occupancy and release boundary. Before enabling configuration, derive checked complete workspace/descriptor requirements, including transient spawn and retained-file populations. Vary durable Sessions/Turns, active execution, clients, retained files/history and payload/cardinality independently. Measure allocator-live/reserved bytes, RSS/physical/private-dirty footprint, native/engine/SQLite high-water, threads/stacks, descriptors, kernel/socket resources, cache/writeback and temporary disk separately; explain unavailable/double-counted counters.

Required populations: **0/100/1,000/10,000 Dormant Sessions** at fixed capacity; **1/10/50/100/1,000 Active Capacity** at fixed population. Run model/Bash/Edit-heavy and mixed loads, repeated 0→capacity→0 churn, unopened/Store-open/Host-started cold/retained idle, capacity zero where supported and separate CLI cost. Use representative Conversation/content; record latency, throughput, overhead and raw inputs/calculations.

| Qualification target | Required measurement |
| --- | --- |
| **≤256 MiB** aggregate Latifa-owned process physical footprint | Planned 1,000-operation mixed/specialized loads and cold/retained idle, including evaluator/helpers; stable churn and no dormant/history-driven resident growth. Model-selected workloads remain separately observed. |
| **<1% of one core** idle CPU | Include retry/workflow polling and idle waits. |
| **≤2 cores** average model-fixture CPU | 1,000 streams × 100 small SSE events/second each, short requests and bounded terminal bursts; exclude fixture server, report tool/mixed CPU separately. |
| **p95 durable control acknowledgement ≤1 second** | Saturated load including inspection/import contention; record maximum, queue/service time and physical cleanup separately. No real-time termination/billing promise. |
| Retry discovery **≤2 seconds after due** | Free capacity/light load; one indexed poll/second, no per-Operation timer or catch-up backlog; separate discovery/admission/launch. |

A miss requires explicit attribution/design or target review; do not exclude helpers or add pools/controllers to conceal it.

Measure transport memory, control responsiveness and useful capture throughput together at capacity: slow writes, a stalled write before and after streams progress, bounded capture queues and repeated bursts with retained connections. Include complete callback/transport-call occupancy, pending capture custody, sink failure, advertised receive credit and library buffers beyond the application queue. Distinguish same-connection reuse, accepted TLS session resumption and full reconnects; report serviced idle retention and teardown. Vary staged request size independently from paced output, including growing and compacted contexts, overlapping uploads/responses, SSE framing/batching and terminal bursts. Record complete request integrity, logical staging/read/capture I/O, first-output delay and achieved delivery rate under controlled link conditions; distinguish relay shaping from real network/socket behavior. Local echo responsiveness or isolated transport footprint does not qualify durable acknowledgement or aggregate Host memory.

Verify **8 GiB** default aggregate logical scratch using smaller exhaustion fixtures across requests, captures/metadata, Edit, ingress, reports/exports and spillover. Reserve growth before I/O; cover overflow, sparse gaps, partial writes, overwrites, truncation, ownership transfer and concurrent reservations without double/refunded charges. Include real disk-full, empty-file/handle populations, failed cleanup and protected I/O. FIFO reclaims oldest eligible spillover only after saved result publication; failure/all-protected cases cannot oversubscribe or spin. External readers may retain removed files and mutate published paths outside Host accounting; report that physical usage honestly. No reuse of saved output paths, hidden reader tracking or replay after loss.

Verify exactly one cleanup owner at every resource handoff, including failed handoff. For each temporary owner, count files, descriptors, tracking records and logical bytes separately at preparation, seal, handoff, commit, semantic completion and final cleanup. Cover empty files, pending opens, overlapping copies, failed shrink/close/remove and delayed callbacks; no reported release without established reclamation, no double charge/refund on ownership transfer, and no replacement-allocation or cleanup spin. Retained cleanup failures must consume capacity or prevent safe startup, never disappear from accounting. Fresh startup removes only owned temporary leftovers before admission; it neither imports them nor removes canonical/diagnostic/unrelated files.

Read canonical content through its owning interfaces with bounded buffers, including an Operation historical view and Runtime materialization. Test reference and caller-buffer survival after reader/view close separately from expired borrowed metadata/handles; prohibit persisted scratch ranges, foreign handles and non-owner SQLite access. Keep content available through recovery/materialization; spillover reclamation cannot remove canonical content. Short reads must either complete the requested range or fail explicitly; missing/corrupt content follows canonical shutdown rather than successful EOF. Do not add repeated external validation of trusted local facts.

Exercise the bounded in-memory retention FIFO with many tiny/empty files: derive its capacity from allocated orchestration memory and complete entry/container cost, and include active/protected files separately in the total population bound. Count separate stdout/stderr files individually. Fill the count capacity with spare scratch bytes and verify oldest-eligible eviction; separately exhaust bytes below count capacity. Verify bounded memory and descriptors across repeated fill/evict churn. Test failed entry reservation and ownership handoff, external path loss and failed unlink: failed deletion retains its entry and byte charge, no occupied entry is overwritten, no unbounded failure list appears, and no file loses its cleanup owner or receives a false refund. Exhaust reclaimable capacity and verify explicit failure without oversubscription or spin. Preserve active protection, saved results/excerpts and canonical evidence through early eviction. Report filesystem metadata/cleanup latency separately. Restart may discard the queue and owned output; startup cleanup failure still prevents unsafe admission.

Tool output tests preserve outcome plus default **10,000 UTF-8-byte** combined tail, omission notice and ordinary Bash-readable path. Cover exact boundaries, long lines, multibyte cuts, stdout/stderr sharing, retrieval/removal races and restart loss without changing saved results. No line/total-capture quota or rerun solely for truncation.

SQLite evidence records effective journal/synchronous/page/mmap/temp/busy/hardening settings, heap enforcement, cache/spill pages and high-water on pinned builds. Compare spill-enabled larger-than-cache imports with a spill-disabled diagnostic control. When selecting cache/spill capacities, distinguish useful transactions, database/journal write requests, syncs and process I/O accounting from final file size; preserve transaction and durability boundaries in timing/throughput comparisons. Derive finite heap/windows against the Host target; no artificial database quota/emergency reserve. Prepare every supported statement/schema shape. Distinguish compiled-program preparation limits from execution time and allocation failure. Inject busy, full, corrupt/foreign content, wrong digest, preparation/import/commit/rollback failure: no partial relation/content, unsaved acknowledgement/occupancy release or dispatch. Canonical faults stop dispatch and require safe shutdown/restart; diagnostic failure alone does not.

## Evaluator lifecycle and limit replacement

During evaluator integration, record prepared body bytes versus requested bytes, preparation elapsed time and scratch high-water across sparse access, broad joins and staggered completions. Count actual evaluations; distinguish bounded preparation memory from repeated copying and metadata-capture latency. Evaluate material delay or scratch exhaustion against existing resource/control targets before changing delivery; a high unused-byte ratio alone does not establish the replacement's benefit. These observations belong to integration qualification, not a separate prerequisite benchmark campaign.

Run one complete lifecycle at a time, including validation/publication and child/pipe cleanup. Test **1,000 tiny evaluations**, runaway then ordinary work, controls/I/O during waits and queue delay separately. Enforce **16 MiB engine heap**, **one-second process CPU** including native decoding/compilation/jobs/encoding, and **five-second parent lifetime** from successful spawn through output/exit. Test stopped/non-cooperative children, output-close without exit, truncation and exhaustion without partial success; reap before reuse and classify unexplained signals/OS overshoot honestly.

Verify empty environment, three stdio pipes, only selected read-only completed-input descriptors, closed writable handles, no JS filesystem/process/network/clock/random/credential/storage capabilities and disabled core dumps. Native input reads stay in range. No numeric descriptor scan, process pool, fixed 64 MiB address-space/RSS killer or exit-time wiping. Compare at least **five invocations** at default descriptor limit and `ulimit -n 256`, recording median time/max RSS without descriptor-ceiling slope.

Exceed obsolete source/argument/result/entry/request/field buffers within actual budgets before removing guards. Include finite **10,000-await** and endless-job cases, full-result selective/parallel decoding, outputs above **64 KiB/512 KiB**, native workspace reuse without stale references, exact repeated keys, strict types/prototypes/accessors/cycles, depth/stack/representability and explicit allocation/scratch/time failure. On QuickJS dependency or build-configuration upgrades, recheck the bounded reader extension against string layout, length representation, allocation/refcount ownership, atom errors and sanitizer allocator behavior. Qualify bounded string construction with every UTF-8 scalar width, split sequences, late Latin-1-to-wide transitions, large/non-ASCII object keys, malformed input and failures during both sizing and filling. Verify author-installed prototype getters/setters do not run during native data construction. Measure simultaneous input, final-value, atom-table and old/new reallocation storage; the engine limit setting alone does not establish the backing-allocation peak. Ensure engine allocation-failure tests reach individual allocations rather than only pool refills; report sanitizer diagnostics separately from release timing/footprint measurements. No dropped requests, truncated success, whole-output copies, decoded cache or retained waiting heap. Audit every surviving cap against its owner/derivation; remove obsolete constants, formats, quota tests and compatibility paths together after replacement safety is demonstrated.

## Diagnostics

Verify configurable **128 MiB** retained history, at most **16 files** including active, each ≤floor(cap/16), and **4 KiB** record boundaries/escaping. Test deletion-before-growth, small/reduced caps, omitted optional text, rotation, partial writes/crash tails, failed deletion and no age expiry/memory queue. Detail shares the cap; exports charge scratch, release source handles, report rotated/missing chunks and fail explicitly when full. Delete/fail diagnostics without changing recovery, retry accounting, permissions or required accepted/rejection provenance. Exclude credentials and keep private provider continuation out of generic content reads; preserve bounded opt-in detail and no automatic upload; vary tries/history bytes independently of active work.

Hold export delivery open across diagnostic rotation and inject partial record/chunk writes. Source handles close between bounded export turns; exported scratch stays charged through its last pending delivery read. Count concurrent exports against ordinary client capacity, including failed construction and rotation overlap. Fail export scratch and log deletion independently: neither changes committed execution, and neither reports incomplete output as complete.

Package under the Latifa identity; verify executable/package names, state and credential identifiers, build/distribution references and fixtures without losing existing user data.

Release demonstrations cover explicit start, no-client progress, restart, direct Sessions, first configuration, cross-Workflow inspected-reference reuse, fan-out/joins, permissions, ordered tools, uncertain Bash/Edit and shared cancellation. Independent native/Cloudflare walkthroughs may expose hidden coupling; they add no embedding/cloud release requirement. Keep evidence attached to the real owners and label remaining gaps before claiming release readiness.
