# OnePage architecture

This document is normative. [`CONTEXT.md`](CONTEXT.md) defines the domain language; accepted ADRs explain hard-to-reverse decisions. Historical design, spike, and research documents are evidence, not authority.

## Platform contract

The redesigned V1 runtime supports Linux and macOS as required targets. Every decision that depends on a macOS facility must have an explicit Linux disposition before the redesigned architecture is ready to implement: a common supported mechanism, a platform-specific implementation at its owning interface, a replacement decision, or a concrete product tradeoff explicitly accepted by the user. A required capability left macOS-only remains unresolved.

Evaluate credential storage and headless operation, native dependencies, Store locking and local sockets, process supervision and cleanup, filesystem/Edit/temporary-retention behavior, evaluator resource enforcement, measurements and build/test qualification on both systems. Shared domain semantics must not inherit an OS mechanism's incidental limitations or silently lose an enforcement guarantee. This requirement does not select a generic platform layer, Linux credential backend, CPU architecture, minimum OS version or distribution matrix; the [platform contract decision](https://github.com/DivyanshGolyan/onepage/issues/126) owns those choices.

Existing macOS measurements and platform-specific accepted mechanisms remain evidence for that target. They do not qualify Linux or prevent replacement during the redesign. [Platform verification](VERIFICATION.md#platform-qualification) requires runtime evidence on both systems.

## System shape

```text
CLI / script clients
  │ HTTP over the Store-derived Unix socket
  ▼
one Host Runtime server
  ├── local HTTP adapter ──► typed Run API ──► Storage Owner ──► SQLite
  ├── bounded Session / Turn / Operation advancement
  ├── disposable Workflow Evaluators
  └── I/O Reactor and temporary external-effect custody
```

One native Zig **Host Runtime** owns one **Host Store**, every live execution resource, every disposable evaluator, and one narrow typed **Run API**. The Run API is a module boundary, not another component or lifecycle. The local HTTP adapter exposes typed operations over a Store-derived Unix socket. CLI and script clients submit commands and read committed facts; JSON belongs to the external adapter and Markdown rendering to the CLI. QuickJS evaluates Workflow Definitions but owns no Session, provider, tool, permission, recovery, or durable state.

A **Session** is one reusable linear Conversation in one Workspace and access scope. A **Turn** begins with one initiating User Message and advances that Session until Final Answer or a typed terminal outcome. At most one Turn is nonterminal in a Session. A **Workflow Run** binds each Run-local operation key to its creation, configuration, or message admission and original result. Multiple messages and Runs may share one internal Turn outcome; there is no intermediate Job domain or exclusive Run ownership of Sessions.

## Simplicity rule

Each durable fact has one relational authority. Each resident byte has one current production consumer, one population multiplier, and one release boundary. Each interface owns one decision and hides its mechanics.

Use established mechanisms without transferring OnePage policy to them: SQLite owns transactions and journal recovery, libcurl owns bounded HTTP/TLS transport, and the OS credential store owns subscription credentials. OnePage retains model-visible context selection, operation admission, permissions, effect recovery, and Conversation meaning.

V1 has no generalized scheduler, provider registry, OAuth framework, runtime tool registry, plugin loader, durable JavaScript continuation, separate daemon manager, event bus, or generalized protocol-adapter framework. The accepted local HTTP adapter is part of the single Host server. A new abstraction requires a current second consumer or an invariant that cannot fit an existing deep module.

## Server ownership and local clients

`onepage serve` acquires exclusive OS-held Store ownership before recovery, endpoint reclamation, or dispatch, and holds it until this process can no longer dispatch or write semantic facts. A second server for the same Store is rejected. One bounded canonical Store selector must be shared by locking and socket discovery: equivalent supported path spellings cannot create two owners, unsupported aliases must reject, and Unix socket path limits must fail explicitly. PID metadata, socket existence, client timeout, and an absent final result never prove ownership or effect termination.

Protect the socket and containing directory with filesystem access controls. Validate intended Store identity and wire version before mutation. Every admitted client acts for the same Local Owner; there is no per-client ACL or implicit tool bypass. Clients never open SQLite or auto-start a server. Unavailable, mismatched, or inaccessible owners fail without fallback. Only after acquiring Store ownership may startup reclaim the expected owned stale socket; it must not unlink an unexpected file or another live owner's endpoint.

Server lifetime is independent of clients. It drives bounded quanta while work is eligible. Infrastructure stop fences new dispatch promptly, interrupts supported effects, preserves available terminal evidence, and performs bounded cleanup without waiting deliberately for LLM completion. Stop and crash do not insert Run cancellation, ordinary Session stop, or user-command Interrupted facts merely because execution becomes unavailable. Explicit restart recovers committed unfinished work using effect-specific rules and remaining budgets, with possible replacement model cost. Loss of ownership is not proof an external effect stopped.

Mutation acknowledgement follows semantic commit. The CLI does not automatically repeat mutations: proven pre-submission failure differs from uncertain acknowledgement loss. Direct Session creation and messages are keyless; explicit resubmission can create another Session/message. Run creation and workflow operations preserve their original keys and canonical bindings for caller-controlled replay. Permission and other controls retain their exact domain uniqueness and applicability rules. HTTP adds no generic command journal, WebSocket, subscription, TCP listener, or per-Run pause/resume system.

Streamed ingress is bounded, charged, validated, and sealed before the referencing semantic admission. Incomplete uploads publish no content or semantic reference. Inbound client connections, slow transfers, completed inspection scratch, and external Active Capacity are separate resource populations; selected budgets must preserve execution and control under connection pressure.

Bound the total local-client connection population and admit ordinary requests and long transfers below that bound, preserving headroom for request classification and short controls within the same server. Each connection handles one request and response, then closes; do not queue pipelined requests or retain idle keep-alive connections. Request headers have both a byte bound and a total completion deadline. Upload and response delivery have client-inactivity deadlines; time spent on Host processing or Host-imposed backpressure does not count as client inactivity. V1 introduces no minimum transfer rate or total-duration limit for a transfer making progress. Connection and scratch bounds remain necessary even when a client trickles bytes. Classification and control headroom do not promise immediate access under arbitrary connection floods or OS resource exhaustion.

Protected short controls are Session stop, Run cancellation, exact Model Interruption, and Permission Decision admission. Protection covers bounded request admission and acknowledgement, not completion waits or large inspection/report delivery. Completion waits and report delivery use ordinary client capacity and must not occupy protected headroom while waiting for execution to finish. This admission distinction changes neither command authority nor effect-specific completion semantics; exact acknowledgement encoding remains with the client contract.

A client timeout closes the connection and releases incomplete transfer resources through their existing owners. It cannot publish an incomplete upload or successful truncated report, cancel saved work, undo a committed command, or authorize automatic mutation replay. The startup-configurable default is 128 total connections, at most 120 ordinary, preserving 8 places for classification/short controls. Bound the combined request line and headers to 16 KiB, short-control bodies to 8 KiB, and acknowledgement/error responses to 8 KiB; reject oversized requests before semantic mutation. Normal message/configuration/content bodies retain their streaming and scratch rules. Initial deadlines are 10 seconds total for headers and 60 seconds of client transfer inactivity. These are accepted policy defaults, not measured requirements or passing production evidence. Configuration is loaded at Host startup; no live resizing protocol is required.

## Canonical relational authority

The Host Store is the sole recoverable OnePage-owned semantic and content store. OS-held credentials are non-semantic security material. Its canonical relationships are equivalent to:

```text
sessions
user_messages
conversation_entries
session_context_revisions
session_context_changes
turns
operations
model_request_manifests
model_context_items
model_output_items
permission_requests
permission_decisions
run_cancellation_intents
workflow_runs
run_turn_memberships
evaluation_generations
content
```

These are the accepted relationships, not frozen SQL names. [ADR-0026](docs/adr/0026-let-operations-own-current-execution-and-final-results.md) makes the Operation the owner of current execution facts and its optional immutable Resolution value; Attempts, Completions and Resolutions are not separate durable entities. Exact mapping of keyed Session operations and ordinary stop/Run settlement remains implementation work.

SQLite constraints and transactions establish identity, parentage, uniqueness, occupancy, ordering, and settlement. OnePage does not persist a generic Session Ledger, reducer image, continuation blob, cached lifecycle phase, or shadow frontier beside these rows. A loaded Session, Turn, or Decision Snapshot is a bounded query result and never a second source of truth.

The Host Store atomically enforces:

- one linear Conversation per Session;
- immutable User Message admission order with at most one Conversation Entry projection per message;
- at most one nonterminal Turn per Session;
- one terminal Turn Outcome;
- at most one immutable Resolution value on each Operation, referenced through Operation identity;
- exact current Attempt identity and non-reused ordinal within its Operation;
- atomic admission accounting and replacement of retry eligibility without losing consumed allowance;
- exact causal parentage between model Operations, Tool Calls, child Action Operations, and Tool Results;
- immutable Permission Requests with at most one Permission Decision;
- idempotent Run creation and keyed Session operations by exact canonical binding;
- distinct keyed message admissions may share one Turn and its outcome across Runs;
- at most one Run Cancellation Intent per Run; and
- content publication together with its first durable reference.

Turn Condition, Session dormancy, Run `permission_required`, runnable work, and observer summaries are derived from canonical rows. Persist a derived value only after measurement proves that an index is necessary; it remains rebuildable and non-authoritative.

V1 is a flag day. Unreleased databases and fixtures are recreated; no ledger-to-relational migration, compatibility reader, alias table, or dual-write path is permitted.

## Shared syntax and representation

Field validation follows the exact consumer rather than a common identifier validator or copied buffer size. Keep semantic invariants, fixed representation boundaries and resource budgets distinct. Each retained boundary names its unit, enforcement owner and explicit failure; it never silently truncates semantic input. Existing source buffers remain protected until their replacement storage and traversal are bounded and verified. Removing a product quota does not authorize unsafe buffer growth.

| Field | Retained contract |
| --- | --- |
| Internal identities and ordinals | Check arithmetic and the selected storage representation before mutation. SQLite INTEGER consumers require signed 64-bit representability; this does not limit opaque identities stored in another representation. Preserve each domain's positivity/nonnegativity and non-reuse rules. Final public ID spelling remains interface work; public integer strings retain their existing wire contract. |
| Binding Digests | SHA-256 values contain exactly 32 binary bytes. Any exposed text must decode to that exact representation; no public hex/base64 choice is introduced here. |
| Store, Workspace and Edit paths | Validate the actual OS/API path representation, embedded NUL and the complete derived path, including suffixes and terminators. Store canonicalization preserves single ownership. Edit retains ordinary absolute/relative path resolution and authorized target checks; historical repository-relative grammar and a universal 1,024-byte product cap are not retained. |
| Unix socket path | Derive capacity from the platform socket address field independently of filesystem path handling, reserving any required terminator. An unrepresentable derived endpoint fails explicitly without truncation. |
| Provider, model and tool names | Supported provider/tool selectors use their finite mappings. Preserve exact supported model identifiers. The provider adapter owns demonstrated wire grammar and compatibility; historical name-buffer sizes do not define universal name quotas. |
| Media/schema types | Validate supported semantic types through the existing content/schema consumer and wire types through their protocol parser. Add no generic metadata registry or independent media-type byte quota without a consumer. |
| Diagnostic codes and text | Keep internal typed failure classifications distinct from external diagnostic text. Escape external text through its renderer and enforce diagnostic memory/retention through that owner. An old short provider-code whitelist is not a universal provider grammar. Credentials remain excluded. |
| OAuth values | Preserve exact nonempty account binding and the adapter's token shape and JSON/header safety rules below. No undocumented UUID grammar, independent 128-byte account limit or 16 KiB token limit is retained. Aggregate authentication parsing, credential materialization and transport allocations still require bounded ownership and explicit resource failure. |

The path boundary is platform/API-specific: the supported macOS SDK exposes a 104-byte Unix socket path field, while filesystem APIs have their own path limits. Derive capacities from the installed target definitions and actual API convention, not duplicated numeric constants. SQLite representation follows [SQLite's datatype contract](https://www.sqlite.org/datatype3.html); OAuth does not prescribe token/value lengths in [its successful-response contract](https://www.rfc-editor.org/rfc/rfc6749#section-5.1). These facts justify representation checks, not new workload quotas. The remaining Host and SQLite decisions own aggregate workspace and command-work policy; this section selects no new numeric budget.

## Conversation and Turns

Conversation contains five immutable V1 entry kinds: User text, assistant text, Tool Call, Tool Result, and System Instruction. Each entry records its Turn and exact causal source. Compaction never edits or deletes these entries.

Starting a Turn is one transaction against an already-created Session with a complete baseline. Admission validates current identity, Workspace, occupancy, canonical keyed inputs where applicable, and every locally decidable continuation-compatibility precondition without provider I/O. It creates the Turn, initiating User Message, and Conversation Entry together. Persistent configuration changes are independent admissions, not a patch bundled with the first message. Model settings are selected when constructing each model request, not frozen at Turn admission; ordinary Session messages require no caller historical revision guard.

The same Session message primitive serves direct clients and workflows. When idle it creates the Turn, initiating User Message, and its Conversation Entry atomically; when active and admissible it joins that work. A workflow additionally commits its key/input/admission/result binding atomically; a direct submission has no caller idempotency key. A later User Message inserts one immutable `user_messages` row with exact Session, internal Turn, admission ordinal, content, and Local Owner provenance; it does not create a Conversation Entry or change an admitted Model Request Manifest. Immediately before admitting the next model Operation that requests an assistant response, one transaction projects every applicable unprojected User Message into Conversation in admission order and freezes the resulting manifest. An internal compaction model Operation reads only already-applied Model Context and leaves pending User Messages unprojected. The unique projection relation derives whether a message remains pending; there is no message phase, batch entity, or resident queue. A Permission Decision authorizes or denies one exact proposed Action and is not Conversation content. Successful Turn settlement requires no applicable unprojected User Message, actionable Permission Request, unresolved Operation or admitted effect that can still change its outcome. Terminal failure uses the precise pending-message exception below. Turn settlement and logical Session occupancy release commit atomically.

Accepted Conversation entries remain canonical after a failed or cancelled Turn. A Session never succeeds, fails, or closes.

## Sparse context and exact model requests

“System prompt” is not one mutable value. OnePage separates:

- persistent Session defaults;
- exact Action permissions and limits on their actual execution scope;
- bounded Conversation projection; and
- exact model-Operation input.

A **Session Context Revision** atomically records only changed persistent components from this closed V1 set:

- model binding;
- Instruction Set;
- Tool Catalog;
- context policy;
- reasoning defaults;
- optional output schema;
- Host-enforced Permission Mode; and
- default output limits.

Session creation and its first complete revision commit atomically. No Turn may be admitted without that baseline. Later revisions are sparse. Each new model request selects the current committed Session Context Revision and resolves the model settings at or before that revision. Action admission independently selects the current Permission Mode; it is not tool authority frozen by a model request. Unchanged components keep their immutable content reference and digest.

```text
r1: model=A, instructions=I1, tools=T1
r2: tools=T2
r3: instructions=I2

resolve(r3) = model=A, instructions=I2, tools=T2
```

A new model request is constructed from one committed view of current Session configuration and its applicable canonical inputs. Its Model Request Manifest freezes the selected revision, resolved settings, input references, and request semantics. Later changes affect only requests not yet constructed; replacement Attempts reuse their existing manifest. There is no Turn-wide copy of model settings, per-setting activation queue, or requirement that every intermediate configuration value be used. Configuration alone creates no model work. Validity and continuation compatibility remain separate from this common construction boundary.

Append-only model-visible updates are the accepted direction. Keep the initial instruction prefix stable and preserve previously supplied messages and provider continuation. Later instruction/context changes enter at a fixed position in the model-visible history, using a provider-supported representation; new requests must preserve continuation compatibility as well as their own immutable retry inputs. Session configuration remains current state and changes independently of message submission. This does not make every Host setting a conversational message, require exposure of every unused intermediate value, authorize Workspace rebinding, or adopt a particular provider's tool/effort update protocol. Appended system instructions are a distinct Conversation Entry kind, visible in Session history alongside messages and tool results. The name is System Instruction, not a generic Context Update. First inclusion commits with the next assistant-response request as described below; exact storage/wire encoding remains implementation work. Existing permission admission and supported compaction contracts remain in force.

System Instruction first inclusion belongs to assistant-response request admission. Derive the last applied instruction from canonical Conversation and initial baseline references, even when a Compaction Base covers the entry. Compare effective instruction content, not revision numbers. After all preceding Tool Results and applicable User Message projections are ordered, validate the prospective replay recipe and atomically commit any net instruction change, those new projections, model Operation, and manifest referencing the entry. A rollback commits none of these new facts; prior inputs and independent configuration remain intact. Preserve immutable content/rendering references and causal request provenance without a separate pending row, applied flag, or application receipt. Recover an admitted request through its existing manifest rather than preparing it again. A -> B -> A emits nothing if B was never included; if B was included, restoring A requires another entry. An admitted failed request still establishes historical application, not proof of provider consumption. See the [boundary traces](docs/design/system-instruction-first-inclusion.md).

A selected configuration revision identifies desired current state; it does not authorize re-rendering earlier instruction positions from that state. The manifest freezes the actual initial-prefix and appended-instruction bindings.

Before admission, compaction reads already-applied Model Context and leaves unapplied instruction changes and pending User Messages for the next assistant-response request. Compaction selects and freezes its own controls without applying new conversation instructions. After an admitted overflow, compaction includes the already-recorded instruction. A later request does not duplicate unchanged instructions, including entries covered by the selected base; exact base/suffix compatibility must preserve their effective instruction meaning. Provider-specific re-establishment controls require demonstrated, frozen adapter rules rather than replaying a configuration mutation.

There is no separate Turn Contract or `turn_contracts` relation. Each fact belongs to its existing consumer. Model-visible date, time, timezone, and Workspace information live in the fixed initial instruction binding or an appended System Instruction, with immutable content/rendering inputs referenced by the request manifest. Tool-observed Workspace facts remain required Operation-owned final evidence and Tool Results. The Session owns Workspace identity; Actions retain their exact descriptors and Permission Request/Authorization provenance. Do not recapture a Turn-wide environment snapshot or reread ambient values when retrying admitted work. This mapping adds no automatic clock refresh, Workspace polling, rebinding, or generic runtime-facts bag.

Resource controls retain their actual scopes. Host admission owns physical capacities, memory/workspace/scratch budgets, and current resource availability. Workflow Run bindings and Evaluation Generations own evaluator limits. Each model Operation owns its retry accounting and eligibility. V1 has no Turn-wide model-request count limit or absolute Turn deadline; successful work, permission waiting, capacity waiting and Host downtime do not consume such a budget. Count admitted tries on their Operation without historical Attempt rows or redundant Turn counters. The limit-matrix gate remains in force. See the [field audit and recovery traces](docs/design/turn-contract-removal.md).

Exact admitted Action descriptors and Authorizations remain valid historical targets; a configuration update does not rewrite them. This decision does not enable implicit bypass or revoke running tools.

Output schema is optional persistent Session configuration, selected by the same request-construction rule. Absence means ordinary final text. The adapter translates the selected schema into a supported provider mechanism and owns response parsing and schema validation at the provider boundary, using the producing manifest rather than current Session settings. Adapter validation runs within the existing bounded validation/import path, without another full response copy or per-Session validator worker. The Host still owns candidate admission, settlement, and exact result identity; the workflow consumes the validated value without a second schema interpretation. No prose-to-object conversion, automatic model repair call, per-caller output schema, or separate schema activation queue is added. Unsupported schemas, refusals, incomplete generation, and invalid output must not become fabricated structured success. Provider schema subsets and concrete error mappings belong to implementation. Codex subscription-endpoint support is an accepted planning assumption, not a live-tested compatibility claim.

Each model **Operation** binds one immutable **Model Request Manifest** containing references and digests for:

- the provider protocol operation, requested concrete model identity, and behavior-affecting protocol options;
- rendered Instruction Set;
- Tool Catalog;
- one exact Model Context replay recipe: an optional derived Compaction Base plus one total ordered suffix of canonical host inputs and accepted model Operation Resolutions;
- the replay format;
- exact content/rendering references for any supplied runtime information, without duplicating values already owned by instruction history;
- reasoning and output limits; and
- output contract.

The recipe freezes the selected Compaction Base and each ordered source identity needed to preserve complete provider-native rounds. Replacement Attempts reuse that manifest and reconstruct the same request semantics; they need not preserve JSON key order or other wire spelling with no provider meaning. Authentication secrets, access tokens, endpoints, sockets, HTTP headers that do not affect behavior, and transport buffers remain late-bound. A changed manifest requires a new model Operation.

The provider owns no OnePage semantic authority. Every value required to reconstruct a continuation is retained in the Host Store and frozen by the manifest. Provider-hosted caches and previous-response identifiers are optional accelerators only when their loss still permits the same request to be constructed from that frozen local recipe.

## Model output and multiple Tool Calls

One model Operation may resolve to assistant-only output or an optional bounded assistant-text prefix accompanied by an ordered bounded set of Tool Calls. All model-visible output and child-call descriptors validate before atomic admission; an invalid member rejects the complete candidate. Successful admission appends the optional assistant-text entry followed by Tool Call entries in call-ordinal order in one transaction. Assistant-only output is a Final Answer only when that settlement transaction proves that no earlier applicable unprojected User Message exists. If a User Message committed first, the output remains ordinary assistant text, no Turn Outcome is inserted, and ordinary advancement later projects the message and admits the next model Operation. If settlement commits first, the terminal Turn Outcome prevents that message command from attaching to the completed Turn. SQLite commit order therefore decides the race without a lifecycle branch outside the canonical transaction classifier. V1 has no model-created conversational Input Request; later input is a User Message admitted independently of model output.

An accepted terminal model response is retained losslessly and in provider order as immutable Model Output Items owned by its Operation. Each item stores its semantic fields, provider-only continuation fields, and required response-evidence fields once, with exact producing-request and Attempt provenance. The Operation commits its immutable Resolution value and these items together; the same transaction publishes every applicable provider-output Conversation projection by reference to those semantic fields. The provider adapter derives a later replay-input view by stripping response-only or non-replayable fields; OnePage does not persist a second replay copy or a complete serialized request body.

Unknown open fields inside a known item are preserved. An unknown consequential discriminator—such as a top-level item, content block, Action subtype, compaction variant, or terminal status—resolves as `unsupported_provider_output` with a bounded typed rejection and its producing-request and necessary causal provenance; it publishes no Conversation, continuation, or effect consequence. Permanent full rejected payload retention is not required. Additional raw detail follows the local diagnostic contract below; unknown fields inside accepted replayable output remain canonical. Core neither exposes private reasoning through generic content reads nor fabricates meaning for opaque or encrypted reasoning, signatures, and compaction items. Raw HTTP and SSE framing, token deltas, partial streams, interrupted output, and late output are scratch evidence, not replay authority, and their execution scratch is deleted after successful canonical import. Explicit bounded diagnostic capture may retain raw detail separately, without granting publication or replay authority.

Each Tool Call creates one child Action Operation with `caused_by_operation_id` and a stable call ordinal. No Step or Tool Call Group is durable authority: the child set is derived from that parent relation.

```text
model Operation M1
├── call 0 ──► Bash Operation B1
├── call 1 ──► Edit Operation P1
└── call 2 ──► Bash Operation B2
```

Child Operations execute and settle independently under Active Capacity, including within one Workspace. Each child commits its own Resolution value and required final evidence when it settles; no sibling holds it outside SQLite. After every child resolves, one transaction appends their Tool Result Conversation Entries in original call-ordinal order. Only then may the next model Operation start. Physical completion order never chooses Conversation order. Denial, failure, cancellation, and uncertainty each produce typed model-visible Tool Results.

OnePage provides no Workspace-wide fence, quiescence assumption, global Action serialization, or isolation claim against other agents and processes. Bash, Edit, and provider work may proceed concurrently under Active Capacity. A closed typed Action adapter gives Bash and Edit the same lifecycle while temporary execution custody exists only for an active Attempt. Edit correctness comes from exact preimage, expected postimage, and observed-state reconciliation.

## Required execution and recovery guarantees

[ADR-0025](docs/adr/0025-enforce-execution-contracts-without-historical-replay.md) separates required behavior from the execution representation. Persist facts needed by a recovery consumer or a concrete product promise. Diagnostic usefulness alone does not require permanent failed-try history. Enforce local delivery and resource-lifetime rules in the owning adapter and execution machinery; durable history must not compensate for an ambiguous producer contract.

| Scenario and consumer | Required behavior and recoverable information |
| --- | --- |
| Crash around dispatch; recovery owner | Admission precedes launch. Preserve the exact admitted work, authorization where applicable, execution identity and consumed allowance needed for conservative recovery; restart cannot recreate launch authority for the old try. |
| Model request has no saved result; Host | Retry unresolved work under the existing policy using its frozen manifest and remaining allowance. Restart never resets consumed allowance or future retry eligibility. A replacement may incur duplicate provider work or billing. |
| Bash result is lost; Agent | Never automatically replay uncertain execution. Preserve enough admitted intent and uncertainty to return an indeterminate Tool Result so the Agent can inspect external state. |
| Edit result is lost; reconciliation owner | Preserve exact authorized intent, preimage and expected postimage. Observe the target and distinguish applied, unapplied, divergent and invalid states under the existing Edit contract. |
| Result acceptance; Conversation and workflow consumers | Commit the accepted result and its semantic consequences atomically. Recovery observes committed meaning without accepting another result or executing the effect again. |
| Model continuation; provider adapter | Preserve accepted output, ordered semantic projections and exact required provider continuation with their request/provenance bindings. Diagnostic logs cannot replace this authority. |
| Stop or interruption; recovery owner | Preserve applicable intent, exact target and committed outcome. Transport cleanup does not manufacture a semantic stop, prove remote cancellation, or allow a stopped Operation to retry. Existing shared-Session Run cancellation semantics remain in force. |
| Duplicate public submission; Caller or evaluator | Preserve existing keyed Run/workflow binding and result replay, permission/control repeat rules, and keyless direct Session uncertainty. These are separate from internal evidence delivery. |
| Old execution notification; live execution owner | Terminal delivery is at most once per execution. After delivery or detachment, suppress further result delivery and reject stale identities before they can affect reused custody or newer work. No historical payload comparison or old-Completion replay service is required. |
| Failed-try inspection; operator | Complete permanent failed-try diagnostics are not a V1 promise. Retain any failure facts still needed for recovery, retry accounting, accepted results or other canonical references; other details may be diagnostic telemetry. |

The [accepted comparison](https://github.com/DivyanshGolyan/onepage/issues/105#issuecomment-5556881068) supports the Operation-owned execution and final-result model selected in [ADR-0026](docs/adr/0026-let-operations-own-current-execution-and-final-results.md). The guarantees above apply independently of diagnostic history; accepted output and public replay remain canonical.

## Local diagnostics and application state

Application state retains facts needed for recovery and promised user-visible behavior. Diagnostics retain recent evidence for investigation. Both may persist locally, but execution never consults diagnostic history to choose recovery, authorize an effect, reset allowance or replace an accepted result. Expiry, deletion, a missing crash tail or diagnostic-write failure cannot alter semantic meaning or grant dispatch authority. A canonical storage failure still follows the ordinary storage-failure contract.

Record small structured diagnostics by default, correlate them with the relevant execution identities, preserve recorded history across ordinary restart and bound total storage by replacing older diagnostic records. Include timings, error classifications and available application/provider version information. Detailed provider/tool payload capture is explicit and bounded. It does not change the lifetime of accepted content, private continuation, exact authorization or other canonical evidence. Credentials stay excluded; generic content reads still exclude private provider continuation. Users can export recent diagnostics to inspect and choose whether to share; this requires no central collection service or automatic upload.

Retained local diagnostic history has a configurable **128 MiB default cap per Host**. Replace the oldest diagnostic records as needed to stay within that allowance, preserving recorded history across ordinary restart. Do not add a separate age-based expiry rule or promise a fixed number of days of history; retained time depends on traffic. The cap is a disk-retention policy, not a RAM allocation or reserved disk space. Canonical application state and the temporary scratch allowance remain separate. Detailed payload capture stays explicit and shares this same allowance; enabling it can evict older summaries sooner.

A typed rejection and producing-request/causal provenance preserve the failure meaning of unsupported provider output. Full rejected payloads may be diagnostic detail rather than permanent canonical evidence. The tradeoff is that a parser/provider fault may require reproduction with detailed capture when the original bytes are absent. Never discard bytes still required by a canonical consumer under this rule.

Diagnostic encoding, retention/rotation, detailed capture and export obey the existing bounded-memory contract. Stream variable content through fixed windows; a diagnostic disk quota does not authorize an equally sized resident buffer. Do not accumulate an unbounded event queue, materialize full log history for export or retain diagnostic buffers per dormant Operation. Any fixed diagnostic workspace must have an explicit owner and resource accounting.

Use one Host-owned append writer and at most 16 size-rotated files of newline-delimited structured records. Each file is bounded by floor(configured history cap / 16), or 8 MiB at the default; the active file and encoded framing count. Delete oldest closed files before growth would exceed the cap. Records are at most 4 KiB encoded through a fixed window: preserve mandatory identity/classification, shorten optional text with an omission marker before overflow, and omit a record if a valid bounded encoding cannot be produced. Reject configuration that cannot fit one record per file. No compression or separate diagnostic database is required.

Opt-in detail uses bounded chunks with capture identity, ordering and completeness markers in the same files. Rotation or missing chunks must never present a partial capture as complete. Discard an incomplete final record before appending after restart; a missing crash tail is allowed and per-record fsync is not required. Write or deletion failure stops/drops diagnostic writes with a bounded notice, without a RAM backlog or semantic failure. After a reduced cap at restart, prune before further growth; failed pruning cannot release fictitious bytes.

Export copies available recent complete records in bounded turns into charged scratch, then uses ordinary report delivery. Record the cutoff and any gaps if rotation overtakes copying; this is best-effort history, not an atomic historical snapshot. Close source handles between copy turns so slow delivery cannot pin deleted logs. Retained originals count against diagnostics and temporary copies against scratch. Unavailable scratch fails the export explicitly.

Application-state retention remains post-V1; bounded diagnostic retention is part of V1. The [comparison discussion](https://github.com/DivyanshGolyan/onepage/issues/105) owns this accepted amendment; the [research note](docs/research/local-diagnostics-conventions.md) supplies supporting examples, not normative defaults.

## Host Runtime execution and settlement

An **Operation** is one model request or admitted Action. It owns immutable request/descriptor and causal bindings, current execution/retry facts, conserved policy accounting, and an optional immutable **Operation Resolution** value. Its ID identifies both the work and its eventual result; no separate Resolution ID or Completion-owned result chain exists. Ordered Model Output Items and other required final content belong to that Operation and remain separate immutable content references, never inline resident payloads.

An **Attempt** remains one physical try, identified by its Operation and a fresh, non-reused ordinal. Admission records that current identity on the Operation; historical Attempt and Completion rows are not authority. **Execution Evidence** is a transient terminal delivery from the effect owner, not a durable entity or publication queue. The Host imports only facts required by current retry policy or final meaning; other details follow the diagnostic contract.

Current facts distinguish unambiguously: no Attempt admitted; an admitted Attempt without saved terminal evidence; and a retryably completed model Attempt with policy-relevant failure facts and future eligibility. These are the recovery facts, not an additional cached lifecycle phase beside another representation. A Resolution is absent in each unresolved case. Once a Resolution commits, it and its required evidence/provenance cannot be replaced, retry eligibility is inapplicable, and further admission or acceptance for that Operation rejects. Permission denial, validation, interruption and cancellation can resolve work without inventing an Attempt or effect evidence.

Attempt admission atomically validates applicability, absence of Resolution, selected retry policy and any due time; records the fresh identity; consumes allowance at its actual scope; and replaces prior waiting eligibility with admitted uncertainty. Rollback preserves the prior facts and emits no permit. Retryable terminal settlement atomically replaces admitted uncertainty with the required current retry classification/eligibility and preserved accounting, without a final Resolution. Every due-work selection revalidates in the admission transaction. Restart, a new try, final settlement or compaction cannot reset consumed allowance. Exact policy and numeric values remain with their existing decision owner.

The explicitly started server control context is the sole Storage Owner and the only code permitted to use SQLite. Every mutation goes through one SQLite-specific command module. A command validates bounded syntax, reserves an Active Credit before a dispatching transaction, starts `BEGIN IMMEDIATE`, loads one bounded canonical Decision Snapshot, invokes one pure total classifier, writes one fixed relational mutation, checks exact affected-row counts, derives any consequence, commits, and releases that consequence only after successful `COMMIT`. Inspection and advancement use the same bounded loader and classifier.

One I/O Reactor multiplexes long-lived provider streams and subprocess pipes and observes temporary Action executors, including Edit. Execution machinery owns only volatile OS and library handles plus fixed borrowed windows while work is active; it cannot access SQLite or decide semantic meaning. One bounded content-free Physical Custody table implements Active Capacity: occupancy of one record is one Active Credit, not a second object or pool. There is no per-Turn driver, permanent Edit lane, retained worker, Session graph, candidate buffer, response buffer, parser workspace, or lifecycle object.

After Attempt admission commits, the Storage Owner asks the selected adapter to materialize an outbound request from the frozen manifest and canonical source items into an immediately unlinked scratch file through fixed windows. Neither the generated request body nor a second replay representation becomes authority. Execution consumes that descriptor and streams inbound bytes directly to another immediately unlinked scratch file. No SQLite transaction spans request construction, network or subprocess execution, filesystem mutation, or response streaming, except that the inspection read transaction may span its private report-scratch writes under ADR-0024, and workflow visibility capture may write only its private snapshot-metadata scratch under Workflow Runs before ending the read transaction. Scratch is dynamically charged, non-authoritative, and nonrecoverable; a process crash discards it and leaves the Operation's current admitted Attempt unresolved for effect-specific recovery. Tool output has the additional temporary spillover consumer described below; retaining that output after settlement does not make it durable execution authority.

Only after transport or execution reaches an effect-specific terminal boundary does its effect owner seal the scratch descriptor and hand it to the Storage Owner. The Storage Owner uses one shared serial validation/import workspace to parse complete model output or tool evidence, then normally commits immutable content, the Operation's Resolution value and required final provenance/evidence, Conversation or permission facts, and the next semantic consequence in one transaction. Retryable model evidence instead commits only current retry facts and eligibility on the unresolved Operation. SQLite current eligibility is the retry queue; one periodic bounded query while the Host is running is the only V1 retry-eligibility trigger.

The post-seal model parser represents variable input as byte ranges into the sealed scratch source, with bounded resident token, nesting, and validation state. Use one sequential, immediately unlinked metadata file for the current shared serial validation/import workspace to record item ranges. Traverse it through fixed windows after complete validation rather than reparsing the entire response inside the import transaction. Its growing bytes count against the shared scratch allowance; release it after import or failure with safe I/O cleanup. This is one implementation file role, not a per-item file collection, resident index, separate quota or per-execution parser workspace. Additional metadata required by the complete provider validator remains bounded in memory and charged if scratch-backed; the prototype's 16-byte start/length pair is evidence for that representation, not a universal provider-metadata size. After complete provider validation, the Storage Owner imports the selected content incrementally within the existing atomic settlement transaction, preserving exact bytes wherever the provider continuation contract requires them. Decoding and projection use bounded windows rather than response-sized strings or an in-memory object tree. A late validation error or failed import cannot publish partial success. The ranges and index are private, non-authoritative preparation state: they grant no independent publication, recovery, or replay capability and are released with their scratch sources. Durable Model Output Items and explicit manifest references retain their existing authority. This refines the existing serial validation/import workspace; it does not require sharing that storage with inspection.

Variable caller content enters through a sealed source supplied to the semantic mutation that first references it. The Run API exposes no independent content-publication operation or caller-visible staged Content Reference. The Storage Owner verifies exact length, digest, media or schema type, and stable positional bytes while importing under its owning content and command-work contract; no semantic row may reference incomplete or unvalidated bytes.

The adapter and effect owner provide at most one terminal Execution Evidence delivery for each Attempt; after delivery or detachment, they suppress further delivery. The Host owns retry policy and semantic acceptance. Live identity checks prevent stale events from affecting newer execution, and a result cannot replace already committed meaning. No internal service promises to return or compare historical Completion payloads on resubmission. Cancellation records intent and may signal the live owner, but only the effect-specific terminal owner may propose Execution Evidence. This removes the generic Completion Inbox, consumption watermark, two-transaction admission protocol, online stream detector, and scratch replay path.

The explicit model-interruption operation targets one exact unresolved Model Operation in nonterminal work. It validates the Local Owner, exact target and its canonical parentage, and current applicability in one transaction. The Interrupted Resolution retains the exact command target and causal provenance; domain repeat/conflict rules recover the prior result or reject conflicting/inapplicable input without a generic command ledger or mandatory direct caller key. No whole-Run revision gates the operation, and ordinary Session controls do not require callers to name a Turn. Its semantic mutation is setting the previously absent Resolution value on that Operation. It neither stops the whole Session nor decides whether another Operation follows. Ordinary advancement derives continuation only when independently admitted User Messages are applicable; otherwise ordinary terminality derives the cancelled Turn Outcome. Remaining observable repeat/error details belong to the Session/client decision, not an implementation-ticket copy.

An intentional model interruption may therefore commit an `Interrupted` Resolution value without inventing terminal Execution Evidence. Current admitted uncertainty under an unresolved Operation requires effect-specific recovery, while an Interrupted Resolution records deliberate abandonment and prevents recovery or retry of that work. The Resolution fences semantic acceptance but does not claim that the provider stopped processing or avoided billing. After commit, one atomic launch-boundary transition in the content-free Physical Custody record decides physical truth: if interruption wins before launch, the I/O Reactor suppresses the unconsumed Dispatch Permit; if launch wins, it closes or detaches the transport. This volatile race selects cleanup only and cannot alter the committed Resolution. Physical Custody and its Active Credit remain occupied until suppression or transport detachment completes; late provider output is discarded. V1 issues no separate provider-cancellation request and waits for no provider acknowledgement. Partial, interrupted, or late provider reasoning, signatures, compaction items, and other continuation material never become durable request input. Actions have no independent interruption command; Turn cancellation follows their effect-specific settlement rules because external state may have changed.

The V1 cancellation command targets one exact Workflow Run. It validates the Local Owner's command and request binding, then inserts the Run's unique Cancellation Intent. Exact replay returns the existing fact and a changed binding conflicts; no whole-Run expected revision is required. The intent immediately fences further evaluator generations and new creation, configuration, and message admissions from that Run. Message admission and this fence serialize, making the affected Session set stable. The intent alone does not fence every Operation through old Run–Turn membership.

An ordinary Session stop selects the current work once and fences its further advancement. Stop completion is the selected work's durable terminal outcome and atomic logical Session occupancy release; an idle stop completes immediately. Acknowledgement of the saved request is distinct from completion. Completion follows the selected work rather than future Session activity, and a stop does not rewrite an outcome committed before its admission. Existing tool Resolution and ordered Tool Result obligations must settle first. Model transport suppression/detachment may finish afterward under its existing Physical Custody owner, with Active Credit retained until release; logical completion does not free resources still in use or prove that a remote provider stopped processing or billing.

Bounded Host driving applies ordinary Session stops to current work in each distinct Session found through the Run's committed message admissions. Creation, configuration, or reads alone do not add Sessions. Each stop serializes with advancement and affects the selected current work regardless of which Run submitted it. A Session may be idle; stopping it is then a no-op. Other Runs observe the stopped outcome without themselves being cancelled. This control path remains permitted after the Run evaluator is fenced.

An unfinished cancellation pass may restart from the beginning after a crash. Use bounded disk queries and a disposable traversal position; do not persist per-Session propagation receipts, idle-check results, or a progress cursor. A stop that previously succeeded or found an idle Session may select newer work during the repeated pass. Callers coordinate Session reuse while cancellation is unfinished. Workflow cancellation completion composes ordinary Session-stop completion for every Session in the pass, regardless of which client submitted the work selected by a stop. There is no separate predicate that waits only for work submitted by the cancelling Run. Use the Session-level completion rule above; workflow cancellation adds no different effect-settlement policy. Request the required stops promptly through bounded traversal before awaiting completion, so one slow tool cannot delay stopping later Sessions. Every stop in a full pass must complete under that common contract before the existing terminal Run outcome can record cancellation completion; once that outcome commits, replay/recovery never runs the pass again. A crash after the final stop but before completion commit may repeat the pass. Failure to commit is not successful cancellation completion. Exact ordinary stop storage and Run settlement integration remain implementation work; no separate cancellation subsystem is introduced.

Ordinary Session stop authority governs admission and settlement of the selected work. If model-result settlement commits first, its Resolution remains valid and the stop governs remaining work. If the stop commits first, late provider bytes cannot bypass it; bounded driving records Interrupted Resolutions for unresolved Model Operations with causal stop provenance. A directly requested model interruption instead cites its Local Owner and exact command-target provenance. An unresolved Action with no Attempt resolves without execution. An Action with a committed Attempt must settle from terminal evidence or reconciliation. Active Bash receives best-effort process-group interruption. Edit may stop as not applied before mutation starts; after mutation starts it finishes bounded execution and reconciliation. Every accepted Tool Call still receives one typed Tool Result in call-ordinal Conversation order.

Pending User Messages and Permission Requests in stopped work become inapplicable through that work's ordinary stop/terminal facts. Their immutable admissions remain; no per-item withdrawal state or resident queue is added. The Session runtime retains effect-specific recovery, cleanup, and safe Turn settlement independently of the propagation loop. Run cancellation completion is not proof that a remote provider stopped processing or billing. No permanent Session fence or protection against overlapping callers is promised.

Attempt admission commits before physical dispatch. Only the invocation that observes that commit receives a volatile one-shot Dispatch Permit; reconstruction never recreates it. The commit records the admitted Attempt on the Operation but does not oblige the owner to launch after a later committed interruption. Permit consumption and post-commit suppression compete through the Physical Custody record's one atomic launch boundary. The winner truthfully determines whether the external effect may have started; it never chooses semantic meaning. Failure while preparing post-commit request scratch is evidence for that Attempt, not authority to erase it.

## Model retries and inactivity

The accepted provider-policy portion of [Choose provider retry and external-effect budgets](https://github.com/DivyanshGolyan/onepage/issues/91) permits three replacement Attempts after the initial model Attempt: at most four admitted tries of one model Operation. Default waits before those retries are 2, 4 and 8 seconds. A valid provider Retry-After is a minimum wait; use the later of the provider time and the default retry time. Persist consumed tries and next eligibility so restart does not reset either. Retry uses the same frozen manifest, including after an uncertain disconnect that may cause duplicate provider work or charges. Ordinary new model Operations have their own retry accounting; there is no aggregate Turn request limit. This does not authorize repeated compaction that cannot make a request fit, automatic redispatch after storage failure, or retries of deterministic incompatible continuation.

Retry temporary connection failures, response-body inactivity timeouts, rate limits and temporary provider-server failures within that same allowance. Invalid requests, unsupported provider responses and authentication failures that credential refresh cannot fix do not qualify for blind retry. Provider adapters map concrete protocol errors into these categories; a permanent error must not become retryable merely because it arrived over the network. Provider-confirmed context overflow takes the existing compaction path rather than an equal-manifest retry. Storage failure and deterministic continuation failure retain their explicit no-automatic-retry rules.

Use a five-minute response-body inactivity timeout while awaiting the first body data and between subsequent incoming body chunks. Any incoming response-body data, including provider heartbeat traffic, resets this timer. A progressing stream has no total-duration deadline. On timeout, stop the local wait through the transport owner and use the same remaining retry allowance; this does not prove remote work or billing stopped. Host admission waits and local request preparation are not provider-response inactivity. Exhausting the retries reports failure and follows the existing safe Turn-settlement obligations rather than bypassing outstanding effects or Tool Results.

The retry count, retry waits, provider-inactivity timeout, tool-output excerpt size and default Bash timeout are adjustable through one Host configuration loaded at startup. The accepted defaults are three retries with 2/4/8-second waits, five minutes of provider inactivity, a 10,000-byte tail excerpt and five minutes of Bash execution. Bash retains its per-call timeout override. No live configuration reload is required. Configuration must not reset consumed tries or already-saved retry eligibility, rewrite an admitted Action's timeout, or change saved Tool Results. Exact setting names and encoding remain implementation details.

Five minutes is an accepted policy default, not a measured maximum provider silence. The [initial timing experiment](research/stream-inactivity/OBSERVATIONS.md) observed only three synthetic Codex requests; it does not certify this timeout or Claude behavior. Provider-confirmed context overflow belongs to the compaction machinery and its existing context-change contract; this decision adds no separate one-compaction-attempt limit.

## Effect-specific recovery

Recovery is effect-specific:

| Operation | Uncertain Attempt |
| --- | --- |
| Model | A policy-authorized replacement Attempt may reuse the same Model Request Manifest while recording possible duplicate work or billing. |
| Bash | Never replay automatically. Resolve as indeterminate and show the evidence to the Agent. |
| Edit | Reconcile the durable Edit Intent against preimage, expected postimage, divergence, or invalid target. |

SQLite owns transaction atomicity and recovery. OnePage owns semantic validation, external-effect uncertainty, and causal admission. It does not reimplement the pager or distrust the configured local machine without evidence.

## Tool output and spillover

Stream tool output to temporary files through bounded I/O windows. There is no per-call total output-capture size limit; Host-produced spillover bytes count against the existing shared temporary-storage allowance while OnePage retains their names. External changes and storage held after eviction by other processes have the explicit scope below. The default excerpt is the last 10 KB (10,000 bytes) of rendered tool-output text, measured as UTF-8 before provider JSON framing. This is one aggregate allowance for the output shown by a Tool Result, not a separate allowance for each output stream. Preserve valid character boundaries when selecting the tail. Short output is included in full; longer output includes the bounded tail plus a small omission notice and spillover reference. Command status and this bounded metadata are separate from the output excerpt. Large output alone is not a failed Tool Call and does not instruct the model to rerun a completed command merely to obtain less output.

A Spillover Tool Result saves the command outcome, bounded excerpt, explicit omission notice and an ordinary absolute temporary-file path. The Agent uses the existing Bash tool with commands such as grep, tail or sed to inspect selected parts; no separate spillover-read tool or opaque retrieval protocol is added. Inspection is an ordinary new Bash call under existing permissions, timeout and output handling. This result is canonical Conversation content; the full Spillover Output is disposable and is not automatically imported into SQLite as durable content or treated as diagnostic capture. Loss on crash is accepted. Missing spillover must be reported as unavailable rather than silently retargeting its reference, altering the saved result, or automatically executing the old command. The Agent may issue a fresh Tool Call, including a deliberate rerun, under ordinary authorization and effect rules; such a call may repeat external changes.

This adds a temporary-output consumer after command completion, amending the earlier assumption that every tool capture is deleted once its result is saved. It does not require raw provider responses or other private execution scratch to remain accessible. Spillover follows the shared FIFO temporary-file policy below, without a separate Turn-end deletion rule or expiry timer. The path-access decision is recorded in [Choose provider retry and external-effect budgets](https://github.com/DivyanshGolyan/onepage/issues/91). A spillover path is not a durable Content Reference. Named spillover is the scoped exception to immediately unlinked private scratch: other internal capture/metadata files keep their existing private access and cleanup. The Host must never reuse a published path for different output, including across Host restart; arbitrary external replacement has ordinary filesystem behavior, not a content-authenticity guarantee. If FIFO cleanup wins before a reader opens it, ordinary file-unavailable behavior is expected. Published spillovers follow the scoped retention accounting below: OnePage does not discover or control arbitrary shell-reader lifetimes or external mutation. Private capture and Host-owned I/O retain strict ownership and growth accounting. Preserve bounded metadata/handle use without keeping a completed command's execution slot solely for spillover access.

When shared temporary storage is full, reclaim eligible files through the shared FIFO policy first. If that cannot provide space, or a real storage failure occurs, the accepted effect-owned stop and explicit storage-failure rules still apply. A bounded model excerpt is intentional and disclosed; incomplete full capture caused by failed storage must not masquerade as successful preservation. Required canonical effect evidence, including Edit reconciliation content, retains its existing recovery guarantees independently of disposable spillover.

## Bash execution timeout

A Bash call has an optional `timeout` parameter. When omitted, use five minutes. The selected timeout bounds total command execution from process start, not inactivity: printing output does not reset it. Waiting for permission or execution capacity does not consume the command timeout. Bind the selected value to the admitted Action so execution does not reread an ambient default.

Expiry initiates effect-owned termination and cleanup and produces a typed timeout Tool Result; it does not undo external changes, prove every descendant has stopped, release custody before safe cleanup, or authorize automatic Bash replay. Existing uncertainty/recovery and Tool Result publication rules remain in force. There is no separate policy maximum for a caller-supplied timeout. Accept a positive finite duration that the selected representation and deadline arithmetic can express safely; reject invalid or unrepresentable values rather than clamp them. Exact units and wire encoding remain implementation details. The historical two-minute source ceiling is not retained.

## Tools and permission

Bash and Edit use ordinary operating-system filesystem access rather than a OnePage Workspace sandbox or Git-based access policy. Session Workspace supplies the working-directory context; it is not an allowed-path boundary. This does not remove the existing per-Action permission flow or change the exact work an approved Action may perform.

The provider-neutral Tool Catalog does not grant execution authority. V1 maps only `bash` and `edit` Tool Keys to executable Actions. Each child Action Operation binds a typed descriptor before permission or dispatch.

Permission Mode is persistent Session configuration, defaulting to `ask`. An admitted client acting for the Local Owner may explicitly change it through independent configuration, including during active work. The child Action admission transaction selects one committed current mode and binds its sparse configuration provenance with the exact validated descriptor. In `ask`, it creates one immutable Permission Request; in explicit `bypass`, it creates Authorization for that same descriptor without a request. Sibling Actions admitted in one transaction share its committed configuration view. A model request's earlier settings do not authorize subsequently admitted Actions.

A later mode change neither answers an existing Permission Request nor revokes an existing Authorization, including one whose Attempt has not started; it does not interrupt a running action. One Permission Decision still targets the exact request, Operation, and descriptor under the Local Owner's Authority. Ordinary cancellation and applicability checks remain in force. Recovery of a committed admission reuses its request or Authorization and bound provenance without rereading the current mode. If admission rolls back, no permission fact exists; a later fresh admission uses current configuration. Replay of a configuration operation returns its recorded result without reapplying its old mode. No Turn-wide policy snapshot, retroactive revocation mechanism, activation queue, resident permission worker, or separate permission history is introduced. Exact relational columns remain implementation work.

## Workflow Runs

V1 retains an explicit caller-supplied key for each durable Session creation, configuration change and message submission within a Workflow Run. Pure JavaScript calculations, helper calls and awaits need no separate key or step wrapper. Distinct intentional operations use distinct keys even when their inputs match; replay uses the original key and complete binding. Run Keys separately identify whole-workflow submissions. Caller keys preserve exact text without trimming, case folding or Unicode normalization. Impose no arbitrary character whitelist or independent key-length quota; any narrower restriction must derive from the actual representation or consumer, with exchange and resource limits enforced by their existing owners.

Explicit keys preserve operation identity when cached results change the order in which concurrent branches reach calls. A global invocation counter is not stable under this evaluator's snapshot replay; a code location alone cannot distinguish repeated loop/helper calls, and an input hash cannot distinguish intentional identical operations. V1 does not add compiler-generated identities or historical promise-delivery replay solely to remove caller keys. See the [industry comparison](docs/research/durable-workflow-step-identity.md) and [call-order counterexample](research/workflow-call-order/README.md). These establish the design rationale, not production recovery evidence.

Workflow authors must derive durable operation keys and canonical inputs from stable source identities, arguments and original recorded results, rather than the order in which concurrent branches become available. For example, identify a verifier by scanner identity and finding identity (or its stable position within that scanner's original result), not a shared counter incremented by whichever scanner resumes first. When several branches contribute to one request, explicitly join their required results and construct its inputs in a stable order. Ordinary local JavaScript mutation remains available; authors must not let completion-sensitive shared state change the identity or inputs of the same logical operation across evaluations.

The Host enforces equal-key binding reuse and rejects changed kind or canonical inputs. This detects conflicting reuse, not every violation of the authoring contract: accidentally producing a different new key can appear to be intentional new work. OnePage does not promise arbitrary shared-mutation determinism, statically police workflow JavaScript, or reconstruct historical promise delivery to repair such programs. Independent branch progress and fresh evaluation after interruption remain unchanged.

A Workflow Run durably binds its Caller Run Key, Workflow Definition bytes, arguments, Workspace, semantics identity, evaluator limits, Evaluation Generations, keyed Session operations and original results, and terminal outcome.

The workflow receives creation, configuration, and messaging functions: `createSession`, `configureSession` (illustrative name), and `sendMessage`. Creation atomically records a Session, its complete baseline context, and its Run-local creation binding before returning the plain Session ID. It creates no Turn or model work; the Session may remain empty. The first message admits work against that existing baseline using the same primitive as later messages.

`sendMessage(sessionId, message, { key, ... })` binds a message admission to its original work outcome and returns that outcome through an ordinary Promise. All operation keys share one Run-local namespace and bind kind and canonical inputs. Equal replay recovers committed identity/admission/result; changed binding conflicts. Existing IDs require no wrapper or attachment. Messages from the same or different Runs may share an internal Turn and its outcome, including the output schema frozen by the request producing that outcome. Exact tables and transaction grouping remain to be finalized; no resident JavaScript object is durable authority. Separate invocations of the same keyed operation deserialize its original saved outcome independently. Object-valued results are distinct objects across invocations; local mutation cannot change a later invocation's value. Repeatedly awaiting one Promise retains ordinary JavaScript identity. The bridge retains no decoded-answer cache after handing a value to its Promise; values still held by workflow JavaScript, promises or suspended functions remain charged to the engine heap.

Configuration is an independent sparse persistent Session update. Its effect and keyed result commit atomically. Replay recovers that result without reapplying an old update. Configuration and later messages are separate admissions; trusted clients may interleave updates and messages. No combined mutation, cross-call lock, historical revision guard, or rollback on later message failure is promised. Configuration alone starts no model work and adds no Session to the message-derived cancellation set.

The request-construction boundary and optional Session output schema are accepted. Other supported settings and the exact configuration acknowledgement shape stay open. The Turn Contract grouping is removed; resource-limit decisions remain with their assigned budget issues. Earlier combined creation, patch-with-message admission, temporary override, and Turn-wide model-setting freeze proposals are superseded.

Each evaluator starts from source against one immutable Visibility Snapshot of recorded Session operation results and stable failures. It returns the complete blocked set and exits. Completeness records every unresolved dependency; it does not impose a global join before any branch can progress. A newly available branch dependency can justify another Evaluation Generation while unrelated dependencies remain unresolved. Explicit joins retain their ordinary input requirements. The current generation never receives live result injection, and eligible evaluation or descendant work still waits for its existing capacity owner. No JavaScript heap, Promise graph, continuation, bytecode, or completion callback survives suspension. V1 retains deterministic joins and excludes `Promise.race` and `Promise.any`; this amendment permits independent branch progress, not completion-order-driven result selection. The [branch-progress trace](docs/design/workflow-branch-progress.md) records the accepted behavior and links the decision closeout.

Workflow completion follows the value or Promise returned by the workflow entry function. A fulfilled returned Promise (or immediate returned value) supplies the Workflow Output, subject to strict-data validation and the existing admission/publication fences. An unresolved returned Promise suspends evaluation with its complete encountered unresolved call set; a rejected returned Promise is a workflow failure under the existing failure path. Do not require every encountered call to resolve before accepting a fulfilled root, and do not reject a workflow merely for an unawaited call. JavaScript constructs dependencies as it executes; the Host neither builds a separate DAG backwards from the return value nor reconstructs Promise reachability.

A successful evaluator result must include the keyed calls encountered during evaluation even when the returned Promise has fulfilled. Process those descriptors through the same complete prevalidation and independent admission path before publishing successful Workflow Output. Already admitted Session work keeps its ordinary lifecycle after workflow completion; completion is not an implicit stop or cancellation. A terminal Run is not reevaluated when that work finishes. Unawaited JavaScript continuations do not survive evaluator exit and are not promised later execution; await or join any continuation whose work or value is required. This adds no detached workflow worker or background Promise lifecycle. Complete unresolved-call capture is a conservative eligibility basis only while the returned Promise remains pending, not a requirement to wait after it fulfills.

Workflow reevaluation uses one asynchronous pull loop within the existing Host. When evaluator capacity is free, the Storage Owner selects the oldest eligible Run from saved facts, ordered by creation time with a stable identity tie-breaker. For a suspended Run, a result now available for any dependency unresolved in its latest published evaluation permits reevaluation; a whole-join completion test is not required. Some evaluations may therefore only observe partial join progress and suspend again. New Runs and interrupted admitted generations are separate discovery cases. Revalidate cancellation, terminality and generation identity before admission. A fresh generation binds one immutable Visibility Snapshot. After a Host crash, abandon an interrupted evaluation and admit a fresh generation using currently available original keyed results; do not reconstruct the interrupted snapshot. Retain committed keyed operations and their original results, and discard unpublished calculations and incomplete dependency/output publication. Equal-key replay reuses committed work; changed kind or inputs conflict. A replacement generation fences publication and command admission from the abandoned generation. Recovery eligibility does not require a newly finished dependency: interruption itself requires a fresh evaluation, subject to cancellation and terminality. A committed generation outcome remains authoritative; a committed terminal Workflow Output is not reevaluated. Snapshot capture must remain fixed within one evaluation, but no durable per-call first-visible-generation marker or historical visibility reconstruction is required solely for crash recovery. This changes workflow evaluation recovery only; provider/tool effect-specific recovery is unchanged.

After one evaluation's publication or failure handling and physical cleanup, give other ready Host work its ordinary bounded service turns, then look for another eligible Run without an intentional polling delay. Only a check finding no eligible work arms a one-second asynchronous idle timer. On expiry, perform a current check; do not accumulate missed ticks. Startup enters the same discovery path. This is not a one-evaluation-per-second rate limit or a hard discovery deadline under contention. Model/tool I/O, controls and settlements continue while the evaluator or idle timer is awaited. SQLite still occupies the sole Storage Owner while a statement executes; asynchronous driving does not make that work concurrent or preemptible.

Supply results on demand from fixed disposable input rather than requiring all visible answers to be decoded before source execution. Capture the exact available Run-local keys, outcome tags and original immutable result references in one visibility read transaction; the read view, not an earlier generation-admission timestamp, defines inclusion. That transaction may write its private charged snapshot-metadata scratch and ends before result-body materialization or evaluator execution. Captured original results must remain immutable and retained through their existing Run/result ownership until materialization finishes. Do not add a separate lease owner merely for this input. Outside the visibility transaction, materialize only captured references through bounded canonical-content reads, finishing SQLite access before each scratch write and giving other Host work service between windows. A result arriving after the captured view stays absent from that evaluation. Exact index/file layout is implementation work; keys use lossless exact comparison and lookup order cannot depend on invocation order.

Native result lookup decodes a requested original outcome and releases the bridge's reference after handoff. Missing, successful null and stable failure remain distinct; malformed ranges, unavailable captured content, short reads and decoding failures are evaluation failures, not missing results. Repeated lookup can repeat I/O and decoding. Lookup, key comparison, decoding and encoding remain within native allocation and CPU/lifetime protection. On-demand loading removes mandatory preload and bridge retention; it does not promise that simultaneous user-held values or an individual decoded result fit the engine heap. No custom promise-delivery scheduler or historical delivery order is introduced. Temporary input is non-authoritative and discarded after interruption.

Capture and completely validate evaluator output before admitting its requests. Capture each call's inputs at invocation time, with JavaScript-specific strict-data checks before serialization. Prevalidation checks all encountered existing bindings and consistency among repeated keys; a known mismatch fails before new requests from that output commit. Equal immutable bindings are reused through read-only comparison without reapplying an operation or opening a mutation transaction solely for replay. New calls retain encounter order and their existing independent atomic admission contracts, rechecking key existence, generation, cancellation and current applicability in the transaction. Retain committed prefixes after a later failure or crash; never claim whole-generation rollback or incomplete successful output.

Between admissions, and during long comparison, validation and materialization sequences, return to the existing Host driving path so controls, cleanup and settlements actually receive service. Release active SQLite statements and borrowed shared workspaces before yielding; keep only owned sealed scratch and bounded continuation state. These turns do not start another workflow evaluation before the current lifecycle ends. No grouped commit policy or durable admission cursor is added. Final complete dependency/outcome publication remains one atomic transaction with final generation/cancellation checks.

For a suspended outcome, publication records the generation's complete unresolved dependencies as the next eligibility basis under its atomic generation-publication boundary. A terminal outcome is authoritative and creates no next reevaluation obligation from unawaited calls. A dependency unresolved in that snapshot remains represented even if its result committed meanwhile, so the next pull sees it. A result already considered by that evaluation does not alone keep it eligible. This same current-fact query handles results arriving before dependency publication, during evaluation, and before Host restart. Workflow discovery requires no Turn-completion hook, deferred completion-ID queue, subscription registry or maintained durable ready set. Waiting Runs retain no evaluator, callback or payload-bearing resident queue; one shared idle timer serves discovery.

Select through bounded resident query/workspace state without loading all waiting or eligible Runs. Use oldest-eligible selection without a rotating cursor, sweep state or ready queue, and give controls/settlements service between discovery/evaluation turns. Evaluate this policy against millisecond evaluation lifecycles and Turn completions taking seconds to minutes. This policy does not promise starvation freedom under sustained evaluator overload; aggregate completion demand and discovery cost still require qualification. Query plans, examined work, selection fairness, the concrete generation/publication mapping and qualification against the existing idle-CPU and control-response targets remain required evidence. A small result limit alone does not establish bounded lookup work. The [readiness comparison](docs/design/workflow-readiness-comparison.md) records the chosen pull loop and the limitations of its synthetic measurements.

Workflow Output has no independent total serialized-result byte cap. Serialize through bounded windows into charged scratch and stream parent capture instead of materializing a complete encoded output frame in both child and parent. The JavaScript result and native conversion/validation state remain subject to their actual memory owners, and evaluation/encoding remain subject to finite work/lifetime protection. Shared scratch exhaustion, memory exhaustion or time exhaustion produces explicit failure, never truncation or a partial successful Workflow Output. Complete validation and canonical content/outcome publication retain their existing atomic boundary. Protocol length representability and strict-data invariants still apply; they do not justify copying the historical 64 KiB result or 512 KiB frame capacities into product quotas. Separate the historical descriptor-scratch use of `workflow_output_bytes` and derive that workspace from its real consumer. This accepted contract requires bounded serialization/capture before removing current buffer-safety checks; the initial engine heap limit is specified under Disposable evaluator construction, while time policy and native workspace derivation remain separate. It does not promise that arbitrary JavaScript result graphs fit.

Cancelling a Workflow Run first fences further evaluation and submissions from that Run, then applies ordinary stops to current work in every distinct Session found through its committed message admissions. If a crash interrupts cancellation before its completion is durably recorded, recovery may repeat the entire stop pass, including stops that previously succeeded or found an idle Session. Callers coordinate Session sharing and reuse while cancellation is unfinished; newer work may be stopped by a repeated pass. Once the existing terminal Run outcome records cancellation completion, recovery does not propagate that cancellation again. Use the existing Run Cancellation Intent, Run outcome, and ordinary Session stop/settlement facts; add no per-Session propagation receipts, idle-check records, durable progress cursor, or cancellation-specific queue. Effect-specific cleanup still applies; Sessions remain reusable. See the [accepted recovery trace](docs/design/workflow-cancellation-stop-mapping.md#accepted-simplification).

## Run interface

The Host Runtime exposes one narrow typed Run API and no separately instantiated Run Service. Its explicit semantic operations create or attach a Run, create and independently configure a Session, admit first or later messages to its current work, decide one Permission Request, interrupt one exact unresolved Model Operation when explicitly targeted, stop current Session work, and cancel one Run. Ordinary Session operations keep Turn identity internal. Each verb owns its distinct authority, concurrency, idempotency, and transaction contract; there is no generic native command or result union.

`drive` is a separate Host scheduler operation. One bounded quantum may settle evidence, admit consequences, release committed external work, and compose several individually atomic transactions before stopping at quiescence, an observable block, terminality, interruption, or quantum exhaustion. It is not one atomic mutation and returns only a small drive report. The server repeats it while immediate runnable work remains. A pending external execution that cannot reserve Active Capacity is not immediate runnable work for this loop; it must not cause repeated driving without a resource or eligibility change. Client mutations admit intent; they do not own scheduling, and no public `advance` call is exposed or required.

Between individual ordinary result settlements, give ready stop/cancel requests and their due cancellation cleanup bounded service turns through the existing Host driving path. A prompt durable stop acknowledgement must not leave already-sealed cancellation evidence and releasable custody behind an entire ordinary-result backlog. Preserve each settlement transaction's atomicity and retain custody until effect-specific physical release is safe. These service turns do not preempt an in-progress import or transaction; ordinary settlement and inspection must also make progress under sustained ready controls. Use the existing owners and custody table. The saturated-workload control-acknowledgement target is defined in VERIFICATION.md; it does not make an individual capture, transaction or library call preemptible. SQLite import and non-inspection command-work policy remains with the SQLite resource decision; inspection query shape, read ownership and responsiveness belong to the separate inspection decision below.

The single I/O Reactor services ready cancellation between individual socket events and between completed-transfer notifications, rather than waiting for an entire ready-network batch. Perform transport removal outside library callbacks and preserve handle, completion-message and descriptor lifetime rules when cancellation changes the ready set. One socket or timeout action and its callbacks remain non-preemptible; this service structure alone does not establish a strict latency bound. The supported transport library/build remains a separate qualification decision.

Current inspection captures a complete report of one Run under one read transaction on the Storage Owner's existing connection. Bounded private batches stream facts incrementally into immediately unlinked scratch; the revision and facts come from the same committed view. Other database admissions and settlements wait during capture. The owner finishes active statements and ends the transaction before delivery, so subsequent execution cannot invalidate the report and a slow client retains no database resources. Commands still validate current exact targets. One shared capture workspace and separately charged completed-report scratch have explicit cleanup on completion, failure, and abandonment. There is no public cursor or snapshot service. Capture time and aggregate scratch require resource budgets and measured whole-Host command latency; batching alone does not bound total work. See [ADR-0024](docs/adr/0024-capture-run-inspection-before-delivery.md). The accepted [inspection query and read-ownership decision](https://github.com/DivyanshGolyan/onepage/issues/115) retains this single-owner design for V1. Integrated query, encoding and sustained-load latency checks are implementation qualification requirements, not a prerequisite to selecting ownership. Use Run-scoped membership traversal and indexed current-fact probes that avoid irrelevant resolved history; preserve original admission identities, shared-Turn semantics, terminal precedence and the ordinary progress classifier. Exact schema and indexes remain implementation choices. If integrated evidence cannot meet the existing responsiveness target, revisit the design explicitly before claiming qualification.

Before starting another queued inspection capture, give already-ready controls and ordinary settlement/advancement work bounded turns through the existing Host driving path. Do not drain an inspection backlog ahead of either class, or drain either class indefinitely ahead of inspection. A capture already in progress retains its complete committed view and is not preempted between private batches. This adds no separate scheduler, priority-queue subsystem, second reader, WAL requirement, or public snapshot mechanism. The service quantum, maximum acceptable delay from one capture, and sustained-load fairness remain with the existing work/resource decisions.

Inspection encoding and escaping use fixed-size windows and block writes to scratch, without per-byte locked output calls or whole-field/report materialization. Permitted strings larger than a window are encoded incrementally; a window size is not a field limit. The prototype buffer and batch sizes select no production constants. Any future cooperative capture budget must define explicit incomplete-report failure and retry behavior; checks between batches are not strict deadlines for blocked writes. The accepted design sets no such deadline or abort quota.

Every actionable Permission Request and every other current logical collection member appears in the complete scan. Physical keyset batching is private and no logical collection cap, caller page size, or serialized continuation token exists. Variable fields are immutable Content References read through fixed windows. JSON is the complete versioned external contract and Markdown is its deterministic model-facing rendering, with wire encoding at the HTTP boundary and rendering in the CLI, outside the native Run API. Client interruption only detaches; server termination stops live execution until explicit restart. Truncated delivery is an explicit failure, not a complete report.

Every Run–Turn membership summary appears in exactly one category derived from committed Turn rows: `runnable`, `waiting_for_permission`, `in_flight`, `completed`, `failed`, or `cancelled`. Derivation has an exact precedence: terminal Outcome wins; otherwise an unresolved Operation with current admitted-execution facts or current future retry eligibility is `in_flight`; otherwise an actionable Permission Request with no remaining progress is `waiting_for_permission`; otherwise the nonterminal Turn is `runnable`. A retry-delayed Turn therefore remains `in_flight` even when it owns no Active Credit and no physical effect is live. `permission_required` is reserved for Run state. A keyed message admission binds atomically to its selected work, which may already exist; its result never retargets to later Session activity. Public wire variants are compiled from this contract, not inferred from historical membership names.

Direct Session observation reads fresh committed facts. Waiting selects current work once at observation start and does not follow subsequent work indefinitely. History reads support recent entries, kind filters, and after-position queries; unapplied message content and reasons remain separately reachable without replaying history in every poll. These reads grant no remembered caller view, Session ownership, or historical revision precondition on later messages. Exact remaining public boundary choices stay in the Session/client decision; compiled public types and exhaustive golden fixtures own field names, tags, omissions, and string encoding of `u64`-class values. No handwritten duplicate wire schema is accepted.

## Provider authentication boundary

Codex remains one private authentication/transport adapter, without SQLite access, a Codex CLI dependency, or its own Conversation/retry authority. Resume derives the provider from the persisted model binding; it cannot substitute a fixture or different provider. Credentials remain in the selected platform credential store and late-bound transport, never semantic manifests, logs, child environments, or workflow-visible content. macOS uses Keychain; the Linux mechanism and headless behavior must be resolved under the platform contract without an implicit weaker fallback. Refresh preserves the validated account binding; account data decoded from the configured TLS peer's bearer token is routing metadata, not independently verified identity.

Encode all provider strings before JSON reuse with one bounded encoder covering quotes, backslashes, and every required control escape. Derive buffer capacity from worst-case expansion. Validate token/account values at construction and before HTTP-header reuse, rejecting NUL, CR, LF, other ASCII controls, and non-ASCII bytes. The compact access-token form consumed by account extraction has exactly three nonempty dot-separated segments. Account IDs are nonempty opaque values preserved exactly, without UUID grammar or normalization. Apply the header-safety profile above; do not impose an independent account/token length quota. Validate the consumed claims encoding and shape explicitly. Authentication response parsing, credential materialization and complete outbound encoding remain subject to their actual resource owners, with capacity derived from complete encoded data and explicit exhaustion. This requires no JWT signature verification, JWKS, discovery, generalized OAuth layer, or change to the trusted-peer model.

## Disposable evaluator construction

The Host treats workflow evaluation as one disposable computation: supply the immutable generation inputs, receive and validate a complete outcome, and finish child/pipe cleanup. The evaluator owns its JavaScript engine and native working storage internally; they are not separate workers or services. SQLite retains workflow authority between evaluations, with no resident evaluator state while the workflow waits for external work.

The evaluator receives an empty environment, three explicit stdio pipes and the explicitly selected read-only descriptors for completed disposable workflow input. The Host closes all writable input handles before handoff; descriptor construction and close-on-exec own inheritance. Native bridge code may perform positional reads on those input descriptors. Workflow JavaScript receives no paths, raw descriptors, filesystem, process, imports, FFI, network, clock, randomness, credential or storage API. This permits no child-side pathname open or SQLite access. Core dumps remain disabled. Do not scan every descriptor number up to `RLIMIT_NOFILE` at each invocation, retain a process pool, or overwrite/free complete input/output/bridge mappings immediately before this disposable process exits. Those mappings have process lifetime and no subsequent in-process owner. This is not a sandbox guarantee after arbitrary native-code execution. The evaluator decision still owns independent containment limits; these lifecycle requirements do not select numeric caps.

V1 runs **one evaluation at a time per Host**. The Host services model/tool I/O and controls asynchronously while the evaluator runs. Finish outcome validation/publication or failure handling and safe child/pipe cleanup before starting the next evaluation, so exited children cannot leave a growing backlog of captures or native workspaces. This serializes workflow evaluation, not model/Bash/Edit execution. Waiting work remains derived from canonical SQLite facts without a retained evaluator or per-waiter resident queue; the asynchronous pull loop under Workflow Runs owns discovery. Bursts may wait for earlier evaluations; no sub-second completion guarantee for a burst of 1,000 workflows follows.

Each evaluation has a **1-second process CPU budget** and a **5-second parent-owned elapsed lifetime deadline**, measured from successful child spawn through complete protocol delivery and child exit. Apply CPU protection to native evaluator work as well as JavaScript, including decoding, compilation, job draining and encoding. Waiting before spawn and external model/tool execution consume neither evaluation deadline. Parent preparation and canonical publication retain their own bounded-work contracts. Derive cooperative checkpoints and the coarse kernel CPU backstop from this policy and OS granularity instead of exposing independent timing knobs. On confirmed exhaustion, fail explicitly without truncated or partial successful output; do not infer a specific resource cause from an unexplained signal. Deadline expiry initiates termination, but cleanup remains owned until actual pipe closure and child reap. These are enforceable deadlines subject to OS scheduling, not hard real-time termination guarantees.

The initial engine-managed JavaScript heap limit is **16 MiB per live evaluator**. This is an allocation ceiling, not memory reserved for every workflow or a whole-process physical-footprint bound. Full replay, report serialization and Store publication must qualify under this limit before release; generated assembly probes alone do not establish compatibility. Native memory and concurrent evaluator population remain separately accounted.

Evaluator native allocations outside the QuickJS heap have explicit bounded ownership. Reuse temporary storage when one phase has finished and no retained reference needs its contents; parsing-key validation and later request encoding may share storage when their lifetimes do not overlap. Retained source, request metadata and canonical request copies remain charged to their actual owner. Engine heap limits do not cover native frames, workspace, stack or parent capture. This requires no additional process, pool or custom combined allocator. Derive the native workspace capacity from the selected representation and workload evidence; the historical 2 MiB arena is not an accepted numeric V1 limit. Buffer capacities and allocation layout are internal implementation choices, derived and verified against the supported workloads and aggregate OnePage footprint. They do not require independent caller configuration or a separate product decision for each allocation. A change that restricts supported workflow behavior or misses the whole-Host target requires explicit review; calling a cap internal does not exempt it from that obligation.

Do not impose independent evaluator source, arguments, aggregate visible-result, total-entry, request-count or microtask quotas merely to preserve historical fixed buffers and tables. Replace those representations with bounded allocation, transfer and work checks at their actual owners. Source compilation may still require contiguous storage within its allocation budget; JS values and request/key metadata consume their respective bounded memory. Preserve existing bounds checks until replacement storage and traversal are safe. Replace the microtask quota with CPU/lifetime checks through job draining and native phases. Resource exhaustion remains explicit; no truncation, missing request, retargeted replay or partial successful publication is permitted.

Retain strict-data/type/prototype/accessor/cycle validation, duplicate-key and exact request-binding checks, checked arithmetic and wire representability, engine/native recursion protection and bounded diagnostics. These protect distinct correctness or resource boundaries. Their internal storage/stack capacities are derived and qualified, not copied into independent workflow-size policies.

The supported macOS V1 build has no fixed 64 MiB address-space policy. Retire the historical setter/constant rather than enabling a setter the supported machine rejects, or treating virtual address space as physical memory. Engine/native allocation ownership, single-evaluator population, CPU/lifetime enforcement and whole-Host physical-footprint qualification provide the selected resource model. This does not add an RSS polling killer or platform memory service. Linux must establish the purpose, enforcement and compatibility evidence for its selected OS resource controls under the platform contract; the macOS result does not select or qualify a Linux address-space guard.

## Capacity and memory

Physical Custody is one startup-sized in-memory table of content-free records. Use plain bounded table scans in V1, without a separate free list, active-record index, resident Session collection, or durable SQLite slot table. Reserve a record before Attempt admission; rollback returns the unused reservation, while successful admission permits dispatch only after its commit is observed. SQLite owns Session occupancy, Operation current execution/Resolution facts, cancellation and retry eligibility; it does not duplicate live handle ownership. Logical settlement does not free a record whose physical cleanup is still outstanding. Reuse requires safe resource release and rejection of stale or duplicate events by exact admitted identity. The accounting invariant is free records plus occupied records equals startup Active Capacity; it does not introduce a second credit pool.

Edit preparation before Authorization occupies one record in the same shared Active Capacity table before acquiring preparation resources. It creates neither an Attempt nor a Dispatch Permit and cannot mutate the target. Persist the exact Edit Intent and required immutable inputs before requesting permission, then safely release preparation buffers, scratch and custody; waiting for permission retains none of them. Approved work reacquires shared capacity to reconstruct and revalidate the same authorized intent before application. A changed target does not authorize a changed edit. Preparation and application each consume one record while active, not two simultaneous credits for the same work. The default 1,000 therefore bounds preparation, execution and outstanding cleanup together, with no independent preparation pool. Preparation records require exact volatile ownership identity for stale-event rejection without inventing an admitted Attempt. Failed preparation, cancellation and crash release volatile resources without manufacturing effect uncertainty for a mutation that was never admitted.

Temporary exhaustion of Active Capacity is backpressure on physical execution, not a terminal failure of already-accepted work. Leave that work eligible in its existing canonical SQLite facts until a custody record becomes available. Waiting for a record admits no new Attempt, consumes no Attempt allowance, creates no Dispatch Permit, and materializes no outbound request scratch. It retains no per-waiter thread, socket, payload buffer, or resident queue entry. Do not add a durable capacity-wait flag, queue position, or second scheduling authority. This does not bypass independent command-ingress, storage, permission, or effect-budget checks.

Safe custody release notifies the existing Host driving path to reconsider eligible work through bounded queries and ordinary admission checks. Revalidate cancellation, applicability and due times before admitting an Attempt; cancellation of waiting work needs no execution credit. Restart derives eligibility from the same durable facts, without manufacturing a prior Attempt or restoring a volatile permit. Preserve the existing effect-specific recovery rules for work that did have an admitted Attempt. These rules select neither FIFO ordering nor numeric capacity or latency guarantees.

When no runnable work or required deadline/poll is due, sleep until an existing I/O/control notification or required wake. Do not add periodic scans merely to revisit an empty or full custody table. Work waiting only for capacity is not a reason to spin; controls, cleanup, other runnable work, required deadlines and the existing retry poll still receive their service turns. Run the existing single bounded SQLite retry-eligibility poll every second, using an indexed due-time query and bounded processing turns. Revalidate eligibility at admission. After sleep or delayed service, perform a current poll rather than queueing missed ticks. Poll discovery is separate from capacity admission and effect launch; backoff policy remains with the provider/effect decision. This is not a whole-process prohibition on allocation after startup: library, evaluator, transport, and scratch resources retain their existing bounded ownership and accounting.

One startup-fixed `active_capacity` bounds the Physical Custody table. The planned V1 startup default is 1,000 shared model/Bash/Edit executions. This is a capacity ceiling, not an up-front allocation of per-execution transports or payloads, and it is not a hard maximum. Actual owner resource requirements must pass startup validation and the supported build must qualify at this default before release; prototype feasibility is not production certification. Reserving a free record before admitting an external Attempt occupies one Active Credit; releasing that same record after its physical resource obligations finish returns it. A cancelled model outcome may precede that release. Bounded Decision Snapshots and the shared validation/import workspace are borrowed serially; neither is preallocated per credit.

Durable Dormant Session, terminal Turn, and Blocked Workflow Run populations reserve no resident driver, Slot, Active Credit, thread, socket, subprocess, evaluator, materialized Conversation, or context graph merely by existing. Temporary cleanup after logical model cancellation remains owned and charged by the Host's Physical Custody until release, not by a resident Session object. Long-lived in-flight work may retain only its credit, content-free custody record, transport handles, and dynamically charged unlinked scratch.

Memory claims report whole-process RSS and the slope of each population separately: durable Sessions, terminal Turns, Active Capacity, provider transports, temporary Action executors and transport resources, SQLite, semantic-validation workspace, evaluator, and model-requested subprocesses. Workload memory is observed separately from OnePage-owned orchestration memory.

Release operation resources explicitly at their owning lifecycle boundaries and reuse the existing bounded processing workspaces. Distinguish allocator-live allocations, allocator-reserved space and process physical footprint after cleanup; freed allocations need not immediately reduce the last measure. The current evidence does not require a custom allocator or explicit pressure-relief mechanism in V1. Any later reclamation policy must follow the selected idle-footprint requirement and measurements on the supported dependency/platform build. Retained memory remains part of Host accounting.

Every large value moves through explicit stages—SQLite or wire, unlinked scratch, one shared validation/import workspace, canonical SQLite content, and presentation window—with one owner and release boundary. Variable content goes to disk unless a measured CPU-critical operation requires a fixed borrowed memory window. A byte limit never authorizes a resident allocation of the same size.

The Host's aggregate scratch allowance defaults to **8 GiB**, configurable for the Host. It limits temporary data written and retained by OnePage across existing owners, including request materialization, captured output and parser metadata, ingress, current/completed reports, temporary exports and retained Spillover Output. It is neither preallocated disk space nor a RAM allowance. Canonical SQLite application state and persistent diagnostic retention have separate lifetimes and do not consume this scratch allowance. Temporary copies of those contents do count while held. Release files with no remaining consumer once safe. Files retained for optional later use follow the shared FIFO policy below; they are not canonical history. Physical custody stays occupied until its execution cleanup obligations finish; optional retained files transfer to the shared temporary-file owner without keeping that execution slot occupied. The allowance counts logical file bytes, including sparse gaps, rather than filesystem-allocated blocks. It is not a guarantee of free volume space, an all-process held-byte ceiling, or successful completion of arbitrary work. For published spillover, external mutation, additional links and data held by other processes after removal are outside the enforced allowance.

### Shared temporary-file retention

One shared temporary-file owner and byte allowance cover all temporary content. A file is protected while required to finish current work, including active output capture, result publication, Edit preparation/reconciliation, report delivery or an outstanding Host-owned read. Once required work has released it, a file retained only for possible later use is disposable. Spillover becomes disposable after its small Tool Result and command outcome are saved and execution no longer needs the file. Files with no later consumer are released immediately rather than retained just to fill the allowance.

When a new temporary-storage reservation needs space, discard the oldest eligible retained files first until enough space is available. FIFO applies only to disposable files; it cannot evict protected files or required canonical evidence. Access does not turn this into a least-recently-used policy. For private scratch, actual safe release must succeed before its charge is reusable. For published spillover, successful removal of the retained name and closure of OnePage's own handles releases its retention charge; unknown external readers do not keep that charge or make the file ineligible for FIFO. If safe reclamation cannot provide enough space, use the existing pre-admission wait or post-admission explicit failure behavior. This runs through the shared owner when space is needed, without another storage pool, per-tool policy, background cleanup service or expiry timer.

A retained spillover file may remain across Turns until FIFO reclamation or Host exit. Loss on crash or cleanup is acceptable and leaves the saved result unchanged; later retrieval reports unavailable and never silently retargets the reference or reruns the old command. Host exit releases OnePage's names and handles safely; other programs may still hold data. Restart does not promise reconstruction of full spillover. This does not make raw provider capture or other private execution files a new diagnostic archive. Retained-file metadata, handles and retrieval must still fit the Host resource envelope; their concrete bounded representation is an implementation obligation within the existing Host envelope, not a promise to account for all external file users.

Published spillover is ordinary filesystem output. Its retention charge is the Host-produced logical size recorded at publication, preserved during transfer from capture and returned on successful name removal plus Host closure. External writes, truncation, links, replacement or open handles are outside that enforced allowance and cannot change the saved Tool Result. FIFO may unlink a published file while a shell reader has it open; normal filesystem behavior applies, and physical disk space may remain in use after the retention charge is returned. OnePage does not promise that the shared allowance bounds that external usage. Real disk exhaustion still fails explicitly. Add no sandbox, global file-reader tracking, advisory-lock protocol or periodic external-mutation scan merely to recover the stronger guarantee; the [ownership research](docs/research/spillover-path-ownership.md) records why ordinary paths cannot provide it. This is one shared budget with a scoped ownership boundary, not a second spillover pool.

Build model execution with an outgoing request file and a captured response file, allowing their lifetimes to overlap through settlement. Build Bash capture with separate stdout and stderr files and a materialized input file only when needed. Stream content through bounded windows and release each file after its last use and safe I/O completion. These file roles derive resource populations for accounting and descriptor sizing; they introduce no per-tool file-count quota or admission check. Their bytes and pending growth count against the shared scratch allowance.

Local client uploads and temporary report/export files use the same aggregate scratch accounting. An ordinary exchange has an incoming-content file role and an outgoing-content file role, materialized only when needed; reports and diagnostic exports share the outgoing role. Their lifetimes may overlap, and both copies count while held. Release each after its last use and safe I/O completion on success, failure or disconnect. A transfer of ownership preserves the charge and release obligation; disconnect does not discard content still needed by saved work. These roles derive file/descriptor populations, not an additional client file quota or accounting system.

Each private scratch or active-capture file has one current owner and one tracked logical size. Published spillovers use the retention charge defined above. Before a write or other size-increasing operation, reserve its possible growth against one synchronized Host total; include outstanding reservations in that total so concurrent owners cannot oversubscribe. For a positional write, growth is the positive difference between its proposed ending offset and the current size, with overflow rejected before reservation or I/O. After partial completion or failure, retain only the actual growth and return the unused reservation. Overwrites within the existing size do not add a charge. Successful truncation returns removed logical bytes; failed truncation does not. Ownership transfer preserves the charge, and temporary copies count separately while both exist.

For private scratch and active capture, the owner serializes mutation and release of its file; no writable alias or alternative growth path may bypass this boundary. Release the remaining charge only after final safe closure with no outstanding I/O or retained handles requiring the file. Initial unlinking alone does not release the charge. Use the existing file/owner state and one shared total, without per-write heap allocation, directory scans, SQLite transactions, or a global lock held across disk I/O. Logical accounting does not model filesystem block rounding, compression, metadata, or cache; real storage faults retain the existing explicit failure behavior.

If required temporary storage remains exhausted after safe FIFO reclamation before Attempt admission, leave already-accepted work waiting in its existing SQLite facts without consuming Attempt allowance or retaining per-waiter execution resources. Reconsider availability through the existing Host driving path; do not add a storage-pressure scheduler or busy retry loop. Available space before admission is not a reservation of all future output and cannot guarantee later writes.

If temporary storage remains exhausted after safe FIFO reclamation after Attempt admission, including during outbound request materialization or active output capture, stop the affected execution safely through its existing effect owner and report an explicit storage failure. Do not rewind the real admission, buffer the remaining output in RAM, silently truncate it into success, indefinitely pause the execution while it retains scratch, or automatically redispatch the failed work. Retain custody and any evidence still needed for safe settlement until their ordinary release boundaries. Bash may already have changed external state; storage failure grants no rollback or blind replay. Edit and other effects keep their existing reconciliation and uncertainty rules. Successfully committed results remain authoritative.

A storage fault that prevents SQLite from durably saving semantic state stops new dispatch and initiates bounded, effect-aware Host shutdown. Do not claim a committed failure, cancellation completion or logical occupancy release when its transaction did not commit. After storage is restored, explicit Host restart uses existing recovery; stopping the Host is not itself a failed or cancelled Turn and does not authorize redispatch outside those rules. Diagnostic-write failure alone keeps its separate non-authoritative behavior. Incomplete ingress and inspection still fail explicitly without publishing partial content or a successful truncated report. Beyond the shared FIFO reclamation of disposable temporary files, this policy introduces no automatic Host restart, emergency memory backlog or numeric disk reservation.


V1 adds no artificial canonical-database size/page-count quota or emergency admission reserve. Canonical state remains subject to SQLite's supported representation boundaries and actual storage availability, with the storage-fault shutdown rule above. A page-cache or heap budget does not impose a durable-history quota. The historical implementation's database cap and reserved admission pages are not retained V1 policy; their removal belongs to implementation against this contract.

Host resource configuration is read at startup, without hot quota resizing, free-space-percentage tuning or an emergency scratch pool. Metadata growth after admission follows the same explicit failure/settlement rule; it must not wait indefinitely while retaining a full quota. Small controls need neither external execution credit nor content scratch, though their semantic writes still depend on canonical storage. Startup validation must derive file/descriptor requirements from bounded owner populations and actual platform availability. Every temporary file, including an empty file, belongs to a bounded live owner; a byte cap alone is insufficient. No additional descriptor pool or per-waiter resource population is introduced.

SQLite incremental import must not retain payload-sized dirty-page memory merely because its page cache has a small suggested size. Retain an enforced finite SQLite heap ceiling; derive and verify its value during implementation against the accepted 256 MiB whole-Host physical-footprint target rather than selecting another product quota in advance. Cache sizes and transfer windows are internal implementation choices. The historical 8 MiB heap allowance is a starting candidate, not an accepted requirement. Required workloads must qualify under the chosen settings before release; if necessary work misses the Host target or a setting restricts promised behavior, review that result explicitly rather than silently changing the contract.

Store initialization explicitly configures spill behavior and verifies the effective cache size, spill threshold, and enforcement of the chosen SQLite heap limit on the pinned build. A successful setter alone is not evidence of enforcement. The Storage Owner owns these internal settings, with no caller-facing SQLite tuning surface. The SQLite heap limit is not a whole-Host resident-memory bound. Verification includes large imports and failed-import rollback. The exact heap/cache/window values are implementation and qualification work, not a prerequisite to resolving the [SQLite resource decision](https://github.com/DivyanshGolyan/onepage/issues/95).

Retain the existing single-owner `DELETE` journal and `EXTRA` synchronization as the implementation baseline, with mmap disabled, file-backed temporary work and explicit verified spill. Preserve foreign-key enforcement, defensive configuration, untrusted-schema restrictions and the existing disabling of unused attachment, extension, trigger and worker features. Qualify these settings against the actual schema and statements; a required feature must have an explicit consumer rather than being enabled speculatively. Page size and SQL implementation bounds are internal choices, not new user quotas. A journal/read-ownership change still needs its own reason and durability evidence through the inspection decision.

SQL text, column, compound-select, function-argument, pattern, parameter and compiled-program ceilings follow the Host's owned statement shapes and the pinned engine's supported representation. Retain a tighter setting only when a concrete consumer or resource boundary justifies it, and verify all supported statements under it. Historical numeric reductions are not automatically V1 requirements. A row/blob representation limit must preserve the accepted content contract and must not become a Conversation, message-count, Run or durable-history quota. Remove obsolete reductions only with their replacement storage, binding and traversal checks in place. The historical database cap/reserve, fixed cache-profile allowlist and buffer-derived content ceilings in `src/host_store.zig`, and the fixed heap default in `src/host_runtime.zig`, must be removed or rederived under these rules; this design decision does not itself alter production checks.

`SQLITE_LIMIT_VDBE_OP` constrains compiled statement size, not executed work or elapsed time; exceeding it can report `SQLITE_NOMEM` during preparation. Preserve operation/error context rather than treating that code alone as proof of actual heap exhaustion. Bound command work through the owned query shapes, indexes and bounded service turns between atomic transactions; import windows bound resident materialization, not total transaction time. Preserve atomic publication, existing cancellation traversal/recovery and fair control/settlement service. Inspection access shape and read ownership remain with their separate decision rather than a guessed statement timer.

Keep SQLite busy handling immediate (`busy_timeout=0`) on the sole Storage Owner; expose typed failure rather than hidden blocking retries or automatic command replay. Rejections before semantic admission and failed read-only captures publish no partial success. Where a command requires a durable stable result, an unsaved result cannot be acknowledged as recorded. After a transaction error, resolve rollback and confirm autocommit state before connection reuse; an unusable or uncertain connection cannot continue semantic work. A fault preventing required canonical state from being saved follows the accepted dispatch-stop and effect-aware shutdown rule. No uncommitted failure, outcome, cancellation completion or occupancy release may be reported as durable. Diagnostic failure keeps its separate non-authoritative behavior.

## V1 limit matrix

This consolidates the accepted resource decisions. Detailed semantics remain in the owning sections linked below; this matrix introduces no additional quota or implementation architecture. Values marked derived must be calculated and qualified against the selected implementation rather than copied from historical buffers. Engineering targets are not runtime admission controls. Existing prototype populations, including Active Capacity 100, are evidence points; the planned shared default is 1,000.

| Class and owner | Retained boundary and population | Release or overload behavior | Basis and required evidence |
| --- | --- | --- | --- |
| Semantic — Store and operation owners | One immutable final result per Operation; exact original keyed bindings; checked identity/ordinal arithmetic | Reject conflicting or unrepresentable admission before mutation; never partial semantic success | [Relational authority](#canonical-relational-authority), [execution guarantees](#required-execution-and-recovery-guarantees); atomicity/recovery and exact-key fixtures |
| Representation — actual field consumer | SQLite INTEGER signed-64 range where used; 32-byte SHA-256 digest; OS/API path and socket representation; finite selectors and adapter token/header grammar | Explicit validation failure without truncation; validate derived paths and encoded values, not only raw input | [Shared syntax](#shared-syntax-and-representation), [authentication](#provider-authentication-boundary); boundary and wire fixtures |
| Resource — Host custody | Startup-configured shared capacity C, default 1,000; models + Bash + Edit preparation/execution/cleanup <= C | Waiting facts stay in SQLite; safe physical release returns custody; no per-waiter resources | [Capacity](#capacity-and-memory); mixed occupancy, cancellation, cleanup and OS-startup checks |
| Resource — shared temporary-file owner | Startup default 8 GiB logical Host-owned bytes including pending growth; all temporary owners and overlapping copies | Reclaim eligible retained files oldest-first; preserve protected work; pre-admission wait or explicit later failure; scoped external-spillover exception | [Temporary retention](#shared-temporary-file-retention); reservations, partial I/O, FIFO, churn, physical disk failure and no RAM fallback |
| Resource — local adapter | Startup defaults 128 total connections, at most 120 ordinary; 8 classification/control headroom, independently of C | One exchange per connection; ordinary traffic cannot consume headroom; completion/disconnect releases state | [Local clients](#server-ownership-and-local-clients); saturated controls and slow-peer/churn fixtures |
| Exchange — local adapter | 16 KiB request line/headers; 8 KiB short control body and acknowledgement/error; per admitted exchange | Reject oversized request before mutation; ordinary content streams through scratch | [Local clients](#server-ownership-and-local-clients); compiled maximal-identity/escaping/error fixtures |
| Lifetime — local adapter | Startup defaults 10 s total headers, 60 s transfer inactivity; per connection | Close stalled exchange; exclude Host processing/backpressure; preserve committed work | [Local clients](#server-ownership-and-local-clients); trickle, attribution and long healthy transfers |
| Resource — diagnostics writer | Startup default 128 MiB history, at most 16 segments, each floor(cap/16); encoded record <=4 KiB | Rotate/delete before growth; shorten optional text explicitly or drop diagnostics; no semantic consequence | [Diagnostics](#local-diagnostics-and-application-state); boundary, smaller-cap restart, incomplete-tail and failure fixtures |
| Resource — detail/export owners | Detail shares diagnostic cap; export uses ordinary connection and charged temporary response | Bounded copy turns release source handles; report rotation gaps; scratch failure explicit | [Diagnostics](#local-diagnostics-and-application-state); slow export and overlapping retention/scratch |
| Population — evaluator owner | One full evaluation lifecycle per Host, independent of C | Next evaluation waits for outcome handling and safe child/pipe cleanup | [Evaluator](#disposable-evaluator-construction), [workflow](#workflow-runs); burst queueing and no residual lifecycle backlog |
| Resource — QuickJS | Initial 16 MiB engine allocation ceiling for that one child | Explicit exhaustion; exit and cleanup release child state; native and parent memory remain additional | [Evaluator](#disposable-evaluator-construction); complete workflow/report compatibility and combined footprint |
| Lifetime — evaluator owner | 1 s process CPU; 5 s parent elapsed lifetime from spawn through protocol completion and child exit | Stop failed evaluation and safely clean up; no partial successful outcome; no hard real-time cleanup promise | [Evaluator](#disposable-evaluator-construction); native/JS phase coverage and non-cooperative child tests |
| Derived — evaluator/native protocol | Native workspace, engine/native stack and recursion protection, encoding windows and kernel CPU backstop | Checked allocation/traversal and explicit failure; retain correctness checks through representation replacement | [Evaluator](#disposable-evaluator-construction); actual layout/OS derivation, strict data and adversarial inputs |
| Effect cost — model Operation owner | Startup defaults 3 retries after initial try; waits 2/4/8 s, respecting later valid Retry-After; per frozen request | Persist used allowance/eligibility; exhaustion fails through safe settlement; no automatic retry of permanent/storage/continuation errors | [Model retries](#model-retries-and-inactivity); restart, uncertain disconnect and error-category tests |
| Lifetime — provider transport | Startup default 5 min response-body inactivity per admitted try; incoming body/heartbeat resets it | End local wait and use remaining retry allowance; no total progressing-stream deadline or remote-stop claim | [Model retries](#model-retries-and-inactivity); first-body, heartbeat and stalled-body tests |
| Lifetime — Bash effect owner | Startup default 5 min execution; per-call positive finite representable override without policy maximum | Execution timeout produces Tool Result and effect-aware cleanup; no automatic retry; permission/capacity wait excluded | [Bash timeout](#bash-execution-timeout); admitted timing, output-independent expiry and descendants |
| Presentation — Tool Result owner | Startup default 10,000 UTF-8 bytes of output tail across one result; omission/path metadata separate | Short output complete; larger output spills without capture-size rejection or semantic truncation | [Tool output](#tool-output-and-spillover); exact boundary, Unicode, combined streams and lost-spillover tests |
| Resource/semantic — native Edit | Nonempty decoded search <=16,384 bytes; exactly one match; reusable 16 KiB I/O window, explicit overlap state; E<=C | Reject invalid/oversized search before Authorization/mutation; no whole-file/replacement cap; safe cancellation/reconciliation | [Native Edit](#native-edit-module); exact-match oracle, bounded decoding, service yields and shared capacity |
| Derived — Store/SQLite | Enforced internal heap; cache, windows and SQL/row representation from actual consumers; one Storage Owner | Explicit typed failure; resolve rollback before reuse; canonical-save failure stops dispatch; never invent durable acknowledgement | [Capacity/storage](#capacity-and-memory); supported statements, imports, failed rollback and aggregate footprint |
| Implementation baseline — SQLite owner | DELETE/EXTRA, mmap off, file-backed temporary work and verified spill; busy_timeout=0; required hardening | Immediate failure rather than hidden busy retries; no journal change for inspection | [Capacity/storage](#capacity-and-memory); pinned engine/build and durability/feature tests |
| Derived — Host and effect owners | Complete workspace, descriptor and file-role totals from simultaneously live owners, including clients, one evaluator and retained spillover | Checked startup validation against actual OS/build; safe release; no separate descriptor/file-count credit pool | [Host accounting](docs/design/host-resource-matrix.md); transient overlap, empty-file populations, cleanup and retained memory |
| Work/discovery — Host driving path | One-second retry poll; workflow rechecks immediately after lifecycle/service and waits one second only when none eligible | No per-waiter timer, catch-up backlog or capacity busy loop; revalidate admission | [Capacity](#capacity-and-memory), [workflow](#workflow-runs); query work, idle CPU and discovery latency |
| Compatibility — model-context owner | Configured Compaction Trigger is an estimate, not a message/content ceiling; provider wire compatibility remains adapter-owned | Preserve complete input; compact under accepted policy; fail explicitly if no fitting compatible continuation | [Compaction](#compaction); provider wire/replay and oversized-input tests; no new numeric trigger default selected here |
| Qualification — whole OnePage | <=256 MiB physical footprint at defined 1,000-operation model/Bash/Edit/mixed fixtures, including evaluator/helpers; same cold/retained idle ceiling | Miss requires explicit implementation/target review, not runtime RSS cancellation | [Host qualification](VERIFICATION.md#accepted-host-qualification-targets); disjoint accounting, full integration and churn |
| Qualification — Host CPU/service | Idle <1% one core; model reference <=2 cores average; p95 durable control ack <=1 s; light/free retry discovery <=2 s | Miss requires explicit review; no arbitrary-report deadline, useful-tool CPU cap or physical-stop guarantee | [Host qualification](VERIFICATION.md#accepted-host-qualification-targets); include capture/import contention, maxima and cleanup |

Complete inspection remains unpaginated and captured on the sole Storage Owner, with control and settlement service between captures. No elapsed-time abort or hard maximum capture delay is selected. The one-second p95 target includes capture delay under the defined workload; it is not promised for arbitrarily large reports.

Unsupported durable-backlog, whole-Turn, content-size, evaluator cardinality and universal field quotas are not retained. Their coordinated replacements and source/test obligations are in [Limit replacement verification](VERIFICATION.md#limit-replacement-verification). No obsolete guard may be removed alone while its fixed buffer/table remains. No compatibility layer or second implementation track is required for unreleased V1 state; subsequent implementation slices follow overall V1 readiness.

## Native Edit module

The V1 Edit tool replaces Git-backed unified Patch input with one existing-file literal replacement: a target path, nonempty `oldText`, and `newText`. Require exactly one match, counting overlapping occurrences as ambiguous. Reject absent, ambiguous, empty-search and identical old/new input without changing the target. Empty replacement deletes the uniquely matched span; it does not delete the file. There is no fuzzy matching, newline/Unicode normalization, replace-all, multi-edit batch, creation, deletion, rename or mode change. Preserve bytes outside the span. File eligibility is independent of Git tracking, ignore status or repository membership. Resolve relative paths from the Session Workspace and accept absolute paths, including outside that Workspace, through ordinary filesystem access; Workspace is not a sandbox. Do not impose a OnePage path allowlist or Git-based eligibility check. Existing exact-intent, permission and effect/recovery requirements remain in force; ordinary path resolution must not bypass authorized target-identity/preimage validation.

Implement the tool as a separate native module inside the Host process, behind the existing closed Action adapter. The Host owns admission, authorization, durable content/intent binding, semantic outcomes and recovery coordination. The Edit module owns exact matching, expected-postimage preparation, file mutation mechanics and effect-specific reconciliation observations. It returns typed evidence; it does not open SQLite, read provider credentials, choose retry policy or decide canonical outcomes. Supply only the scoped inputs and existing scratch/buffer/cancellation facilities it needs, not the entire Host object. The Host must not contain matching loops or file-copy details.

This is module separation, not independent process failure or security isolation. Trusted native Edit code can crash the Host; explicit restart uses the existing durable recovery contract. It introduces no per-edit process, dedicated thread stack, persistent worker, tool RPC service or plugin registry. Process placement does not relax control responsiveness: scan, hash and copy work must advance in bounded service turns through existing execution machinery, retaining only owned continuation state across yields. No transaction spans file I/O. A tested whole-file synchronous prototype is not proof of that integration.

Edit has no separate execution or reconciliation timeout. Large-file work uses the bounded service turns and existing cancellation contract, not the Bash execution timer or provider-inactivity timer. Before mutation, cancellation may stop work without applying the edit; after mutation begins, preserve safe completion and reconciliation before settlement. Absence of a timer does not permit unbounded resident buffers or synchronous whole-file work on the control path, and does not promise that arbitrary filesystem I/O finishes within a fixed wall-clock time.

Build Edit preparation/execution with three scratch file roles: one materialized input container if needed, one sealed source snapshot and one prepared output. Keep decoded input fields as ranges in that container rather than separate files. Allocate on demand; all logical bytes and pending growth reservations count against the shared scratch allowance, including overlapping source/output copies. Release each file once no longer needed and outstanding I/O is finished; phase transitions and handoffs reuse or release these roles rather than accumulating files. The resulting three-file count is derived from the implementation shape, not a separately enforced quota: add no file-count counter, configurable limit, admission check or fourth-file rejection. Enforce the aggregate scratch-byte allowance in the shared scratch owner; use the derived file population for resource accounting and descriptor sizing. The live Workspace target is not scratch and its descriptor is counted separately.

Read a stable owned snapshot and count matches with bounded I/O and explicit search-text storage. For a unique match, stream the unchanged prefix, replacement and suffix into a separate charged scratch file, computing the preimage and expected-postimage bindings. Output length is source length minus matched byte length plus replacement length, with checked arithmetic. Source and replacement bytes need not be resident together; preserve the existing disk-first argument/content path. Do not construct whole-file strings, per-line objects, a search prefix table without justification, or a full-file display diff.

Use one reusable **16 KiB I/O window** for matching and output copying where lifetimes permit. Limit decoded `oldText` to 16,384 bytes, measured after argument decoding, not by character count or escaped wire length. Reject over-limit input during validation before Authorization or target mutation, without materializing an unbounded search string. This limit does not bound source-file or replacement size. The straightforward overlapping-window matcher additionally needs search-text bytes and boundary overlap: its named buffers total at most 49,151 bytes. Therefore 16 KiB is not a total per-edit ceiling; complete workspace and concurrent-owner accounting remain Host-matrix obligations. Release search state as soon as preparation no longer needs it. Borrowed buffers count under their existing owner and cannot be shared by simultaneous mutable users. No new buffer pool or global Edit serialization follows.

Retain exact target identity, immutable Edit Intent before authorization, source-content revalidation, expected postimage, permission provenance, and the existing indeterminate/divergence recovery semantics. Matching and preparation do not authorize mutation. A matching postimage after custody loss is an observation, not proof OnePage wrote it. Do not silently substitute an atomic rename if that changes required inode/target semantics. Storage failure, cancellation before mutation and bounded completion/reconciliation after mutation retain the existing effect rules.

The matcher and application no longer need Git, a named private Git tree, upfront helper-region reservation, child file-size limit or inherited scratch-area lock. Use ordinary charged, immediately unlinked scratch and safe closure. Git-specific prototypes and the [superseded scratch design](docs/design/archive/git-patch-scratch-before-native-edit-2026-09-06.md) remain historical evidence. The accepted file-access decision removes the historical Git-based tracked-file check and Workspace-containment restriction; no Git helper remains for Edit eligibility. Production source still needs that replacement. No four-Edit concurrency cap is selected, and the existing 1,000 shared default and 256 MiB qualification target remain unqualified until integrated measurements pass.

## Compaction

Conversation remains complete. User Messages have no token-admission quota and are never silently split or truncated. The configured token value is an approximate **Compaction Trigger**, not a strict content ceiling: OnePage anchors on provider-reported usage where available and estimates only newly appended model-visible content. Every User Message admitted between consecutive assistant-response model Operations remains a distinct durable fact, and all are applied together when the next such Operation freezes its manifest.

When that estimate predicts pressure before pending User Messages are applied, OnePage may first create a compaction model Operation over only the already-applied Model Context; the pending rows remain unprojected until the following assistant-response model Operation selects the new base, projects every applicable message, and freezes its manifest. If the provider instead authoritatively rejects an already-admitted request for context overflow before output or effects are accepted, the rejected Operation resolves with its User Messages already applied, and compaction covers that committed context. Because compaction changes Model Context, continuation afterwards is a new model Operation with a new manifest rather than a replacement Attempt of the old Operation. An accepted compaction Resolution becomes a **Compaction Base** only when a later manifest selects it. Its creating Operation's complete source manifest defines coverage and lineage; that Operation owns the one canonical replacement output. No separate checkpoint row duplicates those facts.

Later model requests first select the newest accepted Compaction Base in the current lineage, then validate that exact base and append one complete total ordered suffix of canonical host inputs and accepted model Resolutions. Core proves structural replay-recipe validity: lineage, suffix completeness, content presence and digest, and supported stored format. The provider adapter separately applies only compatibility restrictions demonstrated by its wire contract. The total classifier requires both and persists no validity flag. A failed or unresolved compaction never displaces the selected base; missing, corrupt, unsupported, or incompatible material in the selected base fails explicitly as `continuation_unavailable` rather than selecting an older base or rebuilding from visible Conversation. A configuration change known to invalidate continuity is rejected atomically at configuration admission; request preparation separately validates its selected replay recipe. Codex V1 starts with the proven same-concrete-model rule and records both requested model and served-model evidence; broader compatibility requires adapter fixtures rather than a permanent Core equality rule. If compaction cannot produce a fitting compatible request under the allowed policy, the terminal-failure transaction records `ResourceExceeded` with any still-unprojected messages intact and inapplicable. A pre-request failure creates no fake model request; failure after an admitted request preserves its Resolution and already-applied context. Compaction never creates another Conversation or durable-history limit.

## Terminal failure with pending input

Accepted in [Choose terminal failure semantics for pending User Messages](https://github.com/DivyanshGolyan/onepage/issues/102#issuecomment-5550577434).

A definitive inability to continue may settle a Turn as failed once every other semantic obligation is resolved: no unresolved Operation or admitted effect, actionable permission or required Tool Result publication may still change its meaning. Still-unprojected User Messages alone do not prevent this failed outcome. The same transaction inserts the unique failed Turn Outcome and releases logical Session occupancy; the outcome itself makes those messages inapplicable. Requiring their prior inapplicability would create a circular settlement condition. There is no separate early failure intent or per-message phase.

The failed outcome carries typed causal provenance. A pre-request validation failure such as `ResourceExceeded` belongs directly to the outcome, without inventing a Model Operation, manifest or Attempt. Its failure provenance must identify the configuration revision and canonical input frontier selected for the failed preparation; it must not reconstruct those from later current settings or rely on a removed Turn-wide configuration binding. The minimal durable reference mapping remains to be specified; only evidence not recoverable from canonical facts should be retained additionally. Failure established by an accepted Operation Resolution references that same Turn's resolved Operation ID and reads its immutable Resolution value rather than copying provider evidence or retaining a second result identity. Tool errors, retryable model failures, recoverable provider overflow, and direct model interruption do not automatically fail the Turn. Cancellation retains its earlier Run intent while effects settle; failure cannot erase another Operation's obligations.

All User Message admissions and existing projections remain immutable. Inspection derives `applied` from the projection relation, `not applied` from a missing projection plus the failed outcome or applicable cancellation authority, and otherwise `pending` for eligible work. Applied means entered Conversation/model context, not proof that the provider consumed it. Unapplied content and its cause remain inspectable even without a Conversation Entry. Later requests project only the current Turn's applicable messages and use canonical context; they never revive excluded admissions from earlier Turns. Reusing their content requires a new explicit submission.

SQLite commit order decides admission versus failure. A message committed first remains attached to the old Turn and becomes not applied if still unprojected. A Session-current submission after failed-outcome/occupancy-release commit begins new work; a command bound to the old Turn rejects. Rollback leaves the original pending facts. Lost acknowledgment after commit is recovered from the existing outcome, without redispatch or reinterpretation under current ambient settings. A storage fault that prevents commit cannot be reported as a durable failed outcome or occupancy release. A crash alone is neither failure nor cancellation. Physical cleanup and credit release retain their effect-specific owners, and late evidence cannot replace a committed Resolution or Outcome.

## Failure and future scope

Closed failures include stale identity, conflicting replay, invalid canonical data, rejection under independently enforced resource limits, unsupported provider output, storage failure, and corrupt referenced content. Observer state and diagnostics cannot carry authority.

V1 excludes branching Conversations, edit/delete, attachments, automatic provider/model fallback, incompatible provider/model switching, lossy handoff, multi-host coordination, provider registries, dynamic tools, MCP execution, generalized scheduling, retained workflow VMs, and storage migration compatibility. A model change between Turns is allowed only when the selected adapter demonstrates continuation compatibility. A future conversation fork should create a new Session with explicit ancestry rather than turning every Session into a tree.
