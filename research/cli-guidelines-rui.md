# CLI guideline evidence for Rui's shared human journey

Research input for [#293](https://github.com/DivyanshGolyan/rui/issues/293) and the interaction-model decision in #287, using the [caller trace](https://github.com/DivyanshGolyan/rui/issues/286#issuecomment-5825363367). This note compares the primary [Command Line Interface Guidelines](https://clig.dev/) with Rui's accepted authority boundaries; it does **not** select an interface model, TUI, or framework. The clig.dev section anchors below were checked against the page's heading IDs on 2026-09-25.

## Findings

| Guideline and primary source | Rui case | Applicability or conflict | Concrete question / constraint for #287 |
| --- | --- | --- | --- |
| [Help](https://clig.dev/#help): concise default help, full `-h`/`--help`, examples first, links to detailed docs. [Ease of discovery](https://clig.dev/#ease-of-discovery): suggest the next command and recovery action. | The current no-argument and `--help` invocations both print every form with `InvalidArguments` and exit 1, without an example or return path (`src/cli.zig:10-19,514-534`; checked on the local Debug binary). README narrates the actual sequence (`README.md:49,57-65`); #286 found no `wait` or Store-wide Session/record browser. | Strong fit for reducing discovery cost, provided examples say that `message` acceptance is not completion and retain explicit `serve`. Generic “retry” advice would be unsafe unless it identifies the original capture/key and distinguishes replayed admission from current observation. | What minimum help/next-step vocabulary lets a person discover configure → message → inspect exact Action → decide → observe/read, and after restart locate the **same** Store/capture/key, without implying Host auto-start or inventing recovery authority? |
| [Output](https://clig.dev/#output): human-readable output is paramount, use TTY detection, preserve machine-readable output and offer JSON when structure is needed. [The basics](https://clig.dev/#the-basics): primary/machine output to stdout; diagnostics to stderr. | Commands currently write raw Host JSON to stdout; content reads stream bytes there (`src/cli.zig:394-505`). The accepted future boundary says deterministic Markdown by default, explicit JSON, selected data on stdout and diagnostics on stderr (`ARCHITECTURE.md:668`). | Human-first defaults can help a terminal user, but Rui is program-first: a TTY renderer cannot collapse accepted/queued/completed, omit exact identities, or alter piped framing. JSON must remain an explicit stable rendering, not terminal decoration stripped heuristically into an undocumented format. | Which output modes and TTY defaults expose the same typed authoritative facts, and how are stdout framing and diagnostics kept deterministic for pipes? |
| [Errors](https://clig.dev/#errors): rewrite expected errors for humans, explain remediation, keep signal high. [The basics](https://clig.dev/#the-basics): zero on success, nonzero on failure. | CLI prints Host bodies before returning `HostInvocationFailed` for non-accepted HTTP statuses (`src/cli.zig:267-268,503-505`); read failure does likewise (`src/cli.zig:449-470`). Contractually, observed work failure/cancellation is data, invocation/access/render failure is nonzero (`ARCHITECTURE.md:664,668`). #286 observed stale denial as rejected data with exit 0, while unavailable failed result exited nonzero (comment line 8). | Helpful remediation fits, but “failure” cannot be flattened: rejected command, delivered failed/cancelled work, inability to observe, and uncertain lost reply have different authority. A lost reply must not recommend a new key. | Define the exit/status matrix before wording: which valid domain outcomes remain zero, which invocation failures are nonzero, and what exact recovery hint points to `observe`/original-record `retry` without claiming whether an unobserved command committed? |
| [Arguments and flags](https://clig.dev/#arguments-and-flags): prefer descriptive flags, sensible defaults, stdin `-`, never require a prompt. [Configuration](https://clig.dev/#configuration): place settings by variability and define precedence. | Mutations require Store, record, key and Session repeatedly (`src/cli.zig:245-391`); message supports a text file/`-` (`src/cli.zig:271-287`, usage at 521). #286 separates unavoidable exact identities from avoidable repeated selection (comment line 17). Clients capture exact input before send (`src/client.zig:109-123,305-346`) and refuse replacing a published record (`src/client.zig:404-477`). | Remembering stable Store/Session may lower effort, but an implicit default must be visible and cannot change the authoritative destination, overwrite a capture, regenerate a key, or blur original versus new intent after restart. Configuration convenience is client state, not Host/core authority. | Which selections may be remembered, where is that state shown/overridden, and what durable mapping from each human submission to exact capture/key survives a fresh process without silently choosing new intent? |
| [Interactivity](https://clig.dev/#interactivity): prompt only on a TTY, provide flags for every input, disable prompts when stdin is not interactive. | Exact Action approval requires inspecting the Action ID and full arguments, then recording one exact allow/deny (`src/cli.zig:344-375,475-500`; #286 comment lines 6, 13). Pending permission and simultaneous work cannot be summarized as one Session phase (#286 line 17). | TTY guidance is useful only as presentation. No prompt may hold or become permission authority, auto-select an Action, consume piped message input, or make noninteractive behavior ambiguous. Exact flags/records must remain available. | If a TTY offers a guided approval step, how does it display and bind the exact Session/Action/payload and save its own decision key/record? In a pipe, what explicit command and parseable pending-Action fact replace every prompt? |
| [Dangerous actions](https://clig.dev/#arguments-and-flags): confirmation should match risk and remain scriptable; severe actions can require typing exact identity. | Allowing Bash authorizes only the inspected Action; no effect occurs before approval (#286 lines 6, 13). After custody loss, Bash becomes indeterminate and is never automatically replayed (`ARCHITECTURE.md:405-417,469-475`; `VERIFICATION.md:189,195`). | “Confirm dangerous work” aligns with exact Action approval, but a generic `yes`/`--force`, session-wide approval, or automatic retry is too weak. The exact Action and arguments—not terminal prompt state—are the authority. | What explicit confirmation binds to the exact Action while staying scriptable, and how must wording prevent a user from interpreting an indeterminate Bash result as safe to rerun? |
| [Robustness](https://clig.dev/#robustness-guidelines): design reruns to recover after interruption and make progress visible. | A command commits acceptance separately from completion; `retry` sends the retained record (`src/client.zig:165-186`), while result and observation are separate (`src/client.zig:189-223`). #286 demonstrated a lost reply, replayed original queued acknowledgement, saved completed result, explicit fresh Host, and no repeated effect (lines 7, 14). | The guide's convenient “up-arrow and rerun” assumption conflicts unless rerun means the same immutable capture/key. Reissuing `message` with a new key can create new work; replaying an uncertain Bash is forbidden. | How does the human path distinguish **recover original acknowledgement**, **observe current completion**, **read saved result**, and **submit new intent**, especially after explicit Host restart? |

## TTY versus pipe scenario

- **TTY (proposed presentation, not observed behavior):** after a message admission, render “Accepted; not complete” and offer commands that preserve the exact key. If inspection finds Action 7, show the exact Bash payload before offering explicit Action-bound allow/deny. A restart notice should tell the user to start the Host against the same Store and recover the same capture/key; it must not start the Host or generate a replacement request silently.
- **Pipe/noninteractive constraint:** `rui ... --json | jq ...` must emit only the selected structured data on stdout, never prompt, and return without terminal decoration. Pending Action data must be discoverable and approval must require an explicit exact-target command. If a reply is lost, automation reuses the retained record/key and separately observes/reads the saved outcome.

## Concrete observed and proposed text

**Source-observed (local current binary, exit 1, stdout empty; stderr excerpt; source `src/cli.zig:514-534`):**

```text
usage:
  rui message --store PATH --record FILE --key KEY --session REF --text FILE|-
  rui retry --store PATH --record FILE --kind configure|message|...
error: InvalidArguments
```

The Debug binary also printed an internal Zig stack trace. This is a concrete diagnostic observation, not evidence about a release build. Actual #286 message output was raw JSON with `answer.status=accepted`, `queue.status=queued`, and `execution.status=queued`; retry returned the original queued admission with `replayed=true`, not current completion (#286 comment lines 6-7).

**Proposed wording for comparison only (not a contract or interface choice):**

```text
Accepted message key “second”; work is queued, not complete.
Next: observe the same key. If this reply is lost, retry record second.json;
do not submit a new key to recover this request.
```

This illustrates clig.dev's next-step advice while preserving Rui's acceptance/completion and exact-capture distinctions. The final model must decide whether such prose is a TTY rendering, help example, or another mode; it must not contaminate machine output.

## Authority and limits

Rui's current scope and restart guarantees are summarized at `README.md:7-11`; the implemented journey and its explicit-host/permission limits are at `README.md:33-65`. The controlling interface contract is `ARCHITECTURE.md:635-668,700-708`; exact caller recovery and uncertain effects are required by `VERIFICATION.md:65-67,189-195,310-316`. The resolved #286 evidence was deterministic fixture/source and agent-operated CLI evidence, not a human-use study. No TTY usability observation, live-provider experiment, power-loss test, or uncertain-effect manual trace was performed here. clig.dev is design guidance, not evidence that any proposed wording is understandable or that a particular client-state ownership model is correct.
