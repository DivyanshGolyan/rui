# Production qualification evidence

## Current checks and qualification

[Issue #247 scratch and canonical-storage acceptance, 2026-09-22](../../VERIFICATION.md#scratch-and-cleanup): the `issue-247-scratch-ownership` working tree based on `799d411` passed `zig build check`, `zig build test-logic`, `zig build bash-owner-integration`, `zig build measurement-check`, `zig build cross-check-linux`, focused production-owner and real-SQLite tests, Python fixture compilation and diff hygiene in the Linux x86-64 orb. Independently authored nonzero baselines prove failed model-request writes retain the submitted reservation, ownership transfer neither duplicates nor refunds charge, and failed/partial removal retains only actual names, aliases, entries and bytes until successful or already-absent reclamation releases each once. Retention evidence separates zero-byte entry capacity from byte capacity, protects producer reservations, preserves both saved Bash answers byte-for-byte when optional publication fails, and leaves canonical success unfenced. A temporary over-refund mutation (`charged + 1`) made the partial-cleanup oracle fail at the independently expected 44-byte total and was removed. The pinned SQLite path now exercises real second-connection `SQLITE_BUSY`, import-step `SQLITE_FULL` with SQLite's automatic rollback, fresh-reopen absence of every command/Session/content prefix, and allocation denial above the configured hard heap ceiling; existing model-output checks retain enabled-versus-disabled spill-counter assertions. An independent review using an interpretation of Rich Hickey's simplicity lens found no new production authority or lifecycle state and identified one imprecise FULL-phase oracle; the import-step result mapping and test were corrected before the gates above. The macOS-only growth/spill process runner refused this Linux host before executing, so no new process resource artifact or macOS runtime claim follows. Broader ENOSPC/VFS/platform sweeps, power loss and startup reclamation remain with #231/#249/#248; this slice found no Host lifetime/Store-release correction affecting #248 or #186.

[Issue #168 direct-core development baseline, 2026-09-22](model-output/2026-09-22-readiness-capacity-1.json): exact clean revision `e347d778f5696fe735954b4f208651f0a4ca5cb5` passed the four requested native macOS arm64 runners once without threshold, cadence or retry changes. The [Active Capacity 1](model-output/2026-09-22-readiness-capacity-1.json) and [Active Capacity 8](model-output/2026-09-22-readiness-capacity-8.json) artifacts each completed two same-Host 60-second fill/drain rounds: respectively 1,800 and 14,400 events per round, exact request-set digests, exact original key/Session/input/Operation/result audit identity, complete delivery, and final zero custody and scratch. The second retained-idle snapshots grew by 868,352 and 1,048,576 physical-footprint bytes; capacity 8 also grew by 1,916,928 RSS bytes while capacity 1 RSS fell by 1,228,800 bytes. Both remained below the 24 MiB milestone target with no descriptor, custody or scratch growth, but the counters cannot attribute retained pages among the allocator, SQLite and transport libraries, so these are retained-baseline observations rather than evidence of long-duration stability. The [queue artifact](model-queue/2026-09-22-readiness-macos.json) independently varied history through 10,000 rows, stopped admissions through 1,000 and eligible Sessions through 100; the maximum mixed case discovered work in 177 ms, then audited exactly 100 Messages, Turns and Operations in oldest-first admission order and exactly 100 provider requests. Its conservative physical-footprint upper bound was 5,441,024 bytes. The [control artifact](model-control/2026-09-22-readiness.json) exercised 100 live streams and passed the unchanged 1-second limit: exact interruption acknowledgment was 26.593 ms, Session-stop p95 was 34.352 ms, control-first p95 was 77.896 ms, real-settlement p95 was 138.541 ms and the blocked-result control was 37.601 ms. Natural settlement overlap happened in this run but remains diagnostic. Final real-settlement custody and scratch were zero. Two incomplete-ingress drain rounds restored descriptor counts but the second retained snapshot remained 9,124,864 physical-footprint bytes above idle and 2,097,152 bytes above the first drain; this is recorded process-page/cache retention, not treated as a resource-owner leak or as long-churn qualification. Deterministic loopback HTTP does not qualify TLS or a live provider; process termination and SQLite commit evidence do not qualify filesystem power loss; two rounds do not qualify long-duration churn; and this model-only baseline does not qualify the assembled 1,000-operation model/Bash/Edit product owned by #231/#249.

[Pre-fix execution-service recording, 2026-09-19](execution-service/2026-09-19-linux.json): preserve this historical artifact for its recorded revision `5b46625910cb9111b06767e42a9171d3dcb3c760`, not as qualification of the review fixes. Its controls were issued after request preparation and before completion-burst release; its uninterrupted-work metric selected individual sub-phases; and invalid measurement status did not fail the command. Those limitations prevent using its successful labels to establish the required contention behavior. Its platform and empty-cache availability limits remain unchanged.

[Execution-service Linux qualification, 2026-09-20](execution-service/2026-09-20-linux.json): historical v2 evidence at exact clean revision `5677ff864bc979943b7dfe15f478317418fc7e46` passed all ten independent preparation, replay, completion-burst and owner-population cases without retries. Preserve this artifact and hashes (`d8d07cb4541d9f419585179dced976fc7df48b19cc45aaa9054e4a4941c2185e` for the review summary, `10a3020b9346c3e0e4617cd5d5f00d4e68829ce296f1ec3839d7bff857658285` for the [compressed raw report](execution-service/2026-09-20-linux.raw.json.gz)); it does not qualify the later ready-obligation fixture.

[Execution-service Linux v3 qualification, 2026-09-20](execution-service/2026-09-20-linux-v3.json): historical v3 evidence at exact clean revision `89370d0c3b7e36ae3310a626127f5f225aa7232e` passed all ten independent preparation, replay, completion-burst and owner-population cases without retries under the one-shot readiness rendezvous. Preserve this artifact; turn ordering is now proved by `zig build test-logic` against the production turn, not by this gated Host-process matrix. The linked review summary retains verdicts, provenance, parameters, resources and aggregate intervals; its hash-addressed [compressed raw report](execution-service/2026-09-20-linux-v3.raw.json.gz) losslessly preserves every observed interval (`a2c7f39a6701ee905245a632359fccdcb9bb2b740767b87b25bd8d81fe86757f` / `bbbd9d8049db7a3669b9bf237946b876a436ce6e9e57cf48c1511816929dd515`). Maximum measured lifecycle-service gaps were 25.1–76.5 ms; maximum stop-to-effect and deadline-to-service intervals were 50.3 ms and 9.2 ms. Gated intervals are perturbed diagnostics, not production latency. Linux x86-64 structural growth remains `(1280 - 1264) × 1000 + 9480 = 25,480` bytes at default capacity. Historical 2026-09-20 v2 artifacts are unchanged. The runner does not measure whole-process peak memory, and this Linux orb cannot provide macOS physical-footprint/runtime qualification, four-target native compilation, live-provider/TLS behavior or power-loss evidence.

[Execution-service macOS v3 qualification, 2026-09-20](execution-service/2026-09-20-macos-v3.json): historical v3 evidence at exact clean revision `89370d0c3b7e36ae3310a626127f5f225aa7232e` passed all ten independent cases without retries on Darwin arm64 (MacBookPro18,1). Preserve this artifact; it does not qualify later production-turn extraction. The linked review summary retains verdicts, provenance, parameters, resources and aggregate intervals; its hash-addressed [compressed raw report](execution-service/2026-09-20-macos-v3.raw.json.gz) losslessly preserves every observed interval (`058b5a0f286cf7ad5b9d9624dce0457eaab148562563b8efe0d46db89cc16dd5` / `f01486b880af6f96f36fd651f4bc48ac92d7206593bd3d7dc5644be49eef6215`). Maximum measured lifecycle-service gaps were 27.1–87.4 ms; maximum stop-to-effect and deadline-to-service intervals were 69.3 ms and 18.7 ms. Gated intervals are perturbed diagnostics, not production latency. macOS arm64 structural sizes are a 1,248-byte execution slot plus a 9,480-byte shared preparation workspace. Historical 2026-09-20 Linux v2 artifacts are unchanged. The runner does not measure whole-process peak memory. Live-provider, TLS and power-loss evidence remain outstanding.

The current `zig build measure-execution-service` path is ungated empirical measurement of ordinary service intervals and request/answer bytes. Turn ordering, one-completion and one-preparation-advance invariants, wait selection and missing-hint reconciliation belong to `zig build test-logic` and the complementary real-Store tests.

| Purpose | Entry point |
| --- | --- |
| Native tests and process integration | `zig build check`; Zig tests plus [admission](../integration/admission_integration.sh), [dispatch](../integration/dispatch_integration.py), [Bash execution](../integration/bash_integration.py), [Bash lifecycle faults](../integration/bash_lifecycle_integration.py), [controls](../integration/control_integration.py) and [process checks](../integration/host_process_test.py) |
| Shared production-turn logic | `zig build test-logic`; [execution turn](../../src/execution_turn.zig) and portable custody/scratch owners without Host, Bash or transport fixtures |
| Supported-target compilation | `zig build cross-check`; use `zig build cross-check-linux` in Linux-only environments and `zig build cross-check-macos` where Xcode or Command Line Tools provides the Apple SDK |
| Go measurement-runner tests | `GOTOOLCHAIN=local go -C tests/qualification mod download`, then `zig build measurement-check` |
| Resource qualification | The eight `zig build measure-*` commands below; shared Go support in [measurement/](measurement/) |

[VERIFICATION.md](../../VERIFICATION.md#canonical-gates) owns gate selection and required evidence. Recorded results below describe their tested revision and limitations, not every later revision. Historical failures retain their original verdicts.

[Direct Session Current/Full profiles, 2026-09-17](../../src/store.zig): the issue #226 working tree rebased on `6a192a2` passed `zig build check` with 127/127 native tests and all admission, dispatch, control and process integrations, `zig build measurement-check`, `zig build cross-check-linux`, Python compilation and diff hygiene in a Linux orb. Compiled protocol/client fixtures prove omission selects Current, only explicit `full` selects Full, unknown selectors fail, and worst-case escaped Full requests attain their independently derived bound. Current retains #199's active-Turn Action/rejection outcomes, reports work using canonical Operation/permission facts and counts pending Messages above the latest stop cutoff; a 100-versus-10,000 excluded-admission fixture keeps SQLite VM work flat. Full uses the same closed pending/excluded/applied Message projection as keyed command observation, so active or completed Messages covered by later stops cannot also become excluded. Full emits every named Stage 1 collection in deterministic order, streams otherwise unreadable content through a 4 KiB window, and materializes each Conversation value once in its owning same-report collection while retaining immutable length/digest identity at the ordered Conversation occurrence. Session revisions likewise retain every snapshot while the first cross-field occurrence owns each immutable instructions/output-schema value and later occurrences directly name that owner; semantic tools and permission rendering are shared with Current. A 64 KiB worst-case escaped value succeeds under a 512 KiB report budget that cannot hold repeated renderings. Full derives earliest-stop Message exclusion through one ordered Message/stop merge; a factorial 32/128 Message-by-stop fixture rejects multiplicative SQLite work while equal cutoffs retain commit-order attribution. Rejected Model Interruption targets retain exact canonical `u64` text, including zero and values above signed-64 range, without fencing observation. Completed Message answers remain immutable references retrievable by their command keys rather than multiplying answer bytes by selected-Message count. Production contention alternates five Current and five Full captures across all ten ordinary places; protected controls and real settlement passed with 2 overlapping controls, 5.1 ms blocked-result control acknowledgment, 5.8 ms overall control p95, 9.5 ms control-first p95 and 5.9 ms real-settlement p95. Full report scratch-unlink failure remains an observation failure and triggers the established effect-aware Host shutdown/recovery path. The Store schema moved to version 13 for Current's Session-scoped pending and latest-Turn indexes. No resident report collection, public cursor, detached delivery queue, generic content endpoint or new allocation owner was added: one 4 KiB encoder window streams into charged scratch, the Store mutex and statements release after complete capture, and delivery borrows 64 KiB reads while retaining only the ordinary connection and sealed scratch. The accepted macOS model-queue footprint evidence remains recorded separately; this report-specific run did not collect macOS Host physical footprint. Live providers, TLS, filesystem power loss, later Stage 5 Session/Workflow inventory and the complete 1,000-operation product remain unqualified.

[Model queue and control race implementation, 2026-09-16](model-queue/main.go): clean revision `146a4d8` passed `zig build check`, `zig build measurement-check`, `zig build cross-check-linux`, Python compilation and diff hygiene in a Linux orb. The native check includes rollback and post-commit preparation failure, arrival/selection/settlement/restart ordering, successor discovery after failure or dropped hints, capacity-release progress without a new submission, slow/partial ordinary exchanges, control contention, and the new queued-stop plus retry-wait stop/interruption reuse cases. The latter prove through real clients that replay preserves the original cutoff/target after newer work and restart, while excluded input never revives. Store evidence holds eligible work fixed while independently growing stopped pending work and terminal history, then grows eligible work separately; it verifies oldest selection and SQLite VM-step behavior, including history-independent pending-index work. The queue runner adds independently varied 0/1,000/10,000 history, 0/100/1,000 stopped admissions and 1/10/100 eligible Sessions, exact complete Operation-admission and settlement order, a 2,000 ms cold-start discovery target and a held mixed 10,000/1,000/100 resource sample. Its behavior errors, unavailable measurements, target misses and diagnostics have distinct results. At this historical revision the Linux orb could not execute the then-Mac-only runner; the portable result below supersedes that availability limit. No new production allocation owner was introduced. Bash composition remains #168; live-provider, TLS, macOS physical footprint, filesystem power loss and the complete 1,000-operation product remain unqualified.

After rebasing onto `7436321`, clean revision `02ccc6a` passed `zig build check`, `zig build measurement-check`, `zig build cross-check-linux`, Python compilation and diff hygiene with the standards/performance follow-up and upstream control-race changes combined. Runnable selection uses the pending-age index to identify one eligible Session and the Session-order index to aggregate only that Session. Store tests require flat single-selection VM-step work and near-linear complete-drain work across 10/100/1,000 eligible Sessions, while preserving independent history and stopped-population checks and exact order.

[Portable Linux queue qualification, 2026-09-17](model-queue/2026-09-17-linux.json): clean revision `3b89892b04d395ccc1be77d8b4cbacd6468de04b` passed every discovery target, the public last-Message result check and one closed execution audit in exact Operation-admission order. That audit enumerates the closure of every Message, Turn and Operation touching a controlled eligible Session, requires exactly one of each per expected execution, validates their bindings and terminal outcomes, and independently requires one provider request per expected Operation. Query-level negative cases prove that a duplicate Operation hidden behind the Turn's current pointer and an extra Turn without an Operation both fail. Discovery was 6–7 ms across the independent populations and 7 ms for the maximum mixed 10,000-history/1,000-stopped/100-eligible case; terminal observation for that mixed case was 695 ms. While its first request was held, the Host used 6,692,864 RSS bytes, 13 descriptors and a 5,361,664-byte database. The runner builds its shell from Rui's pinned SQLite instead of a system executable, uses the public command API to observe terminal settlement without contending with Host writes, and records portable RSS/virtual memory/CPU/thread/descendant/descriptor/I/O counters. Descriptor qualification uses the process's descriptor count; separate `lsof` row counts remain diagnostic. Queue behavior passed, but the aggregate result is **unavailable** because Linux cannot supply the accepted macOS physical-footprint counter; portable collection passed and the unavailable target is not reported as zero or success. Its serialized 256 MiB threshold is superseded by the accepted 24 MiB model-queue target; Linux still cannot establish either macOS-only verdict. Case, observation, drain, audit and provenance failures are retained as serialized evidence before the runner exits nonzero. The macOS result below supplies the missing aggregate counter. With this evidence update, `zig build check`, `zig build measurement-check`, `zig build cross-check-linux` and diff hygiene passed in the Linux orb.

[macOS queue qualification, 2026-09-17](model-queue/2026-09-17-macos.json): clean revision `c709eec27ae025475baa7e605b4f58c61a12e3ec` passed every independent 0/1,000/10,000-history, 0/100/1,000-stopped and 1/10/100-eligible case, plus the maximum mixed 10,000/1,000/100 case. The mixed case discovered its first request in 47 ms against the inclusive 2,000 ms target and completed public observation in 4,100 ms. Its closed audit found exactly 100 Messages, Turns and Operations in oldest-first admission order, each with the expected permanent provider failure, and the independent endpoint counted exactly 100 requests. On a MacBookPro18,1 with an Apple M1 Pro (10 logical CPUs), 16 GiB RAM and macOS 15.7.7 (build 24G720; Darwin 24.6.0 arm64), the held mixed population measured 5,653,504 bytes lifetime-peak physical footprint with 512 bytes rounding tolerance: a conservative 5,654,016-byte upper bound, below the accepted 25,165,824-byte target. Portable counters were 10,190,848 RSS bytes, 420,728,995,840 virtual bytes, 0.005604458333333333 user and 0.005804708333333333 system CPU seconds, five threads, zero live descendants, 16 process descriptors, zero disk-read bytes and 344,064 disk-write bytes; the database was 5,369,856 logical and 6,344,704 allocated bytes. The run used Zig 0.16.0, Go 1.27.1, gopsutil 4.26.7, SQLite 3.53.4, curl 8.22.0 and OpenSSL 3.6.3; the JSON records dependency and binary hashes. `zig build check`, `zig build measurement-check`, four-target `zig build cross-check`, JSON invariant validation and diff hygiene passed on the tested revision. Reproduce output capture with `zig build measure-model-queue -Dmodel-queue-output=/absolute/path/to/result.json`. Deterministic loopback provider behavior does not qualify TLS or a live provider; Bash composition remains #168, process termination is not power-loss evidence, and this model-only workload is not the complete 1,000-operation product.

[Call classification and Bash denial](../integration/dispatch_integration.py): the development trace feeds valid Bash, unknown-tool and malformed-argument calls through loopback HTTP and the production provider interpreter. Core classifies them against the frozen catalog in one Store transaction, preserves valid siblings as exact Actions, and saves stable call-local rejections without Action, permission, Attempt or custody. The trace pages pending requests without denying an earlier sibling, reads exact escaped identities/arguments, loses a keyed denial reply, restarts, replays its answer, and recovers the denied Action's original identity, descriptor and outcome alongside rejection facts and its unresolved sibling. It separately proves stale denial after a completed stop. For the actual denial/stop race, a test-only owner phase gate holds the first control after observed Store-mutex acquisition until the competing control is observed queued; both commit orders assert exact denied/cancelled sibling outcomes, cancelled Turn terminality, zero custody and no Bash launch. Store tests cover mixed-call order, schema-invalid arguments, deterministic unavailable-tool rejection, frozen-catalog changes, SQL invariants, rollback, ask→bypass provenance and reopen of the exact denied row and call content. The cardinality test validates 1,000 trustworthy envelopes from one sealed response with fixed-size resident output and exactly 520,000 charged metadata bytes: five 104-byte disk records per call, including the already-required private output item. Canonical descriptor lengths remain consumer-bounded and stream through 4 KiB windows. No fixture launches Bash. This is deterministic development evidence only: Bash execution and Action Attempts are unavailable in this slice; terminal-result acceptance provenance, complete-coverage continuation and derived Tool Result rendering belong to the next slice; and this does not qualify allow-once, process execution, model continuation, a live provider, macOS runtime, Linux filesystem durability, power loss or end-to-end proposal-load memory/latency.

[Issue #198 acceptance follow-up, 2026-09-16](issue-198-acceptance-2026-09-16.json): clean revision `1cf4a17c108d15e68745f80464c8abd36f604165` passed the canonical native/process and Linux cross-target gates. The manifest records the machine, pinned dependencies, exact inputs, source hashes, commands, results and qualification limits. macOS cross-compilation and runtime remain untested because the Linux orb has no Apple SDK or `xcrun`; the behavioral limits in the preceding paragraph still apply.

[Rejected/denied-call continuation, 2026-09-17](../integration/dispatch_integration.py): clean revision `12a15c2e06bd8cabb454a04410d14445f6d69e5f` passed `zig build check`, `zig build cross-check-linux`, `zig build measurement-check`, Python compilation and diff hygiene in a Linux orb. The production provider path accepts mixed Bash, unknown-tool and invalid-argument calls, retains each rejection or denial's canonical content and acceptance position, and withholds continuation while a sibling remains unresolved. The fixture stops and restarts the Host after each reverse-order denial and after complete coverage, then verifies one successor request, exact call IDs, original-call-order `function_call_output` items and pending input after the complete group. Store tests cover complete-coverage restart, stable after-position reads, long-name error bounds, import rollback and canonical-read fencing; schema checks require one outcome on each call owner and no Tool Result table, batch or publication marker. Historical views retain one bounded cached group descriptor during serial request preparation; durable growth is two nullable content/position fields per call owner plus one Session-history index, while request and report bytes remain scratch-charged. Empty-cache dependency retrieval was attempted twice but unavailable because Zig reported `ResolvConfParseFailed`; ordinary pinned-cache gates passed. macOS cross-compilation/runtime, live-provider behavior, power-loss durability, successful Action execution/results and the assembled 1,000-operation product remain unqualified.

[Issue #199 review follow-up, 2026-09-17](../integration/dispatch_integration.py): clean revision `6d83eeee17c6f89b588da4877a354409151650fa` passed `zig build check` (116 Store tests and all process integrations), `zig build cross-check-linux`, `zig build measurement-check`, Python compilation and diff hygiene in a Linux orb. The provider fixture now continues with no new Message, crashes before and after successor admission, and recovers one successor Operation with replacement Attempt 2 and one exact ordered result group. It also distinguishes same-key denial replay from a fresh stale denial. Store corruption cases independently remove a call, required Action and required result content and require history or inspection to fence; complete traversal grows groups and calls per group independently under a near-linear SQLite VM-step bound. Historical discovery advances by immutable source Operation and validates one source at a time, retaining bounded ephemeral cursor/group facts and no new durable state. The earlier empty-cache, macOS, live-provider, power-loss, successful-Action and assembled-product limits remain.

[Issue #200 Bash execution acceptance, 2026-09-17](issue-200-acceptance-2026-09-17.json): exact implementation revision `ba07bb67b52ef51ac5c7c9eef2aec3b628f1bbb1` passed canonical Linux and native macOS qualification once without retries or cadence changes. Both `zig build check` runs covered serial admission, dispatch, focused Bash ownership, the complete Bash journey, lifecycle faults, controls and Host-process recovery; macOS also compiled all four supported targets, while Linux passed the measurement audit. The production path proves exact authorization, one-shot launch, timeout/stop/shutdown, complete capture, settlement/continuation and indeterminate recovery without replay. One phase-independent rule freezes finite capture tails after group absence or cleanup deadline; escaped pipe holders observe hangup under reap, reap-watchdog, group-probe and cleanup-watchdog faults while process custody remains retained until trustworthy retirement. Named-scratch and retained-output cleanup share removed/already-absent/retryable reclamation semantics, producer aliases close before retention publication, and consuming preparation transfers leave one actionable owner with exactly-once accounting release. Linux Bash samples retained exact scratch accounting with 7–15 descriptors and 7.6–9.6 MiB RSS/HWM. The shared slot is 1,264 bytes on Linux x86-64 and 1,248 bytes on macOS arm64 versus the preceding 1,216-byte model slot; at default capacity 1,000, slot growth is 48,000/32,000 bytes, tagged custody adds 32,000 bytes and the fixed FIFO allocation is 65,416 bytes. No payload-sized output/excerpt buffer is inline per slot. Live provider, filesystem power loss, detached-descendant containment and the assembled 1,000-operation mixed workload remain unqualified; the mixed workload belongs to issue #168.

[Issue #241 approved Bash Session journey, 2026-09-18](../integration/bash_integration.py): exact revision `9cbb31e2cf47b2573bede5d9009f09f5e8f15ab8` passed three consecutive focused journey runs, `zig build check`, `zig build cross-check-linux`, `zig build measurement-check`, Python compilation and diff hygiene on Linux x86-64 with Zig 0.16.0, Python 3.11.6 and Go 1.27.1. One production direct-Session trace loses and replays keyed Bash/ask configuration and message replies, then feeds raw deterministic HTTP/SSE containing one valid Bash call, one unknown tool and one invalid Bash descriptor through real lowering, capture, interpretation and import. Public inspection identifies the exact pending Action and arguments; keyed allow-once replay neither duplicates the permit nor launches the saved relative-Workspace command twice. Two call rejections are visible before the Action settles while the endpoint remains at one request. After complete coverage, one automatic successor request carries exactly three derived `function_call_output` items in call order. A test-only release gate holds that exact Bash owner's physical cleanup without blocking Host service; the final answer and successful Action remain publicly observable while custody is occupied, no matching cleanup completion occurs before release, and the same Action completes cleanup afterward. Store-owned schema validation rejects a representative noncanonical Tool Result authority on reopen; the journey itself stays on public inspection and ordered provider output. No production owner, durable fact or allocation was added. The Active Capacity 3 trace observed 7.5 MiB cold Host high-water and 9.6 MiB high-water with three delayed-cleanup scratch files; these Linux RSS samples are diagnostics, not the accepted macOS physical-footprint measurement. Deterministic loopback does not qualify TLS, live-provider continuation, macOS runtime behavior, filesystem power loss, detached-descendant containment, fresh-process completion of this full journey or the assembled 1,000-operation workload; later #229 slices own Session reuse and restart composition.

[Issue #242 reused model/Bash Session journey, 2026-09-19](../integration/bash_integration.py): exact implementation revision `53b7da2724d05dec5ce80becddc3c4d25fb3f076` passed three consecutive focused journey runs, `zig build check`, `zig build cross-check-linux`, `zig build measurement-check`, Python compilation and diff hygiene in the Linux x86-64 E2B orb (`6.1.158+`) with Zig 0.16.0, Python 3.11.6 and Go 1.27.1. One production Session queues input behind a permission-blocked Turn containing two valid Bash calls plus unknown-tool and descriptor-invalid siblings. The independent expected transcript requires exact user/call/result ordering, distinct failed/successful Bash result association, each rejection result, one queued input, the first answer in later history and byte-identical semantic retry input. The fast Action settles first; complete coverage advances queued input without another submission, and both selected messages retain the first Turn's answer. Test-owned release gates retain the exact Bash Action attempts and failed model Attempt without blocking Host service; attempt-qualified owner events establish release before cleanup completion, the model owner remains occupied beyond its retry deadline, and release permits automatic retry and the second answer. Failure teardown releases every gate before the emergency Host kill, while the synthetic Bash wait has its own bound. Full original Message, queued-Message and Permission Decision answers replay after later work with only replay metadata changed and create no provider request. Store-owned stop completion now derives from the saved selection and canonical Turn terminality: an accepted stop over an attempted Action replies pending, later observation and replay report completed, and physical cleanup remains independent. Existing focused integrations separately prove full-capacity release starts durable work, an old Permission Decision cannot retarget an unresolved sibling Action, and contract-permitted hint suppression at pre-handoff and sealed-result boundaries grants no authority; handed-off live effects retain the required accepted-control signal. No durable completion bit, cancellation ledger, periodic live-owner sweep or new production allocation was added. Deterministic loopback does not qualify TLS, live-provider continuation, macOS runtime behavior, power loss, concurrent Session writers or the assembled 1,000-operation workload.

[Call-classification population qualification, 2026-09-17](call-classification/2026-09-17-linux.json): clean revision `73921326584f69dc58dc150641de2dc7b3319cc8` passed every Linux behavior and portable-measurement case through raw provider SSE, production interpretation/Store/server ownership and ordinary clients. `zig build measure-call-classification` independently grows valid and rejected calls through 1/32/256 at fixed Active Capacity 1 and fixed descriptor payload, then varies one valid descriptor through 16/4,096/100,000 payload bytes. The mixed case holds the response while removing Bash from current settings, proves classification against the frozen catalog, denies exact Actions without changing siblings, restarts, replays the original keyed denial and audits every call ordinal and domain-separated identity/argument digest. Stopped-Store audits require rejected calls to have no Action or permission state, every valid Action to be denied without a pending Action, and all foreign keys to remain valid. The runner records end-to-end classification admission, report inspection and denial latency; observed Host RSS/virtual memory, descriptors, threads, database and named scratch high-water marks; retained idle resources; database allocation; delayed-cleanup custody; pinned binary/SQLite hashes; and distinct behavior, unavailable, target-miss and diagnostic verdicts. At 256 calls, valid and rejected admission took 344.8 ms and 341.1 ms respectively; valid-denial p95 was 11.4 ms. Peak observed Host RSS was 7,561,216 bytes, descriptors peaked at 15, retained scratch was empty and the largest database was 462,848 bytes. The aggregate verdict is **unavailable**, not passed: this Linux orb cannot supply the accepted macOS physical-footprint counter or execute the required Mac client path. Deterministic loopback does not qualify a live provider, TLS, Bash execution, filesystem power loss or power-loss recovery. The canonical check also exercises 32-call mixed validation/capture/import/commit and metadata-handoff failures, requiring no partial calls, Actions or Bash launch, plus startup recovery of retained named scratch.

[macOS call-classification qualification, 2026-09-17](call-classification/2026-09-17-macos.json): clean implementation revision `07f9b7557e2727147900b9a7b91d14677a4793c4` passed all nine behavior cases on runner `1993-X2` (`Darwin 24.6.0`, arm64) at Active Capacity 1. Every same-Host lifecycle used real loopback HTTP/SSE production transport for the initial classified response and automatic continuation, then reached a completed public outcome, cleanup and retained idle before its lifetime peak was read. The external deterministic provider fixture is workload infrastructure and excluded from Rui-owned process memory; its two requests per case drive Rui's in-process transport/network allocations. Kernel/socket resources absent from process `phys_footprint` remain separate counters. Opt-in inherited pipes hold one ordinary CLI only after its command and output complete; the measured families cover configuration, message, denial, a 256-call inspection and an exact 100,000-byte Action read. Their lifetime peaks were respectively 3,064,832±512, 2,851,840±512, 2,966,528±512, 3,556,352±512 and 3,621,888±512 bytes. The largest Host lifetime-peak interval was 11,010,048–12,058,624 bytes. With a proven maximum one live ordinary CLI and no Host or CLI descendants, checked addition gives a conservative aggregate lifetime-peak upper bound of 15,681,024 bytes; the directly sampled de-duplicated Host+CLI current lower-bound cross-check peaked at 14,781,248 bytes. The resulting 14,781,248–15,681,024-byte interval passes the accepted 25,165,824-byte current up-to-100-operation milestone target. Sharing may be double-counted by the upper bound but no helper can be hidden. Current-main semantics are verified through public Full history and exact public Action reads after restart rather than expecting historical calls in Current. `zig build check`, explicit dispatch/control integrations with their unchanged 1,500 ms cleanup delay and 8 s watchdogs, `zig build measurement-check`, both supported-target cross-checks, Python fixture compilation, JSON assertions and diff hygiene passed. The test-only hold adds no ordinary resident process, thread, queue or payload owner; while enabled it adds two inherited descriptors to the single measured CLI. Deterministic loopback does not qualify TLS, a live provider, Bash execution, filesystem power loss or the assembled 1,000-operation product.

[Configuration admission](configuration-admission/main.go): `zig build measure-admission`; [recorded results](configuration-admission/results.json). Measures the production issue-170 Host at 0/100/1,000/10,000 dormant Sessions with fixed active capacity 1,000, plus one direct-client peak RSS. The run is macOS-only and sequential; it does not qualify concurrent connections, complete Stage 1 workloads, Linux runtime behavior or power loss.

[Message admission](message-admission/main.go): `zig build measure-message-admission`; [recorded results](message-admission/results.json). Measures the production issue-171 Host at ordinary incoming-message sizes of 0, 1,000, 10,000 and 100,000 bytes and, in a separate fresh Store, at 0, 1 and 10 queued messages in one Session, with fixed execution/client capacities. Records admission/observation latency, post-admission and lifetime-peak physical footprint, RSS and database size. Mac-only sequential evidence; model processing remains unavailable, and other targets are compile-only.

[Frozen-request dispatch](model-dispatch/main.go): `zig build measure-model-dispatch`; recorded results live beside the runner. Measures the production issue-172 Host's fixed custody/transport workspace at configured capacities 1/8/16/100, aggregate request-context inputs of 100,000/500,000/1,000,000/4,000,000 bytes with the complete serialized request size reported separately, descriptor retention after saved failure and eight overlapping reactor transfers. Mac-only loopback HTTP evidence; it does not qualify TLS trust, a live provider, Linux runtime behavior, successful output interpretation, retry/restart completion or power loss.

[Model-output load-testing sources](../../research/model-output-load-testing-sources.md) compares established approaches to offered load, achieved rate, scheduling delay, backlog and CPU accounting. Research dated 2026-09-15; it informed the rational offered-load fixture, but is not itself runtime qualification.

[Clean committed qualification, 2026-09-16](model-output/2026-09-16-clean-qualification.json): revision `b6c9761` passed pinned dependency retrieval and the native canonical check with new Zig dependency/compiler caches (17/17 steps, 97/97 tests), four-target cross-compilation, and measurement-runner tests. [Capacity 1](model-output/2026-09-16-clean-capacity-1.json) and [capacity 100](model-output/2026-09-16-clean-capacity-100.json) each passed two output rounds; [100-live-operation controls](model-control/2026-09-16-clean-controls-100.json) and [growth/spill smoke](model-output/2026-09-16-clean-growth.json) also passed. At 100, each round delivered all 600,000 events; Host CPU averaged 0.15 cores and aggregate peak footprint remained within 20.5 MiB. The manifest records commands, durations and cache limits, including the ordinary shared cache used by Linux C cross-compilation. Historical capacity-8/16 strict passes remain applicable to unchanged production code. Output and heartbeat-control loads are separate; this qualifies the model foundation on macOS, not Linux runtime, live providers, power loss, maximum throughput or the complete 1,000-operation product.

[100-operation qualification, 2026-09-16](model-output/2026-09-16-qualification.json): the local product-goal qualification changes passed the canonical check, measurement tests, [growth/spill smoke](model-output/2026-09-16-growth.json), [two 100-operation output rounds](model-output/2026-09-16-capacity-100.json) and [25 controls each starting with 100 live streams](model-control/2026-09-16-capacity-100.json). Reproduce with `zig build check`, `zig build measurement-check`, then `go -C tests/qualification run ./model-output --capacity 100 --output /tmp/output.json /absolute/path/to/rui` and `go -C tests/qualification run ./model-control --output /tmp/control.json /absolute/path/to/rui`. Recorded source hashes identify the uncommitted changes over `45270a6`; the production binary is unchanged. Output CPU averaged 0.21–0.24 cores, footprint peaked at 20.5 MiB; control p95 was 39.6 ms. Output load and heartbeat-control load are separate fixtures. Cadence remains diagnostic; no maximum-throughput, live-provider, TLS, Linux-runtime or 1,000-operation claim follows. Earlier results below retain their original stricter cadence verdicts.

[Model output](model-output/main.go): `zig build measure-model-output`; [historical full results](model-output/results.json). Measures the production issue-173 Host in three independent families. Growth cases use a 100,000-byte ordinary answer and reasoning-item counts of 1/4/32 in fresh Stores at Active Capacity 1. Capacity cases run two same-Host 60-second rounds at capacities 1/8/16 with 30 realistic 260-byte SSE events/second per stream and capacity 100 with 100 events/second per stream, recording rational pacing and verifying work counts, capture, result identity, scratch/custody release, memory and Host CPU while the same Host remains live. After both rounds and confirmed Host stop/reap, private SQLite audits verify every round-specific durable row before final qualification. Spill-on and spill-off cases both import a 100 KB answer through a 32 KiB test-only SQLite cache, verify its signed raw setting separately from actual cache-used bytes, and require respectively positive and zero spill-counter movement without changing the 4 MiB production default. The full run passed growth, spill and capacities 8/16, but remained incomplete at capacities 1/100 because the fixture's unchanged lateness/gap oracle rejected some streams despite exact work and resource observations within target. After adding two-back catch-up spacing without relaxing that oracle, exact-revision [capacity-1](model-output/2026-09-15-capacity-1-rerun.json) and [capacity-100](model-output/2026-09-15-capacity-100-rerun.json) reruns eliminated burst violations but retained the timing-incomplete result. Mac-only loopback HTTP evidence; it does not qualify TLS trust, a live provider, Linux runtime behavior, retry of interrupted Attempts, power loss or the default-capacity 1,000-operation finished-product workload. The `-projection` runner option adds 1/4/8 KiB representative cases and a 100 KB stress case, each plain and escape-heavy: `GOTOOLCHAIN=local go -C tests/qualification run -mod=readonly ./model-output -projection -output /tmp/projections.json /absolute/path/to/rui`. [Projection results](model-output/2026-09-15-projections.json) record the 4 KiB source window, metadata-only public answers, completion and delivery-with-verification timings, and bounded measured process memory; aggregate concurrent-reader qualification remains separate.

[Model retry](model-retry/main.go): `zig build measure-model-retry`; recorded results live beside the runner. Measures the production issue-174 Host's due-indexed, age-ordered retry admission separately from delayed launch at 1/10/100 waiting-retry backlog cases and its future-only idle cost, then runs five 20-Operation temporary-failure/retry/permanent-settlement rounds for 100 total Operations at Active Capacity 16 with delayed cleanup. Store tests separately compare SQLite VM steps for a fixed tiny due set behind 100/1,000/10,000 future retries, record due-population scaling and preserve age-before-deadline selection. The runner records exact Attempt facts, custody/scratch drain, descriptors, idle CPU and aggregate process footprint as durable history grows. Mac-only loopback HTTP evidence; it does not qualify TLS trust, live-provider behavior, Linux runtime behavior or power loss.

[Model queue](model-queue/main.go): `zig build measure-model-queue`; [Linux result](model-queue/2026-09-17-linux.json); [macOS result](model-queue/2026-09-17-macos.json). A real client initializes each Store, then named-column SQL through Rui's pinned SQLite builds foreign-key-checked canonical populations while the Host is stopped. The production Host owns subsequent discovery, Operation admission, provider launch and terminal settlement. Capacity 1 holds the first request while recording the sustained population and, for the maximum mixed case, process/database resources; after release, the public command observation requires the last Message's expected failed result. The stopped Store audit then enumerates the closure of all Messages, Turns and Operations touching the controlled eligible Sessions and compares it once with the expected execution set: exactly one of each durable entity and one provider request per execution, reciprocal bindings, expected terminal outcomes and complete oldest-first Operation-admission order. The endpoint count proves request cardinality; Operation IDs do not independently identify which provider request belongs to which Operation. Cold-start discovery is the qualified end-to-end interval, while terminal-observation duration is diagnostic. Linux and macOS expose portable process counters; macOS additionally requires its physical-footprint sample. Deterministic loopback evidence does not qualify TLS, a live provider, Bash composition or power loss, and population sizes are workloads rather than quotas.

[Model control](model-control/main.go): `zig build measure-model-control`; [historical results](model-control/results.json). The historical run measured the production issue-175 Host at idle, with 10 stalled incomplete ingresses across two fill/drain rounds, with eight live model streams cancelled by one exact interruption plus seven Session stops, and with 10 fully captured reports held during both control-first settlement loss and real Store import of one streamed 100,000-byte answer while two later controls queue. It then starts 10 complete result deliveries, uses a 4 KiB test-only socket send buffer and a status-prefix-only reader to prove those handlers remain blocked, acknowledges a protected control, and independently verifies a clean reread. Records protected-control acknowledgment latency, transport disconnects, descriptors, physical footprint, retained resource deltas and output integrity. Both ingress drains released all 10 connection descriptor/socket rows; see the recorded results for run-specific OS counters. Two rounds do not qualify long-duration churn. Mac-only loopback HTTP evidence; it does not qualify TLS trust, live-provider behavior, Linux runtime behavior, tools, Workflow cancellation or power loss.
